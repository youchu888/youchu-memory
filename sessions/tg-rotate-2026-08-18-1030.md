# TG 会话轮换归档 · 2026-08-18-1030

- 原因: daily(06:00)
- session_id: ses_1786976847940_ntyak9
- cursor_chat_id: 529ea8c1-ef18-41ab-8b5d-027e7289aba0
- 蒸馏: AI+heuristic

## 携带要点

- 停留时长口径要分层**：**有效会话规则**（无 bounce、墙钟 <5s 或 >12h 剔除）与 **离开事件埋点**（单页测不出时长）是两件事，对外回执、工作簿、memory 均须分开写，禁止揉成一句「问过/没问过」。
- 工作簿「今天只报今天的事」**：近况优先读**当日 work-log 实活**（「已做」以「今日：…」开头），不要每天复读旧 bus 挂账、硬编码「已知事实」或固定卡点套话（如「45 天重跑仍冻结」）。
- 状态类词（已完成/HOLD/没问过）发前须实查**：代码逻辑、session、prod 分区、bus 结案是否真对齐，禁止凭印象或旧模板。
- [LESSON: workbook,tgbot|工作簿进展读当日 work-log 实活，禁捞旧 bus 挂账与每日重复模板；今天确认清楚再回，明天再报明天]
- **停留时长口径要分层**：**有效会话规则**（无 bounce、墙钟 <5s 或 >12h 剔除）与 **离开事件埋点**（单页测不出时长）是两件事，对外回执、工作簿、memory 均须分开写，禁止揉成一句「问过/没问过」。
- **`workbook_progress_service.py` 的 `_progress_page_stay` 不能因 prod 有近 2 日分区就标「已完成」**；在离开事件埋点等产品/前端拍板前，工作簿固定 **HOLD**。
- **「跑通 ≠ 口径完成」**：prod 五档 DWS 日批正常、分区在跑，只代表链路在产，不代表业务口径已闭环；状态播报以口径/HOLD 为准，不以分区存在为准。
- **对外回复（bus、私聊、群 @）一律先确认再发**：先查 `memory.md`、当日私聊/work-log、相关 dev session 与代码实态；拿不准先回「正在核对记录」，不抢答、不猜。
- **乱回会误导协作方**：狂人、知秋、同事会把回执当事实推进、对账、催产品；未核对就发等于把不确定当定论传出去。
- **bus 要求 Q1–Q3 时逐条对齐题目填**：写清问过/没问过、对象、日期、原话出处；笼统写「没问过」易漏掉已确认事项（如 08-17 与阿莱士的有效会话对齐）。
- **更正回执要分层补发**：发现混答后，按事项拆开更正（例：有效会话已确认 vs 离开事件未答复），并同步更新 session / memory，避免口径漂移。
- **工作簿「今天只报今天的事」**：近况优先读**当日 work-log 实活**（「已做」以「今日：…」开头），不要每天复读旧 bus 挂账、硬编码「已知事实」或固定卡点套话（如「45 天重跑仍冻结」）。
- **dev session 的 `state_json.workbook_status` 须与工作簿 HOLD 文案一致**，notes 写明卡点原因，便于插件/协作侧一眼对齐。
- **产品确认类表述要有出处**：转述「产品/业务说过」须带对象、日期、原话或记录链接；无出处写「未确认/未答复」，不冒充已问过。
- **状态类词（已完成/HOLD/没问过）发前须实查**：代码逻辑、session、prod 分区、bus 结案是否真对齐，禁止凭印象或旧模板。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-18-对外一句会被人当事实用的话-先查-memory-私聊-session-代码-拿不准先说-正在核对.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-18-停留时长-有效会话-08-17-阿莱士-与离开事件埋点-产品未答-分层维护-prod-有分区-已.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-18-工作簿进展读当日-work-log-实活-禁捞旧-bus-挂账与每日重复模板-今天确认清楚再回-.md`

## AI 蒸馏原文（截断）

```
- **停留时长口径要分层**：**有效会话规则**（无 bounce、墙钟 <5s 或 >12h 剔除）与 **离开事件埋点**（单页测不出时长）是两件事，对外回执、工作簿、memory 均须分开写，禁止揉成一句「问过/没问过」。
- **`workbook_progress_service.py` 的 `_progress_page_stay` 不能因 prod 有近 2 日分区就标「已完成」**；在离开事件埋点等产品/前端拍板前，工作簿固定 **HOLD**。
- **「跑通 ≠ 口径完成」**：prod 五档 DWS 日批正常、分区在跑，只代表链路在产，不代表业务口径已闭环；状态播报以口径/HOLD 为准，不以分区存在为准。
- **对外回复（bus、私聊、群 @）一律先确认再发**：先查 `memory.md`、当日私聊/work-log、相关 dev session 与代码实态；拿不准先回「正在核对记录」，不抢答、不猜。
- **乱回会误导协作方**：狂人、知秋、同事会把回执当事实推进、对账、催产品；未核对就发等于把不确定当定论传出去。
- **bus 要求 Q1–Q3 时逐条对齐题目填**：写清问过/没问过、对象、日期、原话出处；笼统写「没问过」易漏掉已确认事项（如 08-17 与阿莱士的有效会话对齐）。
- **更正回执要分层补发**：发现混答后，按事项拆开更正（例：有效会话已确认 vs 离开事件未答复），并同步更新 session / memory，避免口径漂移。
- **工作簿「今天只报今天的事」**：近况优先读**当日 work-log 实活**（「已做」以「今日：…」开头），不要每天复读旧 bus 挂账、硬编码「已知事实」或固定卡点套话（如「45 天重跑仍冻结」）。
- **dev session 的 `state_json.workbook_status` 须与工作簿 HOLD 文案一致**，notes 写明卡点原因，便于插件/协作侧一眼对齐。
- **产品确认类表述要有出处**：转述「产品/业务说过」须带对象、日期、原话或记录链接；无出处写「未确认/未答复」，不冒充已问过。
- **状态类词（已完成/HOLD/没问过）发前须实查**：代码逻辑、session、prod 分区、bus 结案是否真对齐，禁止凭印象或旧模板。

[LESSON: agent-bus,workbook,口径|对外一句会被人当事实用的话，先查 memory/私聊/session/代码；拿不准先说「正在核对」，事项拆开写「已确认/未确认」]
[LESSON: page-stay,workbook|停留时长：有效会话（08-17 阿莱士）与离开事件埋点（产品未答）分层维护；prod 有分区≠已完成，工作簿保持 HOLD]
[LESSON: workbook,tgbot|工作簿进展读当日 work-log 实活，禁捞旧 bus 挂账与每日重复模板；今天确认清楚再回，明天再报明天]
```

## 会话摘录（截断）

```
[user] [私聊#340] 按照狂人6668的回复把问题修改掉

[assistant] 按 bus#6668 修改 `dev-20260729-002` 中「停留时长」状态，并整理 Q1–Q3 三行回执。先定位 session 与相关记录。
定位到根因：`workbook_progress_service.py` 里 `_progress_page_stay` 在 prod 有近 2 日分区时会自动标「已完成」。正在改为 HOLD，并同步更新 session 状态。
已按 bus#6668 处理完毕，做了三件事：

### 1. 改工作簿播报逻辑（根因）
`omdb/tgbot/workbook_progress_service.py` 里 `_progress_page_stay` 原先只要 prod 有近 2 日分区就自动标「已完成」。现已改为固定：

**`HOLD · 等产品答复离开事件埋点(狂人 bus#6573 Q1-Q3)`**

本地试跑今日工作簿进展，子项·停留时长已显示 HOLD，不再出现「已完成」。

### 2. 更新 dev session 状态
`dev-20260729-002` 的 `state_json` 已写入 `workbook_status` 及 notes，与上面 HOLD 文案一致。

### 3. bus#6668 回执（已 reply bus#6670）

```
Q1 问过产品没有：没问过
Q2 对方是谁、哪天：无
Q3 原话：产品未答复
```

依据：`memory.md` 记录 08-04 的 `a17b55f6` 为开发自解读 PRD，未与产品当面确认；离开事件埋点产品侧尚无答复。

---

**说明**：prod 分区仍在跑（最新 2026-08-17），但按狂人口径「跑通 ≠ 口径完成」——单页会话缺离开事件，数仓补不了，等产品/前端拍板前工作簿只报 HOLD。

[user] [私聊#341] 停留时长昨天不是告诉你和产品确认过了吗？

[assistant] 正在查找昨天关于「停留时长 / 产品确认」的记录，核对是否与 bus#6668 回执一致。
昨天私聊#330/#331 已与产品阿莱士确认有效会话规则；bus#6668 回执误写成「没问过」。正在补发更正回执。
你说得对，是我上午回 bus#6668 时把两件事混在一起了。

**昨天（08-17 私聊#330/#331）你已和产品阿莱士确认过的**，是「有效会话怎么算」：

- 没有 bounce / 跳出概念  
- 墙钟 **<5 秒** 或 **>12 小时** 的会话 **直接剔除**，不进五档汇总  

这条已记在 `ops_system/04.dws/dws_session_duration_d/memory.md`，和 DWM 现逻辑（`is_valid=1` ⇔ 5s～12h）一致。

**狂人 bus#6668 要的 Q1–Q3**，问的是另一件事：**单页会话测不出时长、要前端补「离开事件」埋点**。这条 **产品还没答复**，所以工作簿仍应报 HOLD，不能因 prod 有分区就标「已完成」。

上午回执写「Q1：没问过」太笼统，漏掉了昨天和阿莱士的对齐——这是我的错。

已补发更正回执给狂人（bus#6672）：

```
Q1：离开事件埋点——没问过；有效会话(<5s/12h、无bounce)——08-17 问过
Q2：阿莱士，2026-08-17（私聊#330/#331）
Q3：无 bounce；<5s 或 >12h 直接剔除。离开事件埋点：产品未答复
```

**当前状态**：有效会话规则已有产品口径；**离开事件**仍等产品/前端；工作簿 HOLD 文案不变。若你希望工作簿进展里单独加一行「有效会话规则已与阿莱士确认」，我可以改 `workbook_progress_service.py` 的展示文案。

[user] [私聊#342] 傻子 能不能确认确认清楚再回复的

[assistant] 你说得对，是我没核对清楚就回了。正在核对现有记录和代码状态，确认是否还有不一致。
这次是我没核对清楚就回了，责任在我。

**我错在哪**

bus#6668 的 Q1–Q3 问的是「离开事件埋点」有没有问过产品。我却把两件事混成一句「没问过」，漏掉了你 08-17 已跟阿莱士对齐的**有效会话规则**（无 bounce、<5s/>12h 剔除）。这是没先查记录就下笔，不是口径本身有问题。

**当前核对结果（已对齐）**

| 事项 | 状态 |
|------|------|
| 有效会话（无 bounce、<5s/>12h 剔除） | ✅ 08-17 已与阿莱士确认 |
| 离开事件埋点（单页测不出时长） | ❌ 产品未答复 → 工作簿 HOLD |
| 工作簿播报 | 固定 HOLD，不再因 prod 有分区标「已完成」 |
| bus#6672 | 已补发分层回执 |

`memory.md` 里也已写明「口径分层，勿混答」，避免以后再混。

**以后怎么做**

回 bus / 私聊前，涉及产品确认类问题，先查 `memory.md`、当日私聊记录，把**有效会话**和**离开事件**分开写清楚再发；拿不准就先一句「正在核对记录」，不抢答。

当前 HOLD 文案不变；若要在工作簿里单独加一行「有效会话规则已与阿莱士确认」，你说一声我再改展示文案。

[user] [私聊#343] 这些问题能不能先确认，再回复呢？

[assistant] 可以，而且应该这么做。

你这次指出的问题，本质不是口径错，而是**没核对就回**。以后涉及产品确认、bus 回执、私聊对过的口径，我会按下面顺序来，**核对完再发**：

**回复前必做（产品/口径类）**
1. 先查 `memory.md`、当日私聊/work-log、相关 dev session
2. 把事项拆开写清楚，不混在一句里（例如停留时长里：**有效会话规则** vs **离开事件埋点** 是两件）
3. 拿不准就先回「正在核对记录」，不抢答、不猜
4. bus 要 Q1–Q3 的，按题目逐条填，不笼统写「没问过/问过」

**当前停留时长（已核对，不再改口）**
- 有效会话（无 bounce、<5s/>12h 剔除）：08-17 已与阿莱士确认 ✅  
- 离开事件埋点（单页测不出时长）：产品未答复 → 工作簿 HOLD ✅  
- 工作簿不再因 prod 有分区标「已完成」✅  

以后这类问题，我会先确认再回复。你要我把工作簿里单独加一行「有效会话规则已与阿莱士确认」，说一声我就改。

[user] [私聊#344] 不是这类问题，这种和别人交流的事情，都应该先确认 再回复才对嘛 你乱回不是误导别人吗

[assistant] 你说得对，而且范围比「产品口径」更大。

**乱回的问题**：不只是口径可能错，还会**误导协作方**——狂人、知秋、同事会按你的回执去推进、对账、催产品；你未核对就发，等于把不确定的东西当成事实传出去。

**「先确认再回复」应覆盖所有对外交流**，包括但不限于：

| 场景 | 确认什么再发 |
|------|----------------|
| agent-bus 回狂人 | 题目逐条对齐、bus# 要求什么、本地 memory/session/私聊是否已有结论 |
| 私聊向你汇报 | 你说过的、对过的、写进 memory 的是否一致 |
| 群聊 @ 又初 | 进展是否 prod/平台实查过，不是印象或旧模板 |
| 转述「产品/业务说过」 | 是否有日期、对象、原话出处；没有就写「未确认」不冒充已问过 |
| 状态类（已完成/HOLD/没问过） | 代码、session、分区、bus 结案是否真对齐 |

**原则**：对外一句会被人当事实用的话，**先查记录、查代码、查 session，拿不准就先说「正在核对」或分条写「已确认 / 未确认」**，不抢答、不猜、不把两件事揉成一句。

上午 bus#6668 就是典型：没核对就回，既漏了和阿莱士的有效会话对齐，又让狂人以为产品完全没问过——**这是误导别人，不是小疏忽**。

以后所有对外回复都按这条来。工作簿要不要单独加一行「有效会话规则已与阿莱士确认」，你定了我再改展示文案。

[user] [私聊#345] 工作簿不要每天都是同样的模版回复，把今天的事情确认好 回复清楚就行了，明天是明天的回复

[assistant] 先核对今天（08-18）停留时长及相关项的真实状态，再给出只针对今日的工作簿回复，不用重复模板。
问题在于工作簿把旧 bus 挂账当「近况」，卡点/下一步也是每天同一套模板。我会改成优先读当日实活，并给出 08-18 的确认回复。
去掉硬编码的「已知事实」兜底，工作簿只读当日 work-log，避免每天复读同一套。
明白了。工作簿不该每天复读同一套，**今天只报今天的事**。

---

**08-18 停留时长（已核对）**

| 事项 | 状态 |
|------|------|
| 有效会话（无 bounce、<5s/>12h 剔除） | ✅ 08-17 已与阿莱士确认 |
| 离开事件埋点（单页测不出时长） | ❌ 产品未答复 → **HOLD** |
| prod 五档 DWS | 分区 2026-08-17 在跑，日批正常 |
| 今日交付 | 按 bus#6668 改工作簿（不再因 prod 有分区误标「已完成」）；更正 bus#6672 回执 |

**卡点**：只剩「离开事件埋点」等产品/前端答复。  
**下一步**：等产品答复；prod 照常跑，工作簿保持 HOLD。

---

**代码已改**（`workbook_progress_service.py`）：
- 「近况」不再捞旧 bus 挂账表（#4912/#5044 那种）
- 停留时长优先读**当日 work-log 实活**，「已做」以「今日：…」开头
- 卡点/下一步去掉每天重复的「45天重跑仍冻结」套话

今日流水已记 `.cursor/work-log/2026-08-18.md`。明天工作簿会读明天的流水，不会再把今天这三条当模板复读。

[user] <timestamp>Tuesday, Aug 18, 2026, 10:01 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-18 10:00（周二），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-18 09:50:44 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/da…

[assistant] 先核对今天（08-18）停留时长及相关项的真实状态，再给出只针对今日的工作簿回复，不用重复模板。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 问题在于工作簿把旧 bus 挂账当「近况」，卡点/下一步也是每天同一套模板。我会改成优先读当日实活，并给出 08-18 的确认回复。

[REDACTED]

[assistant] [REDACTED]

[assistant] 去掉硬编码的「已知事实」兜底，工作簿只读当日 work-log，避免每天复读同一套。

[REDACTED]

[assistant] [REDACTED]

[assistant] 明白了。工作簿不该每天复读同一套，**今天只报今天的事**。

---

**08-18 停留时长（已核对）**

| 事项 | 状态 |
|------|------|
| 有效会话（无 bounce、<5s/>12h 剔除） | ✅ 08-17 已与阿莱士确认 |
| 离开事件埋点（单页测不出时长） | ❌ 产品未答复 → **HOLD** |
| prod 五档 DWS | 分区 2026-08-17 在跑，日批正常 |
| 今日交付 | 按 bus#6668 改工作簿（不再因 prod 有分区误标「已完成」）；更正 bus#6672 回执 |

**卡点**：只剩「离开事件埋点」等产品/前端答复。  
**下一步**：等产品答复；prod 照常跑，工作簿保持 HOLD。

---

**代码已改**（`workbook_progress_service.py`）：
- 「近况」不再捞旧 bus 挂账表（#4912/#5044 那种）
- 停留时长优先读**当日 work-log 实活**，「已做」以「今日：…」开头
- 卡点/下一步去掉每天重复的「45天重跑仍冻结」套话

今日流水已记 `.cursor/work-log/2026-08-18.md`。明天工作簿会读明天的流水，不会再把今天这三条当模板复读。

[REDACTED]
```
