# TG 会话轮换归档 · 2026-08-25-0616

- 原因: daily(06:00)
- session_id: ses_1787570381736_5xh372
- cursor_chat_id: ac1fdf31-0508-44b7-add8-cb76ea24993d
- 蒸馏: heuristic

## 携带要点

- > **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 会话摘录（截断）

```
[user] [私聊#415] 你现在正常了吗 可以干活吗

[assistant] 正常了，可以干活。

刚才连续失败是 Cursor 会话连不上（resume 被丢弃），不是业务本身的问题。这轮会话已起来。

你前面卡着的两件事我这边都能接着做：
1. **按你发的文档画指标库 ER 图**（私聊 #410 / #411）
2. 其它当日数据活（查数、验数、bus、文档）

直接说下一步要哪件就行。

[user] [私聊#416] er图画好了吗？画好了推送到文档库

[assistant] ER 图之前因会话失败没交完。我先核对本地是否已有稿，再按设计文档补齐并推到文档库。本地已有一版 ER 图，但你发来的是 `spec_v0.3`；我按该文档核对并重画，再推文档库。按你发的 `spec_v0.3` 重画 ER（表名对齐：`entity_dict` / `event_ext` / `role_dict`），再推文档库。画好了，并已推到文档库。

之前会话挂了没交成；这轮按你发来的 **`spec_v0.3_20260824.md`** 重画（表名对齐正式稿，不用旧的 domain_entity 临时名）。

**文档库**
- 分类：指标库
- 打开：http://54.255.236.159:8012/library/metric_library_er_diagram_v03_20260824
- slug：`metric_library_er_diagram_v03_20260824`（id=65）

**图里有什么**
- 蓝：v0.2 指标核（concept / label / implementation / candidate）
- 黄：v0.3 扩层（`entity_dict` / `event_ext` / `role_dict` + 字段增量）
- 绿：`report_metric_binding` 报表闭环
- 灰：glossary / 表列挂接

**仓库镜像**
- `docs/metric_library_er_diagram_20260824.html`
- `docs/metric_library_er_diagram.md`

[SEND_FILE: metric_library_er_diagram_20260824.html]
[SEND_FILE: metric_library_er_diagram.md]

需要的话我再 commit+push 到 origin/dev。

[user] <timestamp>Monday, Aug 24, 2026, 9:20 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-24 21:10（周一），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-24 19:59:37 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/data…

[assistant] ER 图之前因会话失败没交完。我先核对本地是否已有稿，再按设计文档补齐并推到文档库。

[assistant] 本地已有一版 ER 图，但你发来的是 `spec_v0.3`；我按该文档核对并重画，再推文档库。

[assistant] 按你发的 `spec_v0.3` 重画 ER（表名对齐：`entity_dict` / `event_ext` / `role_dict`），再推文档库。

[assistant] 画好了，并已推到文档库。

之前会话挂了没交成；这轮按你发来的 **`spec_v0.3_20260824.md`** 重画（表名对齐正式稿，不用旧的 domain_entity 临时名）。

**文档库**
- 分类：指标库
- 打开：http://54.255.236.159:8012/library/metric_library_er_diagram_v03_20260824
- slug：`metric_library_er_diagram_v03_20260824`（id=65）

**图里有什么**
- 蓝：v0.2 指标核（concept / label / implementation / candidate）
- 黄：v0.3 扩层（`entity_dict` / `event_ext` / `role_dict` + 字段增量）
- 绿：`report_metric_binding` 报表闭环
- 灰：glossary / 表列挂接

**仓库镜像**
- `docs/metric_library_er_diagram_20260824.html`
- `docs/metric_library_er_diagram.md`

[SEND_FILE: metric_library_er_diagram_20260824.html]
[SEND_FILE: metric_library_er_diagram.md]

需要的话我再 commit+push 到 origin/dev。
```
