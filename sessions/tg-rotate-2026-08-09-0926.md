# TG 会话轮换归档 · 2026-08-09-0926

- 原因: daily(06:00)
- session_id: ses_1786197268859_ov1v0o
- cursor_chat_id: 1aa01868-7060-4df7-8628-73bfc20734b6
- 蒸馏: AI+heuristic

## 携带要点

- 私聊 #283/#284 失败根因在 **Cursor 侧**（`--resume` 传输故障 / 云端 API 不可达），不是业务 SQL；排查时先区分基础设施 vs 任务逻辑。
- 大漏斗第 3 轮 executor OOM：`user_is_new` **JOIN 无分区 `dim_user_all`（~1.5 亿行）** 是根因；应改由**当日 `user_register` 推导**。
- 第 5 轮「跑通但 0 行」：源表 **`app_2556` 本身 0 行**，有效数据在 **`SF-81`（约 6022 万行）**；跑通 ≠ 成功，必须先核对 **app_id 与源表行数**。
- 用户说「直接检查错误原因」时：应出**根因表 + 当前状态 + 可补做项**（如补推日报、补跑 r5b），而非只复述失败现象。
- [LESSON: funnel-etl|user_is_new 禁止 JOIN 无分区 dim_user_all，改由当日 user_register 推导，避免 executor OOM]
- [LESSON: funnel-etl|宽漏斗禁单 SQL 宽聚合，拆成 metrics + wide 两阶段，避免 driver heap OOM]
- Bot 遇到失效 resume 会**按设计清掉旧会话**；用户可重发指令或发「重启 agent」强制新开。
- 任务 18:13/18:33 入队、23:35 才跑并失败，说明中间是 **Cursor 卡住/排队**，不能据此判断又初未执行。
- 日报 API 中断时：本地草稿可能已在 `~/.dc-platform/memory/work-log/hosts/*/reports/`，但 **TG 推送与云端上传会漏**；恢复后需补推、补传。
- 第 4 轮 driver heap OOM：单文件宽聚合导致计划树过大（`TreeNode.generateTreeString`）；**弃用单 SQL 宽聚合**，改拆分链路。
- **r5b（SF-81）两阶段 ETL**（metrics ~6.3h + wide ~17s）已验证无 OOM；宽表粒度 `(dt, app_id, is_new)`，dt=2026-08-03 产出 **3 行**符合预期。
- 群聊里 `@worker_ant_bot` 派给猫猫的 `ads_product_day_stat_d` 改动（video_play 从 dwd 换 dwm）是**旁听背景**，与本会话大漏斗/日报排查无直接执行关系。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-09-user_is_new-禁止-join-无分区-dim_user_all-改由当日-user_r.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-09-宽漏斗禁单-sql-宽聚合-拆成-metrics-wide-两阶段-避免-driver-heap.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-09-etl-跑通但-0-行时先核对-app_id-与源表行数-再决定是否换-app-补跑.md`

## AI 蒸馏原文（截断）

```
- 私聊 #283/#284 失败根因在 **Cursor 侧**（`--resume` 传输故障 / 云端 API 不可达），不是业务 SQL；排查时先区分基础设施 vs 任务逻辑。
- Bot 遇到失效 resume 会**按设计清掉旧会话**；用户可重发指令或发「重启 agent」强制新开。
- 任务 18:13/18:33 入队、23:35 才跑并失败，说明中间是 **Cursor 卡住/排队**，不能据此判断又初未执行。
- 日报 API 中断时：本地草稿可能已在 `~/.dc-platform/memory/work-log/hosts/*/reports/`，但 **TG 推送与云端上传会漏**；恢复后需补推、补传。
- 大漏斗第 3 轮 executor OOM：`user_is_new` **JOIN 无分区 `dim_user_all`（~1.5 亿行）** 是根因；应改由**当日 `user_register` 推导**。
- 第 4 轮 driver heap OOM：单文件宽聚合导致计划树过大（`TreeNode.generateTreeString`）；**弃用单 SQL 宽聚合**，改拆分链路。
- 第 5 轮「跑通但 0 行」：源表 **`app_2556` 本身 0 行**，有效数据在 **`SF-81`（约 6022 万行）**；跑通 ≠ 成功，必须先核对 **app_id 与源表行数**。
- **r5b（SF-81）两阶段 ETL**（metrics ~6.3h + wide ~17s）已验证无 OOM；宽表粒度 `(dt, app_id, is_new)`，dt=2026-08-03 产出 **3 行**符合预期。
- 用户说「直接检查错误原因」时：应出**根因表 + 当前状态 + 可补做项**（如补推日报、补跑 r5b），而非只复述失败现象。
- 群聊里 `@worker_ant_bot` 派给猫猫的 `ads_product_day_stat_d` 改动（video_play 从 dwd 换 dwm）是**旁听背景**，与本会话大漏斗/日报排查无直接执行关系。

[LESSON: funnel-etl|user_is_new 禁止 JOIN 无分区 dim_user_all，改由当日 user_register 推导，避免 executor OOM]
[LESSON: funnel-etl|宽漏斗禁单 SQL 宽聚合，拆成 metrics + wide 两阶段，避免 driver heap OOM]
[LESSON: funnel-etl|ETL 跑通但 0 行时先核对 app_id 与源表行数，再决定是否换 app 补跑]
```

## 会话摘录（截断）

```
[user] [私聊#283] 继续推进大漏斗任务

[assistant] ❌ Cursor 会话连接失败（已自动丢弃旧 resume）。请重发刚才那句；或发「重启 agent」强制新开。

[user] [私聊#284] 整理今天日报

[assistant] [31m✗ Failed to reach the Cursor API. If you are behind a corporate proxy, set the HTTPS_PROXY environment variable.[0m

[user] [私聊#285] 发现有问题的直接检查错误的原因吧

[assistant] 理解为：排查私聊#283/#284 失败根因，并继续大漏斗任务。我先读启动记忆、查 funnel 目录与近期错误日志。
私聊 #283/#284 和大漏斗两条线我都查过了，结论如下。

---

## 1. 私聊 #283 / #284：Cursor 连不上（不是业务 SQL 问题）

| 私聊 | 时间 | 现象 | 根因 |
|------|------|------|------|
| #283 | 08-08 23:35 | 「Cursor 会话连接失败，已丢弃旧 resume」 | `cursor-agent --resume` 传输层故障，bot 按设计清掉失效 resume |
| #284 | 08-08 23:36 | `Failed to reach the Cursor API` | 同一时段 Cursor 云端 API 不可达 |

两条任务实际在 **18:13 / 18:33** 就进了队列，直到 **23:35** 才跑起来并失败——中间是 Cursor 侧卡住，不是又初没干活。

**当前状态**：本机 `cursor-agent` 正常（2026.08.04），这次私聊能通，说明昨晚是**瞬时故障**，已恢复。

**#284 日报**：`~/.dc-platform/memory/work-log/hosts/new-mac/reports/2026-08-08-日报.md` 里有草稿，但因 API 中断**没推 TG、也没上传云端**。需要的话我现在可以按你的格式补推。

---

## 2. 大漏斗：各轮失败根因 + 当前状态

| 轮次 | 失败现象 | 根因 | 处理 |
|------|----------|------|------|
| 第 3 轮 | executor OOM | `user_is_new` JOIN 无分区 `dim_user_all` 全表 ~1.5 亿行 | 改由当日 `user_register` 推导 |
| 第 4 轮 | driver heap OOM | 单 SQL 计划树过大（`TreeNode.generateTreeString`） | 弃用单文件宽聚合 |
| 第 5 轮（app_2556） | 跑通但 **0 行** | **app_id 写错**：源表 `app_2556` 0 行，`SF-81` 约 6022 万行 | 补跑 r5b |
| **r5b（SF-81）** | **已完成** | 两阶段 ETL 生效，无 OOM | metrics ~6.3h + wide ~17s |

**刚在集群验过的产出**（dt=2026-08-03, app=SF-81）：
- 宽表 **3 行** → 符合粒度 `(dt, app_id, is_new)`，即 is…

[user] <timestamp>Sunday, Aug 9, 2026, 9:04 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-09 09:04（周日），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 多渠道统一上下文

含群旁听、私聊、agent-bus。判断要不要回：**仅显式 @youchu_ai_bot / @youchu8888 / @又初 / @初儿** 时在群里回复；裸写「又初」「初儿」或只 @ 别人 → 只作背景，**不要声明「我不插嘴」**——直接不回。

# TG / Cursor / agent-bus 统一上下文（最近消息，与 IDE 同一工作区）

**📥 收到** / **👁 旁听** / **📤 又初回复** 分块展示。

👁 **旁听** · `2026-08-08 10:13:14` · telegram_group
**发言** @hull1889
> [群聊·@hull1889]: @worker_ant_bot 你带猫猫吧项目日报做吧，给猫猫派任务

📥 **收到** · `2026-08-08 10:15:43` · uid=0 · worker_ant_group
**发言** @worker_ant_bot
> [工作狂人] 收到，队列改过来了，项目日报排第一，coin-001 押后。已经派给猫猫（bus #6252），一次就这一件。

派的是：新开一个 session，只改 ads_product_day_stat_d.sql 第 288~299 行的 video_play 段，从 dwd 换成 dwm。别的一行都不动。

这一改同时解决两件事：
1. 性能 —— 这个 task 今早 06:31 跑…

[assistant] 理解为：排查私聊#283/#284 失败根因，并继续大漏斗任务。我先读启动记忆、查 funnel 目录与近期错误日志。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 私聊 #283/#284 和大漏斗两条线我都查过了，结论如下。

---

## 1. 私聊 #283 / #284：Cursor 连不上（不是业务 SQL 问题）

| 私聊 | 时间 | 现象 | 根因 |
|------|------|------|------|
| #283 | 08-08 23:35 | 「Cursor 会话连接失败，已丢弃旧 resume」 | `cursor-agent --resume` 传输层故障，bot 按设计清掉失效 resume |
| #284 | 08-08 23:36 | `Failed to reach the Cursor API` | 同一时段 Cursor 云端 API 不可达 |

两条任务实际在 **18:13 / 18:33** 就进了队列，直到 **23:35** 才跑起来并失败——中间是 Cursor 侧卡住，不是又初没干活。

**当前状态**：本机 `cursor-agent` 正常（2026.08.04），这次私聊能通，说明昨晚是**瞬时故障**，已恢复。

**#284 日报**：`~/.dc-platform/memory/work-log/hosts/new-mac/reports/2026-08-08-日报.md` 里有草稿，但因 API 中断**没推 TG、也没上传云端**。需要的话我现在可以按你的格式补推。

---

## 2. 大漏斗：各轮失败根因 + 当前状态

| 轮次 | 失败现象 | 根因 | 处理 |
|------|----------|------|------|
| 第 3 轮 | executor OOM | `user_is_new` JOIN 无分区 `dim_user_all` 全表 ~1.5 亿行 | 改由当日 `user_register` 推导 |
| 第 …
```
