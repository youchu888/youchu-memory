# TG 会话轮换归档 · 2026-07-28-0602

- 原因: daily(06:00)
- session_id: ses_1785149547353_x1vttb
- cursor_chat_id: 23512eb4-3c04-4dc4-94ca-776b08bf1fb1
- 蒸馏: AI+heuristic

## 携带要点

- 群聊被 @ 又初/初儿**：必须给实质答复；禁止「没@我/群里不回」类推脱
- 设计链接是否收到**：先查 agent-bus inbox + 当日 TG 镜像；无记录则 bus 狂人问清 library URL / bus# / file_id，勿空猜
- [LESSON: device_tag,dim|device_id 空率≥90%（本需求 100%）则直建 dim_device_all，勿再设计 uid 反查兜底]
- [LESSON: extension,release|vsix 放 dc-platform-server/extension/ 走 git→pull→scp；CHcode 即 tq-git dmp/dc-parent dev，勿与 Desktop 另一 dc-parent 混用]
- **停留时长两条线不可混查**：PRD 五档看板用 `dws_session_duration_user_d`/`device_d`（墙钟 `session_duration_sec` + `duration_bucket`）；账户日页面停留用 `dws_user_page_stay_d`（`daily_valid_stay_sec`）
- **合表 v2 主表**：`dws_session_duration_user_d`/`device_d` 已吸收废弃的 `dws_session_daily_*_d`；查询必带 `stat_grain`（`session`=单次分档+启动次数，`daily`=当日累加分档+账号/设备数）
- **后端对接稿落点**：`omdb/tgbot/outgoing/停留时长表结构-后端对接-*.md`；设计口径源 `ops_system/04.dws/dws_session_duration_d/design.md`
- **群聊被 @ 又初/初儿**：必须给实质答复；禁止「没@我/群里不回」类推脱
- **设计链接是否收到**：先查 agent-bus inbox + 当日 TG 镜像；无记录则 bus 狂人问清 library URL / bus# / file_id，勿空猜
- **bus 时区 bug 曾吞消息**（如 5543/5544）；狂人需重发，通路修后以重发 bus 为准
- **device_tag 新方案**：Paimon 迁移（6 湖表 + wrapper），废 07-20 phased；参照 `origin/dev` user_tag Spark v2 框架；设计稿在 library（如 `dws_device_tag_d_d_design`）
- **device_tag 阶段 1 拍板**：`order_paid_d_r`/`register_v2` device_id fill=100% → 直建 `dim_device_all`；uid 反查兜底方案作废
- **vsix 发版通道（方案 a）**：vsix+manifest 进 `dc-platform-server/extension/` → commit/push `dev` → 狂人 pull + scp 到 `/data/dcpaltform/metadata/extension/` → 验收 `GET /api/v1/extension/version`
- **工作区归属**：CHcode = `tq-git` 的 `dmp/dc-parent` dev；Desktop 另一份 `opengit datacenter/dc-parent` 不是这条发版线
- **extension API 现状**：`api_v1_extension.py` 仅 GET/download，无 POST upload；长期上传通道需另批

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-07-28-prd-五档看板只查-session_duration-合表并带-stat_grain-page.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-07-28-device_id-空率-90-本需求-100-则直建-dim_device_all-勿再设计-.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-07-28-vsix-放-dc-platform-server-extension-走-git-pull-s.md`

## AI 蒸馏原文（截断）

```
- **停留时长两条线不可混查**：PRD 五档看板用 `dws_session_duration_user_d`/`device_d`（墙钟 `session_duration_sec` + `duration_bucket`）；账户日页面停留用 `dws_user_page_stay_d`（`daily_valid_stay_sec`）
- **合表 v2 主表**：`dws_session_duration_user_d`/`device_d` 已吸收废弃的 `dws_session_daily_*_d`；查询必带 `stat_grain`（`session`=单次分档+启动次数，`daily`=当日累加分档+账号/设备数）
- **后端对接稿落点**：`omdb/tgbot/outgoing/停留时长表结构-后端对接-*.md`；设计口径源 `ops_system/04.dws/dws_session_duration_d/design.md`
- **群聊被 @ 又初/初儿**：必须给实质答复；禁止「没@我/群里不回」类推脱
- **设计链接是否收到**：先查 agent-bus inbox + 当日 TG 镜像；无记录则 bus 狂人问清 library URL / bus# / file_id，勿空猜
- **bus 时区 bug 曾吞消息**（如 5543/5544）；狂人需重发，通路修后以重发 bus 为准
- **device_tag 新方案**：Paimon 迁移（6 湖表 + wrapper），废 07-20 phased；参照 `origin/dev` user_tag Spark v2 框架；设计稿在 library（如 `dws_device_tag_d_d_design`）
- **device_tag 阶段 1 拍板**：`order_paid_d_r`/`register_v2` device_id fill=100% → 直建 `dim_device_all`；uid 反查兜底方案作废
- **vsix 发版通道（方案 a）**：vsix+manifest 进 `dc-platform-server/extension/` → commit/push `dev` → 狂人 pull + scp 到 `/data/dcpaltform/metadata/extension/` → 验收 `GET /api/v1/extension/version`
- **工作区归属**：CHcode = `tq-git` 的 `dmp/dc-parent` dev；Desktop 另一份 `opengit datacenter/dc-parent` 不是这条发版线
- **extension API 现状**：`api_v1_extension.py` 仅 GET/download，无 POST upload；长期上传通道需另批
- **长时间任务中被问「挂了」**：短回在线 + 当前在干什么 + 下一步（bus reply / 阶段产出）

[LESSON: session_duration,backend|PRD 五档看板只查 session_duration 合表并带 stat_grain；page_stay 的 valid_stay 与 session 墙钟不是同一指标]

[LESSON: device_tag,dim|device_id 空率≥90%（本需求 100%）则直建 dim_device_all，勿再设计 uid 反查兜底]

[LESSON: extension,release|vsix 放 dc-platform-server/extension/ 走 git→pull→scp；CHcode 即 tq-git dmp/dc-parent dev，勿与 Desktop 另一 dc-parent 混用]
```

## 会话摘录（截断）

```
[user] [私聊#246] 整理下最新的停留时长表结构，给后端对接

[assistant] 整理停留时长相关表结构给后端对接。先检索仓库里的设计文档与元数据定义。
正在把对接文档写到 `omdb/tgbot/outgoing/`，便于后端直接取用。
停留时长这块已整理成对接稿，完整版在：

`omdb/tgbot/outgoing/停留时长表结构-后端对接-2026-07-27.md`

下面是给后端的核心摘要。

---

## 两条线，别混查

| 场景 | 查什么 | 主指标 |
|------|--------|--------|
| **PRD 使用时长看板**（单次/日均五档柱状图） | `dws_session_duration_user_d` / `dws_session_duration_device_d` | 墙钟 `session_duration_sec` + `duration_bucket` |
| **账户日页面停留** | `dws_user_page_stay_d` | `daily_valid_stay_sec`（valid_stay 页间停留） |

valid_stay ≠ session 墙钟，五档看板不要查 page_stay 表。

---

## 后端主表（PRD 五档 · 合表 v2）

### `dws.dws_session_duration_user_d`（账号）

- **PK**：`dt, app_id, stat_grain, source_type, is_new, duration_bucket, device, user_type, country`
- **必带 `stat_grain`**：
  - `session` → 单次墙钟分档，Y=启动次数（`session_cnt`、`bounce_cnt`、`avg_session_duration_sec`）
  - `daily` → 当日累加后再分档，Y=账号数（`user_cnt`、`avg_daily_duration_sec`）
- **已废弃**：`dws_session_daily_user_d`（已合入本表）

### `dws.dws_session_duration_device_d`（设备）

- 同构，无 `user_type`，用 `device_cnt` 代替 `user_cnt`
- **已废弃**：`dws_session_daily_device_d`

### `duration_bucket`（候选 B，全链统一）

| bucket | 含义 | 区间（秒） |
|--------|------|------------|
| 0 | bounce（仅 session） | 单 PV |
| 1 | ≤1 分钟 | (0, 60] |
| 2 | 1–5 分钟 | (60, 300] |
…

[user] [群聊#147] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 你收到狂人给你发的设计链接了吗？
问题正文: 你收到狂人给你发的设计链接了吗？

[assistant] 知秋在群里问了狂人发的设计链接，我先查 agent-bus 和近期入站记录。
知秋，刚查了 agent-bus inbox 和今天 TG 镜像：

**今天（07-27）没收到狂人发的「设计链接」。**

· 狂人最近一条 bus 停在 **07-21 #5412**（dwell_time 追问），之后没新入站  
· 今天收到的是 **野花 bus#5535~5539**（五档合表复审 PASS），不是狂人  
· 18:00 是我主动 bus 狂人报 page_stay publish 409，也不是他发链

若说的是 **07-21 library 规矩**（bus#5280），那个早 ack 了：`http://54.255.236.159:8012/library/`

停留时长设计稿我这边用的是仓库 `ops_system/04.dws/dws_session_duration_d/design.md`，刚也整理了后端对接稿在 `omdb/tgbot/outgoing/停留时长表结构-后端对接-2026-07-27.md`。

要是狂人今天群里另发了链接、没进 bus，麻烦指一下 **bus# 或群 msg id**，我马上去拉对齐。

[user] [群聊#148] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 你bus一下他，问问他
问题正文: 你bus一下他，问问他

[assistant] 知秋，已 bus 狂人（**bus#5545**），问他设计链接是哪条、有没有 HTML/library URL 或 file_id。他回了我在群里同步。

[user] [群聊#149] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 设备标签的设计收到了是吧？
问题正文: 设备标签的设计收到了是吧？

[assistant] 收到了。

狂人 **22:38 重发 bus#5554**（早上 5543/5544 因 bus 时区 bug 被吞，5549 通路已修）。

· 设计稿：`http://54.255.236.159:8012/library/dws_device_tag_d_d_design`（id=46）  
· 参照代码：`origin/dev` **b4cf1f0a**，抄 user_tag Spark v2 框架  
· 新派活：device_tag **Paimon 迁移**（6 张湖表 + wrapper），废 07-20 phased 老方案  

阶段 1 他要我先回 **order_paid_r / register_v2_r 的 device_id 空率**（>90% 直用，否则 uid 反查兜底）。我这就开查。

[user] [群聊#150] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 你挂了？
问题正文: 你挂了？

[assistant] 在，没挂。

刚收到狂人 **bus#5554** 设备标签 Paimon 迁移设计稿，正在查 `order_paid_r` / `register_v2_r` 的 device_id 空率，阶段 1 回执整理完就 bus 他。

[user] <timestamp>Monday, Jul 27, 2026, 10:09 PM (UTC+8)</timestamp>
<user_query>
[群聊#148] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 你bus一下他，问问他
问题正文: 你bus一下他，问问他
</user_query>

[assistant] [REDACTED]

[assistant] 知秋，已 bus 狂人（**bus#5545**），问他设计链接是哪条、有没有 HTML/library URL 或 file_id。他回了我在群里同步。

[REDACTED]

[user] <timestamp>Monday, Jul 27, 2026, 10:47 PM (UTC+8)</timestamp>
<user_query>
[群聊#149] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 设备标签的设计收到了是吧？
问题正文: 设备标签的设计收到了是吧？
</user_query>

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 收到了。

狂人 **22:38 重发 bus#5554**（早上 5543/5544 因 bus 时区 bug 被吞，5549 通路已修）。

· 设计稿：`http://54.255.236.159:8012/library/dws_device_tag_d_d_design`（id=46）  
· 参照代码：`origin/dev` **b4cf1f0a**，抄 user_tag Spark v2 框架  
· 新派活：device_tag **Paimon 迁移**（6 张湖表 + wrapper），废 07-20 phased 老方案  

阶段 1 他要我先回 **order_paid_r / register_v2_r 的 device_id 空率**（>90% 直用，否则 uid 反查兜底）。我这就开查。

[REDACTED]

[user] <timestamp>Monday, Jul 27, 2026, 10:52 PM (UTC+8)</timestamp>
<user_query>
[群聊#150] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 你挂了？
问题正文: 你挂了？
</user_query>

[assistant] 在，没挂。

刚收到狂人 **bus#5554** 设备标签 Paimon 迁移设计稿，正在查 `order_paid_r` / `register_v2_r` 的 device_id 空率，阶段 1 回执整理完就 bus 他。

[REDACTED]

[user] <timestamp>Monday, Jul 27, 2026, 10:53 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#5556 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 5556 --text "[ACK] bus#5556 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 5556 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：[bus#5555 阶段1收 · 拍板收] fill=100% (order_paid_d_r + register_v2 + 牡丹交叉) 无异议, 走 device_id 建 dim_device_all, uid 反查兜底方案作废。等你阶段 2 HTML 设计稿, 我审后 GO 阶段 3。知秋 22:47 北京在 TG 群 @你问'挂了', 顺便切回 TG 打声招呼~
</user_query>

[user] <mcp_meta_tools>
You have access to MCP (Model Context Protocol) tools through `GetMcpTools` and `CallMcpTool`.

## MCP Tool Discovery and Invocation

Use `GetMcpTools` to discover tool schemas, then `CallMcpTool` to invoke them. Aim to minimize round-trips: ideally one `GetMcpTools` call followed by one `CallMcpTool` call.

If the user mentions, references, or links to a product or service that corresponds to an available MCP server, and the request likely depends on information from that service, proactively inspect that MCP server before answering. Do not wait for the user to explicitly ask you to use MCP. If you are unsure which server matches, use `GetMcpTools` with a pattern based on the service name.

`GetMcpTools` supports four modes:

1. `{"server":"<id>"}`: returns full input sch…

[user] <timestamp>Monday, Jul 27, 2026, 10:53 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#5556 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 5556 --text "[ACK] bus#5556 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 5556 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：[bus#5555 阶段1收 · 拍板收] fill=100% (order_paid_d_r + register_v2 + 牡丹交叉) 无异议, 走 device_id 建 dim_device_all, uid 反查兜底方案作废。等你阶段 2 HTML 设计稿, 我审后 GO 阶段 3。知秋 22:47 北京在 TG 群 @你问'挂了', 顺便切回 TG 打声招呼~
</user_query>

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 已处理 **bus#5556**：

· **ACK**：已发（静默去重，不重复刷）  
· **reply bus#5558**：阶段 1 拍板收到（fill=100%、device_id 建 dim、uid 反查作废）；阶段 2 HTML 按 bus#5554 新方案出稿后 bus 你审  
· 知秋群里「挂了」——上一轮已回「在，没挂」，reply 里也注明了

下一步：按 bus#5554 出 **阶段 2 HTML 设计稿**，完事 bus 狂人审。

[REDACTED]

[user] <timestamp>Monday, Jul 27, 2026, 11:05 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#5559 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 5559 --text "[ACK] bus#5559 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 5559 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：[bus#5557 处理] 情况: 我本地没 dc-platform-0.0.121.vsix (dc-parent repo 无 extension/ 目录), 服务端 api_v1_extension.py 只有 GET/download 无 POST upload, 不能直接代 scp。  3 个方案让又初选 (等知秋 GO): (a) 又初 git commit .vsix + manifest.json 到 dev 分支 (放 dc-platform-server/extension/ 下), push 后我 pull + scp 服务器 (b) 又初 TG 私聊 @worker_ant_bot 直接…

[assistant] 先 ACK，再查本地 vsix 与仓
```
