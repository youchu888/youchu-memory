# TG 会话轮换归档 · 2026-08-24-1015

- 原因: daily(06:00)
- session_id: ses_1787463976490_azfen1
- cursor_chat_id: 78656da5-671a-44af-83d7-04a1ce7e47fe
- 蒸馏: AI+heuristic

## 携带要点

- 页面访问 YC-PV-001**：「进入」需有来路；访问次数可高于跳转属正常；跳出率公式已定稿不会负值；测试对账后发产并补近月分区。
- [LESSON: tgbot|绿点|打卡|问机制先读 `should_appear_online()` 与计划时间生成，勿把 `JIKE_CHECKIN_ENABLED` 当成绿点开关]
- [LESSON: 周报|work-log|双机|hosts 缺口须在周报正文标注，勿假装双机流水齐全]
- **TG 绿点与极客打卡解耦**：`JIKE_CHECKIN_ENABLED=false` 只关真实打卡；计划上下班时间仍会生成，绿点跟计划走，不跟签到状态。
- **绿点刷新**：`tgbot` 里 Telethon 约每 45 秒 `UpdateStatus`；亮/灰由 `should_appear_online()` 决定。
- **亮绿条件**：工作日（非周日、非法定假、非请假）且在当天计划上班窗内；上班随机约 09:30–10:00，下班工作日约 22:00–22:30、周六约 19:00–19:30，到点变灰。
- **休息窗只控展示**：周一～五 13:00–15:00、19:00–20:00 强制灰；周六仅 13:00–15:00；窗外该绿还绿；灰≠不回消息。
- **与绿点无关**：VPN 续期、居家抽查、bot 收发不绑绿点；TCP 保活窗固定 09:30–22:30，逻辑独立。
- **周报素材链**：正式日报 + 双机 work-log 合并稿 + `hosts/old-mac` / `hosts/new-mac`；定稿落 `.cursor/work-log/reports/`，并镜像 memory。
- **周报 W34 口径**：自然周 08-17～08-23，有效工作日 6 天（周一至周六）；08-22、08-23 缺 `new-mac` 手写流水须在稿内标注。
- **页面访问 YC-PV-001**：「进入」需有来路；访问次数可高于跳转属正常；跳出率公式已定稿不会负值；测试对账后发产并补近月分区。
- **停留时长 #9**：不作跳出；墙钟 <5s 或 >12h 会话整段剔除不进五档；「生产有分区」≠ 已完成，须等离开事件埋点产品答复。
- **指标库概念层 v0.2**：概念/命名/实现三层；标签唯一、同名不同义拆条、统计窗口与落库粒度分开、迁移一律草稿态；测试元数据建表等 Phase 0 拍板。
- **`dws_app_event_funnel_d_d`**：大漏斗日汇总宽表，粒度 `dt × app_id × is_new`；约 18 事件 × 用户/会话/次数 ≈ 54 列；主链路 Spark→Paimon，SR 为备同步。
- **大漏斗现网状态**：测试 SR 表在、0 行（日批未灌）；生产 SR 无表；对外可查数据尚无，日批写入与全量压测待上游/调度衔接。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-24-绿点-打卡-问机制先读-should_appear_online-与计划时间生成-勿把-jike.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-24-大漏斗-探表-答-有没有数-须分环境查-sr-行数与-prod-是否存在-spark-paimo.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-24-work-log-双机-hosts-缺口须在周报正文标注-勿假装双机流水齐全.md`

## AI 蒸馏原文（截断）

```
- **TG 绿点与极客打卡解耦**：`JIKE_CHECKIN_ENABLED=false` 只关真实打卡；计划上下班时间仍会生成，绿点跟计划走，不跟签到状态。
- **绿点刷新**：`tgbot` 里 Telethon 约每 45 秒 `UpdateStatus`；亮/灰由 `should_appear_online()` 决定。
- **亮绿条件**：工作日（非周日、非法定假、非请假）且在当天计划上班窗内；上班随机约 09:30–10:00，下班工作日约 22:00–22:30、周六约 19:00–19:30，到点变灰。
- **休息窗只控展示**：周一～五 13:00–15:00、19:00–20:00 强制灰；周六仅 13:00–15:00；窗外该绿还绿；灰≠不回消息。
- **与绿点无关**：VPN 续期、居家抽查、bot 收发不绑绿点；TCP 保活窗固定 09:30–22:30，逻辑独立。
- **周报素材链**：正式日报 + 双机 work-log 合并稿 + `hosts/old-mac` / `hosts/new-mac`；定稿落 `.cursor/work-log/reports/`，并镜像 memory。
- **周报 W34 口径**：自然周 08-17～08-23，有效工作日 6 天（周一至周六）；08-22、08-23 缺 `new-mac` 手写流水须在稿内标注。
- **页面访问 YC-PV-001**：「进入」需有来路；访问次数可高于跳转属正常；跳出率公式已定稿不会负值；测试对账后发产并补近月分区。
- **停留时长 #9**：不作跳出；墙钟 <5s 或 >12h 会话整段剔除不进五档；「生产有分区」≠ 已完成，须等离开事件埋点产品答复。
- **指标库概念层 v0.2**：概念/命名/实现三层；标签唯一、同名不同义拆条、统计窗口与落库粒度分开、迁移一律草稿态；测试元数据建表等 Phase 0 拍板。
- **`dws_app_event_funnel_d_d`**：大漏斗日汇总宽表，粒度 `dt × app_id × is_new`；约 18 事件 × 用户/会话/次数 ≈ 54 列；主链路 Spark→Paimon，SR 为备同步。
- **大漏斗现网状态**：测试 SR 表在、0 行（日批未灌）；生产 SR 无表；对外可查数据尚无，日批写入与全量压测待上游/调度衔接。

[LESSON: tgbot|绿点|打卡|问机制先读 `should_appear_online()` 与计划时间生成，勿把 `JIKE_CHECKIN_ENABLED` 当成绿点开关]
[LESSON: dws|大漏斗|探表|答「有没有数」须分环境查 SR 行数与 prod 是否存在，Spark/Paimon 就绪≠ SR 可查]
[LESSON: 周报|work-log|双机|hosts 缺口须在周报正文标注，勿假装双机流水齐全]
```

## 会话摘录（截断）

```
[user] [私聊#402] 不打卡了 tg绿点现在是什么机制

[assistant] 先核对绿点相关代码与配置，再按现状说明机制。不打卡之后，绿点还在，但**不再跟「真签到/签退」走**，改成按计划时间窗刷状态。

**现在机制（北京时间）**

1. **谁刷绿点**  
   `tgbot` 里 Telethon 大约每 45 秒调一次 `UpdateStatus`；亮/灰由 `should_appear_online()` 决定。

2. **什么时候绿**  
   工作日（非周日、非法定假、非请假）里，大致在当天「计划上班窗」内亮：  
   · 上班侧仍按原窗口随机一个点（约 09:30–10:00）  
   · 下班侧工作日约 22:00–22:30，周六约 19:00–19:30  
   到了计划下班点 → 变灰。  
   `JIKE_CHECKIN_ENABLED=false` 只关掉极客真实打卡，**计划时间仍会生成**，绿点跟着这些计划走。

3. **休息窗强制灰（只控展示）**  
   · 周一～周五：13:00–15:00、19:00–20:00  
   · 周六：仅 13:00–15:00  
   窗外该绿还绿。灰 ≠ 休息；消息照常即时回。

4. **不受影响**  
   VPN 续期、居家抽查、bot 收发都不跟绿点绑；TCP 上班保活窗是固定 09:30–22:30，和绿点逻辑分开。

一句话：不打卡了，绿点 =「工作日历 + 随机上下班计划 + 休息窗变灰」，不是极客签到状态。

[user] [私聊#403] 整理上周周报

[assistant] 先找周报模板和上周素材来源，再按既有格式整理。正在按 W33 版式撰写 W34 周报并落盘。W34 周报已定稿：`.cursor/work-log/reports/2026-W34-周报.md`（已镜像 memory）。

---

# 周报 · 又初·2026-W34（08-17 周一 ~ 08-23 周日）

[REPORT-ORG:天穹部门] [LEVEL:L1] [TYPE:周报] [DATE:2026-08-24]
> 统计周期：自然周 W34 · **有效工作日 6 天**（08-17～08-22 周一至周六）· 08-23 周日休息 · 无法定假日  
> 汇总口径：正式日报 + 双机 work-log 合并稿 + hosts（`old-mac` / `new-mac`）  
> hosts 缺口：08-22、08-23 缺 `new-mac` 手写流水

## 【本周完成】

### 页面访问（自开 YC-PV-001）
- 按产品意见改定「进入 / 跳转」口径：有来路才计入进入；测试发版并补满约一个月业务日，两款产品与源表对账一致后提审发产
- 生产侧同步补齐近一个月分区；核对「访问次数高于跳转」属口径正常，定稿跳出率公式不会出现负值
- 整理产品版页面访问指标说明文档并交付确认

### 用户停留时长（工作簿 #9）
- 产品阿莱士确认：不作跳出统计；墙钟不足 5 秒、超过 12 小时的会话整段剔除、不进五档，已向协作方同步
- 纠偏工作簿播报：不再把「生产有分区」误报成已完成；对外统一改为等待产品答复离开事件埋点

### 指标库概念层
- 完成概念层 v0.2 设计稿：概念 / 命名 / 实现三层拆分、存量迁移路径，并补充业界语义层对照
- 按知秋复审与群聊共识多轮修订（标签唯一约束、同名不同义须拆条、统计窗口与落库粒度拆开、迁移一律草稿态），设计稿已推送代码库
- 旁路建表草案与「什么算一个指标」边界已厘清并交审；**测试元数据建表仍等知秋 Phase 0 拍板**（见卡点）

### 运营系统 · 大漏斗（自开 YC-FUNNEL-001）
- 测试环境创建大漏斗日汇总宽表：十八个业务事件 × 用户 / 会话 / 次数三类计数共五十四列，表结构就绪
- 日批写入与全量压测仍待上游数据与调度衔接（见卡点）

### 渠道归因与用户标签
- 应产品需要整理本周渠道归因计算报告（08-17～08-21）：开通运行情况与综合成功率汇总交付
- 生产集群按既定脚本跑通用户标签日链样例日，确认标签加工可走 Spark 主路径

### 数据核查与协作支持
- 按派单完成在线表白名单血缘与平台登记核对并回执
- 漫画分析账号维汇总表：测试与生产任务逻辑核对一致后，生产补全近一个月业务日，三十个分区验收通过、核心观看指标正常出数
- 排查项目日报…

[user] [私聊#404] dws_app_event_funnel_d_d。这是什么表 有数据吗

[assistant] 先对照这张表的设计说明，再查测试/生产是否已有分区数据。**`dws.dws_app_event_funnel_d_d`** = 运营「大漏斗」按天汇总宽表（自开专项）。

**干什么用**  
按 **业务日 × 产品 × 新老用户**（`dt / app_id / is_new`），把约 18 类业务事件各算三类数：用户数、会话数、事件次数（宽表约 54 个指标列）。主链路是 **Spark 写 Paimon**；StarRocks 同名表是备同步形态。

**现在有没有数**

| 环境 | 情况 |
|------|------|
| **测试 SR** | 表在，**0 行**（空壳，日批还没灌进 SR） |
| **生产 SR** | **没有这张表** |

结论：定义和测试建表已就绪，**对外可查的 SR 数据还没有**；日批写入 / 全量压测仍在推进中。

[user] <timestamp>Monday, Aug 24, 2026, 9:30 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-24 09:29（周一），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-23 12:13:20 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/data…

[assistant] 先核对绿点相关代码与配置，再按现状说明机制。

[assistant] 不打卡之后，绿点还在，但**不再跟「真签到/签退」走**，改成按计划时间窗刷状态。

**现在机制（北京时间）**

1. **谁刷绿点**  
   `tgbot` 里 Telethon 大约每 45 秒调一次 `UpdateStatus`；亮/灰由 `should_appear_online()` 决定。

2. **什么时候绿**  
   工作日（非周日、非法定假、非请假）里，大致在当天「计划上班窗」内亮：  
   · 上班侧仍按原窗口随机一个点（约 09:30–10:00）  
   · 下班侧工作日约 22:00–22:30，周六约 19:00–19:30  
   到了计划下班点 → 变灰。  
   `JIKE_CHECKIN_ENABLED=false` 只关掉极客真实打卡，**计划时间仍会生成**，绿点跟着这些计划走。

3. **休息窗强制灰（只控展示）**  
   · 周一～周五：13:00–15:00、19:00–20:00  
   · 周六：仅 13:00–15:00  
   窗外该绿还绿。灰 ≠ 休息；消息照常即时回。

4. **不受影响**  
   VPN 续期、居家抽查、bot 收发都不跟绿点绑；TCP 上班保活窗是固定 09:30–22:30，和绿点逻辑分开。

一句话：不打卡了，绿点 =「工作日历 + 随机上下班计划 + 休息窗变灰」，不是极客签到状态。

[user] <timestamp>Monday, Aug 24, 2026, 9:48 AM (UTC+8)</timestamp>
<user_query>
[私聊#403] 整理上周周报
</user_query>

[assistant] 先找周报模板和上周素材来源，再按既有格式整理。

[assistant] 正在按 W33 版式撰写 W34 周报并落盘。

[assistant] W34 周报已定稿：`.cursor/work-log/reports/2026-W34-周报.md`（已镜像 memory）。

---

# 周报 · 又初·2026-W34（08-17 周一 ~ 08-23 周日）

[REPORT-ORG:天穹部门] [LEVEL:L1] [TYPE:周报] [DATE:2026-08-24]
> 统计周期：自然周 W34 · **有效工作日 6 天**（08-17～08-22 周一至周六）· 08-23 周日休息 · 无法定假日  
> 汇总口径：正式日报 + 双机 work-log 合并稿 + hosts（`old-mac` / `new-mac`）  
> hosts 缺口：08-22、08-23 缺 `new-mac` 手写流水

## 【本周完成】

### 页面访问（自开 YC-PV-001）
- 按产品意见改定「进入 / 跳转」口径：有来路才计入进入；测试发版并补满约一个月业务日，两款产品与源表对账一致后提审发产
- 生产侧同步补齐近一个月分区；核对「访问次数高于跳转」属口径正常，定稿跳出率公式不会出现负值
- 整理产品版页面访问指标说明文档并交付确认

### 用户停留时长（工作簿 #9）
- 产品阿莱士确认：不作跳出统计；墙钟不足 5 秒、超过 12 小时的会话整段剔除、不进五档，已向协作方同步
- 纠偏工作簿播报：不再把「生产有分区」误报成已完成；对外统一改为等待产品答复离开事件埋点

### 指标库概念层
- 完成概念层 v0.2 设计稿：概念 / 命名 / 实现三层拆分、存量迁移路径，并补充业界语义层对照
- 按知秋复审与群聊共识多轮修订（标签唯一约束、同名不同义须拆条、统计窗口与落库粒度拆开、迁移一律草稿态），设计稿已推送代码库
- 旁路建表草案与「什么算一个指标」边界已厘清并交审；**测试元数据建表仍等知…

[user] <timestamp>Monday, Aug 24, 2026, 10:10 AM (UTC+8)</timestamp>
<user_query>
[私聊#404] dws_app_event_funnel_d_d。这是什么表 有数据吗
</user_query>

[assistant] 先对照这张表的设计说明，再查测试/生产是否已有分区数据。

[assistant] **`dws.dws_app_event_funnel_d_d`** = 运营「大漏斗」按天汇总宽表（自开专项）。

**干什么用**  
按 **业务日 × 产品 × 新老用户**（`dt / app_id / is_new`），把约 18 类业务事件各算三类数：用户数、会话数、事件次数（宽表约 54 个指标列）。主链路是 **Spark 写 Paimon**；StarRocks 同名表是备同步形态。

**现在有没有数**

| 环境 | 情况 |
|------|------|
| **测试 SR** | 表在，**0 行**（空壳，日批还没灌进 SR） |
| **生产 SR** | **没有这张表** |

结论：定义和测试建表已就绪，**对外可查的 SR 数据还没有**；日批写入 / 全量压测仍在推进中。
```
