# TG 会话轮换归档 · 2026-09-03-0615

- 原因: daily(06:00)
- session_id: ses_1788311648769_iwom26
- cursor_chat_id: 44126a83-70e6-457b-8701-22b2da046b12
- 蒸馏: AI+heuristic

## 携带要点

- 「换了 VPN 重新推」标准动作：先 `git status` / 对比 `origin/dev`，再 `git push`；已对齐则如实报 Everything up-to-date，勿空推
- 沙箱 explain 统一走 `run_test.sh`，禁止用 `run_step_once.sh`（bus#7836 已明确）
- uid_map 沙箱 PASS 门槛：daily SQL 内嵌 `CREATE DATABASE/TABLE IF NOT EXISTS` + `${OUT_DB}`；建表 9 列与最终 SELECT 9 列逐项对齐（含 `first/last_uid_dt` 的 CAST 类型）
- 复审派单（bus#7838）闭环：60 秒内 ACK → 改完 push → reply 带 commit（如 `9d29f0f5`）与改动摘要；uid_map PASS 后等大漏斗复审再放行 explain
- 上传成功应回传：日期、云端记录 ID、首次状态（inserted）；主人追问「没上传吗」时先查同 ID 是否仍在（可能已是 updated），勿重复上传
- [LESSON: sandbox-explain|沙箱 explain 必须用 run_test.sh，禁止 run_step_once.sh]
- VPN 切换后 Cursor 会话 resume 可能失败；提示用户重发指令或发「重启 agent」强制新开，重启后应接上上一轮未完成的上下文继续干
- 大漏斗沙箱打回三类修法：`params.render` 补 `app_filter_e/v/n/c`（默认空串=全 app）；新增 `sandbox_steps_full.json`（`slots 00~23` 写全，`slot00`=`[metric_stg_d → d_d]`）；`stage_metrics`/`stage_wide` 两阶段 SQL 内嵌建表（对齐 uid_map 做法）
- 日报上传云端仅在主人明说「上传云端」时执行；稿源用 memory 定稿 `work-log/reports/日报-YYYY-MM-DD.md`，原样上传不改字
- 填报页查不到时优先提示按日期筛选/刷新缓存，而非假定上传失败

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-03-换-vpn-后会话-resume-易断-连接失败时让用户-重启-agent-并接上上一轮任务续做.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-03-沙箱-explain-必须用-run_test-sh-禁止-run_step_once-sh.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-03-日报云端上传须主人指令触发-定稿原样传-追问时用记录-id-复核而非重传.md`

## AI 蒸馏原文（截断）

```
- VPN 切换后 Cursor 会话 resume 可能失败；提示用户重发指令或发「重启 agent」强制新开，重启后应接上上一轮未完成的上下文继续干
- 「换了 VPN 重新推」标准动作：先 `git status` / 对比 `origin/dev`，再 `git push`；已对齐则如实报 Everything up-to-date，勿空推
- 沙箱 explain 统一走 `run_test.sh`，禁止用 `run_step_once.sh`（bus#7836 已明确）
- uid_map 沙箱 PASS 门槛：daily SQL 内嵌 `CREATE DATABASE/TABLE IF NOT EXISTS` + `${OUT_DB}`；建表 9 列与最终 SELECT 9 列逐项对齐（含 `first/last_uid_dt` 的 CAST 类型）
- 大漏斗沙箱打回三类修法：`params.render` 补 `app_filter_e/v/n/c`（默认空串=全 app）；新增 `sandbox_steps_full.json`（`slots 00~23` 写全，`slot00`=`[metric_stg_d → d_d]`）；`stage_metrics`/`stage_wide` 两阶段 SQL 内嵌建表（对齐 uid_map 做法）
- 复审派单（bus#7838）闭环：60 秒内 ACK → 改完 push → reply 带 commit（如 `9d29f0f5`）与改动摘要；uid_map PASS 后等大漏斗复审再放行 explain
- 日报上传云端仅在主人明说「上传云端」时执行；稿源用 memory 定稿 `work-log/reports/日报-YYYY-MM-DD.md`，原样上传不改字
- 上传成功应回传：日期、云端记录 ID、首次状态（inserted）；主人追问「没上传吗」时先查同 ID 是否仍在（可能已是 updated），勿重复上传
- 填报页查不到时优先提示按日期筛选/刷新缓存，而非假定上传失败

[LESSON: cursor-vpn|换 VPN 后会话 resume 易断，连接失败时让用户「重启 agent」并接上上一轮任务续做]
[LESSON: sandbox-explain|沙箱 explain 必须用 run_test.sh，禁止 run_step_once.sh]
[LESSON: daily-report-cloud|日报云端上传须主人指令触发；定稿原样传，追问时用记录 ID 复核而非重传]
```

## 会话摘录（截断）

```
[user] [私聊#454] 更换了vpn 重新推

[assistant] ❌ Cursor 会话连接失败（已自动丢弃旧 resume）。请重发刚才那句；或发「重启 agent」强制新开。

[user] [私聊#455] 重启 agent

[assistant] 会话已重启。先接上你上一句「换了 VPN 重新推」：核对本地改动与远端，再 push。本地与 `origin/dev` 已对齐。正在确认 push 可达，并核对 bus#7836 是否需回执。Agent 已重启。上一句「换了 VPN 重新推」已核对完。

**Push 状态**：`origin/dev` 已是最新，`git push` 回报 Everything up-to-date。当前 HEAD：`5ea30e4e`。

**近期相关提交**（都在远端）：
- `bb905feb` · uid_map 沙箱四点（狂人已 PASS）
- `009bc16f` · 大漏斗沙箱 OUT_DB / tagTargets
- `5ea30e4e` · uid_map daily 内嵌建表 + 完整 steps

**bus#7836**：已 ACK + reply 结案（说明用 `run_test.sh` 跑 explain、漏斗已 push）。

接下来若要继续：用 `run_test.sh` 跑沙箱 explain（不要 `run_step_once.sh`），或等狂人审大漏斗。你说一声我接着干。

[user] [私聊#456] 日报可以上传云端

[assistant] 正在核对今日定稿并原样上传云端。
已上传云端。

- **日期**：2026-09-02  
- **云端记录 ID**：87051  
- **状态**：inserted（新建成功）  
- **稿源**：`~/.dc-platform/memory/work-log/reports/日报-2026-09-02.md`（定稿原样上传）

填报页按 9.2 查一下应该能看到。

[user] [私聊#457] 日报没有上传云端吗？

[assistant] **已经上传了。**

你 21:55 左右说「日报可以上传云端」后，又初已按定稿原样上传：

- **日期**：2026-09-02  
- **云端记录 ID**：87051  
- **首次状态**：inserted（新建成功）

刚才又核对了一遍，云端仍是同一条记录（`87051`，状态 `updated`），说明 9.2 日报已在云端。

填报页请按 **2026-09-02** 查；若还看不到，可能是页面筛选或缓存，刷新后再试。

[user] <timestamp>Wednesday, Sep 2, 2026, 5:45 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-02 17:45（周三），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-02 17:39:12 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/da…

[assistant] 会话已重启。先接上你上一句「换了 VPN 重新推」：核对本地改动与远端，再 push。

[REDACTED]

[assistant] 本地与 `origin/dev` 已对齐。正在确认 push 可达，并核对 bus#7836 是否需回执。

[REDACTED]

[assistant] Agent 已重启。上一句「换了 VPN 重新推」已核对完。

**Push 状态**：`origin/dev` 已是最新，`git push` 回报 Everything up-to-date。当前 HEAD：`5ea30e4e`。

**近期相关提交**（都在远端）：
- `bb905feb` · uid_map 沙箱四点（狂人已 PASS）
- `009bc16f` · 大漏斗沙箱 OUT_DB / tagTargets
- `5ea30e4e` · uid_map daily 内嵌建表 + 完整 steps

**bus#7836**：已 ACK + reply 结案（说明用 `run_test.sh` 跑 explain、漏斗已 push）。

接下来若要继续：用 `run_test.sh` 跑沙箱 explain（不要 `run_step_once.sh`），或等狂人审大漏斗。你说一声我接着干。

[REDACTED]

[user] <timestamp>Wednesday, Sep 2, 2026, 9:02 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7838 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7838 --text "[ACK] bus#7838 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7838 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：【复审 0de62fd0 · uid_map PASS / 大漏斗打回】  ■ uid_map(dwm_device_uid_map_d) = PASS,可以开沙箱 explain  · daily SQL 内嵌 CREATE DATABASE/TABLE IF NOT EXISTS + ${OUT_DB},建表 9 列与最终 SELECT 9 列逐项对齐(app_id/device_fingerprint/uid/first_uid/first_uid_dt/last_uid_dt/uid_cnt/device_id/update_time),类型也对得上(first/last_uid_dt 都 CAST …

[assistant] 收到 bus#7838，先 ACK，再读完整正文并处理大漏斗打回项。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 正在按 bus#7838 三条打回项修复大漏斗沙箱：补 `app_filter` 渲染、新增 `sandbox_steps_full.json`，并在两阶段 SQL 内嵌建表。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] bus#7838 已处理完毕。

**ACK** 已发 · **reply** 已结案（outbound#7839）

**改动**（`origin/dev` · `9d29f0f5`）：
1. **app_filter 渲染**：`params.render` 补 `app_filter_e/v/n/c`（默认空串=全 app）
2. **full.json**：新增 `sandbox_steps_full.json`，`slots 00~23` 写全，`slot00` = `[metric_stg_d → d_d]`
3. **内嵌建表**：`stage_metrics` / `stage_wide` 各加 `CREATE DATABASE/TABLE IF NOT EXISTS`（对齐 uid_map）

**uid_map**：PASS 收到，后续用 `run_test.sh` 开 explain。

等大漏斗复审放行后再跑 explain。

[REDACTED]

[user] <timestamp>Wednesday, Sep 2, 2026, 9:55 PM (UTC+8)</timestamp>
<user_query>
[私聊#456] 日报可以上传云端
</user_query>

[assistant] 正在核对今日定稿并原样上传云端。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 已上传云端。

- **日期**：2026-09-02  
- **云端记录 ID**：87051  
- **状态**：inserted（新建成功）  
- **稿源**：`~/.dc-platform/memory/work-log/reports/日报-2026-09-02.md`（定稿原样上传）

填报页按 9.2 查一下应该能看到。

[REDACTED]

[user] <timestamp>Wednesday, Sep 2, 2026, 10:07 PM (UTC+8)</timestamp>
<user_query>
[私聊#457] 日报没有上传云端吗？
</user_query>

[assistant] [REDACTED]

[assistant] **已经上传了。**

你 21:55 左右说「日报可以上传云端」后，又初已按定稿原样上传：

- **日期**：2026-09-02  
- **云端记录 ID**：87051  
- **首次状态**：inserted（新建成功）

刚才又核对了一遍，云端仍是同一条记录（`87051`，状态 `updated`），说明 9.2 日报已在云端。

填报页请按 **2026-09-02** 查；若还看不到，可能是页面筛选或缓存，刷新后再试。

[REDACTED]
```
