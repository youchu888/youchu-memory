# TG 会话轮换归档 · 2026-07-29-0601

- 原因: daily(06:00)
- session_id: ses_1785189771529_ym0l5i
- cursor_chat_id: 017cd192-a233-4b54-86ac-00e8db3462b2
- 蒸馏: AI+heuristic

## 携带要点

- 群聊点名必回实质内容**：正文含「又初/初儿」、@ bot 或 @ 主人时，必须在群里给可验收答复；禁止「没@我」「群里不回」类推脱。
- bothub 未触发要重发新 id**：outbound 对方 poller 没拉到时，用**新 bus id** 重发（如 #5600→#5603），不要复用旧 msg_id；可在 bothub 按 `after_id` 核对 `to_agent=worker_ant`。
- DDL 改列勿用 IF NOT EXISTS 裸建**：`CREATE TABLE IF NOT EXISTS` 会**保留旧 schema**（例：`play_day_flag` 挡住新列 `is_eff_play`）；bootstrap 用 **`combined_N_ddl.sql` 全表 DROP+CREATE**，INSERT 补**显式列清单**，单表热修可单独 repair 脚本。
- 新 launcher 走 `_templates/`**：只填 `JOB` / `SQL` / `PROFILE`，调 `ops_system/_templates/spark_yarn_launcher.sh`；新 wf 先 **step0 DDL bootstrap** 再跑数据链，勿手抄 yarn 参数。
- [LESSON: device-tag,spark,ddl|Paimon/Spark 表结构变更时用 DROP+CREATE bootstrap，禁止仅靠 CREATE TABLE IF NOT EXISTS 期望新列生效]
- [LESSON: ops-system,templates,spark|新 Spark 任务 launcher 只填 JOB/SQL/PROFILE 走 _templates，wrapper 必须先 step0 DDL bootstrap 再跑数]
- **群聊点名必回实质内容**：正文含「又初/初儿」、@ bot 或 @ 主人时，必须在群里给可验收答复；禁止「没@我」「群里不回」类推脱。
- **bus 收件≠只有私聊 inbox**：发给猫猫/蓝猫审核线的 bus，又初可通过**协作群 TG 镜像**同步感知；答复时要区分「私聊 inbox 有无」与「业务上是否已收到」。
- **bothub 未触发要重发新 id**：outbound 对方 poller 没拉到时，用**新 bus id** 重发（如 #5600→#5603），不要复用旧 msg_id；可在 bothub 按 `after_id` 核对 `to_agent=worker_ant`。
- **设备标签不等狂人审阶段2**：知秋明确「设计/样例已给，先开发完 push git 再看」时，按 **library#46 + 姿态 F** 直接开阶段3，不阻塞在阶段2 HTML 审。
- **无 test 环境的标准交付**：Scala/Paimon 代码 + DDL 写完 → **push 配置库 `origin/dev`** → 群里发 **commit SHA + 改动路径** → 由狂人 **spark-submit** 跑数；又初不自 submit、不自验集群。
- **姿态 F 主链路**：`device_tag_wrapper.sh` 六步串行 ad→finance→video→active→dim→宽表，Paimon 落湖；DWM **中间3张有 dt**，dim/finance/宽表**无 dt 快照**；cron 挂 **user_tag wrapper 尾巴 UTC03:00**；lifecycle 用旧表 **9 档**，不加 network/screen/ua。
- **`dwm_device_tag_merge_pool_d_d` 是 v2 老线**：增量驱动池（delta/expiry/boundary），产物宽表是 `dws_device_tag_d_d`；**姿态 F step6 五表 JOIN，不经过 merge_pool**——被问表用途时要讲清「老架构 vs 当前 F 线」。
- **设备标签统一 Spark、海豚停更**：新开发/补数/验收只认 Spark wrapper；海豚 `wf_设备标签_日` 等 v1/v2（merge_pool、8 桶 UPSERT）为历史包袱，**跑通 F 后一并下线**，不在海豚修 SQL。
- **老海豚 P0 真错不修**：如 merge_pool preparedStatement 参数 bind bug，与「全迁 Spark」方向一致时，**不修海豚 SQL**，标「待 Spark 迁移下线」，与姿态 F 下线节奏合并处理。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-07-29-paimon-spark-表结构变更时用-drop-create-bootstrap-禁止仅靠-.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-07-29-姿态-f-主链路不经过-merge_pool-海豚老-task-p0-与-spark-迁移方向一.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-07-29-新-spark-任务-launcher-只填-job-sql-profile-走-_templa.md`

## AI 蒸馏原文（截断）

```
- **群聊点名必回实质内容**：正文含「又初/初儿」、@ bot 或 @ 主人时，必须在群里给可验收答复；禁止「没@我」「群里不回」类推脱。
- **bus 收件≠只有私聊 inbox**：发给猫猫/蓝猫审核线的 bus，又初可通过**协作群 TG 镜像**同步感知；答复时要区分「私聊 inbox 有无」与「业务上是否已收到」。
- **bothub 未触发要重发新 id**：outbound 对方 poller 没拉到时，用**新 bus id** 重发（如 #5600→#5603），不要复用旧 msg_id；可在 bothub 按 `after_id` 核对 `to_agent=worker_ant`。
- **设备标签不等狂人审阶段2**：知秋明确「设计/样例已给，先开发完 push git 再看」时，按 **library#46 + 姿态 F** 直接开阶段3，不阻塞在阶段2 HTML 审。
- **无 test 环境的标准交付**：Scala/Paimon 代码 + DDL 写完 → **push 配置库 `origin/dev`** → 群里发 **commit SHA + 改动路径** → 由狂人 **spark-submit** 跑数；又初不自 submit、不自验集群。
- **姿态 F 主链路**：`device_tag_wrapper.sh` 六步串行 ad→finance→video→active→dim→宽表，Paimon 落湖；DWM **中间3张有 dt**，dim/finance/宽表**无 dt 快照**；cron 挂 **user_tag wrapper 尾巴 UTC03:00**；lifecycle 用旧表 **9 档**，不加 network/screen/ua。
- **`dwm_device_tag_merge_pool_d_d` 是 v2 老线**：增量驱动池（delta/expiry/boundary），产物宽表是 `dws_device_tag_d_d`；**姿态 F step6 五表 JOIN，不经过 merge_pool**——被问表用途时要讲清「老架构 vs 当前 F 线」。
- **设备标签统一 Spark、海豚停更**：新开发/补数/验收只认 Spark wrapper；海豚 `wf_设备标签_日` 等 v1/v2（merge_pool、8 桶 UPSERT）为历史包袱，**跑通 F 后一并下线**，不在海豚修 SQL。
- **老海豚 P0 真错不修**：如 merge_pool preparedStatement 参数 bind bug，与「全迁 Spark」方向一致时，**不修海豚 SQL**，标「待 Spark 迁移下线」，与姿态 F 下线节奏合并处理。
- **DDL 改列勿用 IF NOT EXISTS 裸建**：`CREATE TABLE IF NOT EXISTS` 会**保留旧 schema**（例：`play_day_flag` 挡住新列 `is_eff_play`）；bootstrap 用 **`combined_N_ddl.sql` 全表 DROP+CREATE**，INSERT 补**显式列清单**，单表热修可单独 repair 脚本。
- **新 launcher 走 `_templates/`**：只填 `JOB` / `SQL` / `PROFILE`，调 `ops_system/_templates/spark_yarn_launcher.sh`；新 wf 先 **step0 DDL bootstrap** 再跑数据链，勿手抄 yarn 参数。
- **「开发完」= 集群验收通过**：代码 push 不算完工；姿态 F 以 **wrapper 六步全绿 + 宽表 step6 通过** 为闭环（如 dt 验数 dim/宽表行数对齐），再报「可上线」。

[LESSON: device-tag,spark,ddl|Paimon/Spark 表结构变更时用 DROP+CREATE bootstrap，禁止仅靠 CREATE TABLE IF NOT EXISTS 期望新列生效]

[LESSON: device-tag,dolphin,merge-pool|姿态 F 主链路不经过 merge_pool；海豚老 task P0 与 Spark 迁移方向一致时不修 SQL，等 F 跑通后整 wf 下线]

[LESSON: ops-system,templates,spark|新 Spark 任务 launcher 只填 JOB/SQL/PROFILE 走 _templates，wrapper 必须先 step0 DDL bootstrap 再跑数]
```

## 会话摘录（截断）

```
[user] [群聊#151] [群聊·@youchu8888]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: 初儿bus你的消息收到了吗？@worker_ant_bot
问题正文: 初儿bus你的消息收到了吗？@worker_ant_bot

[assistant] 先核对 agent-bus 收件状态，再回群。
收到了，通路没断。

上午狂人 **bus#5588 → #5596/#5597** 我是通过**协作群 TG 镜像**收到的（event 新字段 5 表复审全 PASS，7 个 session 已转 knight，等你 request-publish 发产）。这几条**没直接进又初 agent-bus 私聊收件箱**——bus 上是发给猫猫/蓝猫审核线的，群里同步算收到了。

· 07-27 设备标签 **bus#5554** 设计稿：agent-bus 私聊已收并已回执  
· 11:08 我又初 outbound **#5600** 发了设备标签阶段2 HTML 给狂人审  
· 刚 11:33「活着·值班中」也收到了，审核通道在岗

有要又初跟的活直接 @ 我就行。

[user] [群聊#152] [群聊·@youchu8888]: 【群聊硬规则】本条已点名初儿（@youchu_ai_bot / @主人 / 正文含「又初」「初儿」/回复本 bot），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @worker_ant_bot 
📤 又初→狂人（方案/正文）
主线 bus#5556 · 出站 bus#5603
────────────
【重发·设备标签阶段2回执送审】先前 outbound #5600 你侧 bothub 未触发，按你群里说的重发一次。

对照: library#46
本地 HTML: omdb/tgbot/outgoing/dws_device_tag_stage2_ack_2026-07-28.html

主人钦定:
1) DWM 按姿态F：中间3张有dt；dim/finance/宽表无dt快照（纠偏稿「6张全无dt」）
2) cron 选A串行：挂 user_tag wrapper 尾巴，同 UTC03:00
3) network/screen/ua → 审完不加（旧宽表/user dim/ETL皆无）
4) 旧py/phased 全废，按F重写
+ lifecycle 纠偏：用旧表/user_tag 9档(new/reactivated/oneday/growth/churn/silent/dormant/stable/low)，不用稿里 active_7d/churn_30 套
+ 阶段1：device_id fill=100%，直用

请审回执；放行后我开阶段3。查旧条可用 bus msg_id=5600。
问题正文: @worker_ant_bot 
📤 又初→狂人（方案/正文）
主线 bus#5556 · 出站 bus#5603
────────────
【重发·设备标签阶段2回执送审】先前 outbound #5600 你侧 bothub 未触发，按你群里说的重发一次。

对照: library#46
本地 HTML: omdb/tgbot/outgoing/dws_device_tag_stage2_ack_2026-07-28.html

主人钦定:
1) DWM 按姿态F：中间3张有dt；dim/finance/宽表无dt快照（纠偏稿「6张全无dt」）
2) cron 选A串行：挂 user_tag wrapper 尾巴，同 UTC03:00
3) network/screen/ua → 审完不加（旧宽表/user dim/ETL皆无）
4) 旧py/phased 全废，按F重写
+ lifecycle 纠偏：用旧表/user_tag 9档(new/reactivated/oneday/growth/churn/silent/dormant/stable/low)…

[assistant] 先核对 bus#5603 是否已发出，未发则补发狂人。
知秋，**bus#5603 已重发**，bothub 上能查到。

刚核了 8012：`to_agent=worker_ant` 没错，**#5603 是新 id**（不是复用 #5600）。#5600（11:08）和 #5603（11:37，reply bus#5556）两条都在狂人 inbox 里，你那边 poller 可能没拉到。

@worker_ant_bot 阶段2回执要点：
· 设计稿 `http://54.255.236.159:8012/library/dws_device_tag_d_d_design`（id=46）
· 姿态F：DWM 中间3张有dt，dim/finance/宽表无dt快照
· cron A 串行挂 user_tag wrapper UTC03:00；lifecycle 用旧表9档
· 阶段1 device_id fill=100% 直用；network/screen/ua 审完不加

HTML 在 `omdb/tgbot/outgoing/dws_device_tag_stage2_ack_2026-07-28.html`。你 bothub 还看不到就按 after_id≥5600 拉一下，有结论我开阶段3。

[user] [群聊#153] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 设备标签你弄好了吗？
问题正文: 设备标签你弄好了吗？

[assistant] 先核对设备标签当前进度，再回群。
还没弄好，卡在阶段2等狂人审。

当前进度：
· **阶段1** 已闭环：device_id 空率 100%，直用；Paimon 试点 SF-81 宽表曾落湖约 19 万行（test）
· **阶段2** 回执 HTML 已出（姿态F、6 湖表+wrapper、lifecycle 9 档），bus#5603 今早重发给狂人
· **卡点**：狂人侧还没审完阶段2（他排队等知秋定优先级），**prod 未上线**，阶段3 编码还没开

你 07-27 钦定的 Paimon 迁移方案在推，审完我就开阶段3。要插队的话你拍个优先级我 bus 催他。

[user] [群聊#154] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 审什么啊，设计，代码例子，全都给你了。你都开发完，开发之后提交git，再给你看
问题正文: 审什么啊，设计，代码例子，全都给你了。你都开发完，开发之后提交git，再给你看

[assistant] 收到，不等狂人审阶段2了。我先对齐现有设计和代码基线，再开干。
知秋，明白了，不等狂人审阶段2了。

设计稿 library#46 + user_tag 姿态F 代码样例都在，我按这个把 **6 湖表 + wrapper + 宽表** 开发完，test 跑通后 **push origin/dev**，再发你 commit 和验数结果。

今天先收口阶段3代码和 test 海豚，有卡点群里报。

[user] [群聊#155] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 这里没有测试环境，代码写完提交配置库，狂人给你sumbit到spark跑，懂了吧？
问题正文: 这里没有测试环境，代码写完提交配置库，狂人给你sumbit到spark跑，懂了吧？

[assistant] 懂了。

这边没有 test 环境可自跑，我按 library#46 + 姿态F 把 Scala/Paimon 代码和 DDL 写完，**直接 push 配置库 origin/dev**，不自己 submit Spark。

跑数你让狂人那边 spark-submit，我 push 完把 **commit SHA + 改动路径** 发群里，他拉代码上集群跑。

[user] [群聊#156] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot dwm_device_tag_merge_pool_d_d
这是什么东西？
问题正文: dwm_device_tag_merge_pool_d_d
这是什么东西？

[assistant] `dwm_device_tag_merge_pool_d_d` 是**设备标签 v2 的 DWM 中间表**，不是最终宽表。

作用一句话：**当天哪些 device 需要重算标签**——把「今日有动静的设备」+「滚动窗口边界要衰减的设备」合成一张日键表，宽表只 JOIN 这张表，避免像老方案那样每天扫十几亿全量设备。

里面主要是 `(dt, app_id, device_id, pool_source)`，来源三块：
· **delta**：今天活跃/播放/下单/广告/注册有新信号的设备  
· **expiry**：7/15/30 天窗口滑出边界、要重算 last_N 指标的  
· **boundary**：昨天宽表里登录很久但窗口计数还没清零的

产物表是 `dws.dws_device_tag_d_d`；merge_pool 是宽表前面的**增量驱动池**。

补充：这是 **07-19 v2 重设计（Scala/StarRocks）** 那条线的 L2 表，test 海豚 `wf_设备标签_日` 里有过。你现在推的**姿态 F 六步 wrapper**（ad/finance/video/active/dim → 宽表）是另一条，step6 直接五表 JOIN，**不经过 merge_pool**。所以这张表是 v2 增量架构用的，不是姿态 F 主链路必跑表。

[user] [群聊#157] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 设备标签都放到spark，不要留在海豚
问题正文: 设备标签都放到spark，不要留在海豚

[assistant] 知秋要求设备标签全部迁到 Spark、不再走海豚调度。我先核对当前仓库里设备标签在海豚与 Spark 上的分布，再给你可执行的群聊答复。
收到，按这条执行。

**结论**：设备标签**统一走 Spark**，不再在海豚挂日批任务。

**现状**：
- **姿态 F 主链路**（当前在推的）已经是 Spark：`device_tag_wrapper.sh` 串 6 步（ad → finance → video → active → dim → 宽表），Paimon 落湖，集群由狂人 submit，**不依赖海豚**。
- 海豚里还留着的是**老 v1/v2 线**（`wf_设备标签_日` / `wf_dwm_设备标签_日`、8 桶 UPSERT、merge_pool 等），属于历史包袱，**不再作为目标架构**。

**后续**：
1. 新开发、补数、上线验收**只认 Spark wrapper**，代码继续推 `origin/dev`。
2. 海豚侧设备标签 task **停更、不扩**；等姿态 F 跑通后再下线旧 wf（需狂人/运维配合停调度）。
3. `merge_pool` / `uid_map` 若还要用，也改成 Spark SQL + wrapper 挂接，**不回海豚**。

今天修完的 video 表 schema（`is_eff_play`）也是 Spark 路径上的，狂人重跑 wrapper step3+6 即可。

[user] [群聊#158] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot 所以你的标签任务已经开发完了？
问题正文: 所以你的标签任务已经开发完了？

[assistant] 还没完，不能算「开发完了」。

**现状**（设备标签 · 姿态 F）：
- **代码侧**：六步 Spark wrapper + 6 张 Paimon 表 SQL 已推 `origin/dev`；今天还修了 DDL（DROP 重建）和 video 表 `is_eff_play` 字段问题。
- **跑数侧**：`dt=2026-07-27` 前 5 步已过，**第 6 步宽表**因旧 schema 挂过；修复已推，等狂人在集群重跑 step3+6 验收。
- **上线**：**未 prod**；海豚老 `wf_设备标签_日` 按你要求不再维护，姿态 F 跑通后一起下线。

**一句话**：开发主体在 Spark 线上，还差 **step6 集群验收通过** 才能说做完；验收 OK 我再报「可上线」。

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 根因是 `CREATE TABLE IF NOT EXISTS` 保留了旧 schema（`play_day_flag`），新 DDL 的 `is_eff_play` 没生效。正在修 bootstrap：改为 DROP + CREATE，并给 phase2b 补上显式列清单。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 已处理 **bus#5621** 并 push 到 `origin/dev`（`21777d8f`）。

**根因**：`CREATE TABLE IF NOT EXISTS` 保留了旧表 schema（`play_day_flag`），新 DDL 的 `is_eff_play` 没生效，step6 编译挂。

**修法**：
- `combined_6_ddl.sql` — 六表改为 **DROP + CREATE**
- `phase2b_dwm_video.sql` — INSERT 补显式 col_list
- `repair_dwm_video_schema.sql` — 仅修 video 表的热修脚本

**狂人侧重跑**（dt=2026-07-27）：
1. `repair_dwm_video_schema.sql`
2. 重跑 phase2b（step3）
3. 重跑 step6 daily SQL

bus reply **#5622** 已发。

[REDACTED]

[user] <timestamp>Tuesday, Jul 28, 2026, 6:40 PM (UTC+8)</timestamp>
<user_query>
[群聊#156] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot dwm_device_tag_merge_pool_d_d
这是什么东西？
问题正文: dwm_device_tag_merge_pool_d_d
这是什么东西？
</user_query>

[user] <timestamp>Tuesday, Jul 28, 2026, 6:40 PM (UTC+8)</timestamp>
<user_query>
[群聊#156] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot dwm_device_tag_merge_pool_d_d
这是什么东西？
问题正文: dwm_device_tag_merge_pool_d_d
这是什么东西？
</user_query>

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] `dwm_device_tag_merge_pool_d_d` 是**设备标签 v2 的 DWM 中间表**，不是最终宽表。

作用一句话：**当天哪些 device 需要重算标签**——把「今日有动静的设备」+「滚动窗口边界要衰减的设备」合成一张日键表，宽表只 JOIN 这张表，避免像老方案那样每天扫十几亿全量设备。

里面主要是 `(dt, app_id, device_id, pool_source)`，来源三块：
· **delta**：今天活跃/播放/下单/广告/注册有新信号的设备  
· **expiry**：7/15/30 天窗口滑出边界、要重算 last_N 指标的  
· **boundary**：昨天宽表里登录很久但窗口计数还没清零的

产物表是 `dws.dws_device_tag_d_d`；merge_pool 是宽表前面的**增量驱动池
```
