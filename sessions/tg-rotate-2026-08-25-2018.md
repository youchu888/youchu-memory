# TG 会话轮换归档 · 2026-08-25-2018

- 原因: max_transcript(401KB>=400KB)
- session_id: ses_1787609856146_n267ys
- cursor_chat_id: 4562fe8a-c41c-463f-837e-427377ccf5e0
- 蒸馏: AI+heuristic

## 携带要点

- 主人说「重新推送昨天日报」时：先定位 `reports/日报-YYYY-MM-DD.md` 定稿，再跑 `post_daily_report_to_dm.py --date YYYY-MM-DD`，不要拿旧缓存或半成品推。
- agent-bus 派单：60 秒内先 `ack`，干完再 `reply` 结案；同一 Cursor 主会话处理，勿 spawn 新 Agent。
- 设计可视化审稿（如 metric_library v0.3 id=66）：对照源稿 id=67 逐条核对章节门禁（G1–G7、U1–U10、Phase 0–4+2.5），缺啥补啥，不要只回「主体达标」。
- 可视化 HTML 铁律：纯 HTML/SVG 手工 ER，禁 mermaid CDN；v0.3 新增字段用 `hi` 黄底高亮；浅色卡片底 `#f4f5f7`，TOC 可快跳。
- 再审/补稿前**必须先对平台现网正文**（本地稿 ≠ 现网）；若现网已是较新的「组合约束」版，应以现网为底再 merge 缺失段落，而不是盲目重传本地旧补丁。
- TG 日报私聊推送默认**只发正文**：去掉「📋 又初 · 日报 …（定稿自动推送）」类标题头；改 `post_daily_report_to_dm.py` 后要让主人在 TG 目视确认格式。
- 推送脚本/日报相关改动要同步进 `youchu-memory`（memory git），避免另一台机或定时任务仍用带头版本。
- 审稿常见缺口清单（本轮）：§1 九数基线、§12.5 事件字典自增长、§13.3 四机制、§15 三表导出、§16 `metric_standard` 映射；ER 锚点错位需单独修。
- 「组合约束」类增补（C1–C4：dim_uses 成对、G7-b 三角等式、published 多件套等）与上述核心缺口**都要保留**，不能后补一版把前一轮补丁盖掉。
- 覆盖上传开发平台可视化后：reply 里写清改了哪些 §、平台路径/id，并提示可按审稿意见转交下一审核人（如知秋）。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-25-tg-重推日报前先确认推送脚本已改为只发正文无标题头-推完请主人在私聊目视验收.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-25-补设计可视化前先拉平台现网正文作-merge-底稿-避免本地补丁被后续版本覆盖后再审仍报缺.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-25-收到-mode-work-派单-60-秒内-ack-完工-reply-结案-审稿类任务按回执缺口.md`

## AI 蒸馏原文（截断）

```
- 主人说「重新推送昨天日报」时：先定位 `reports/日报-YYYY-MM-DD.md` 定稿，再跑 `post_daily_report_to_dm.py --date YYYY-MM-DD`，不要拿旧缓存或半成品推。
- TG 日报私聊推送默认**只发正文**：去掉「📋 又初 · 日报 …（定稿自动推送）」类标题头；改 `post_daily_report_to_dm.py` 后要让主人在 TG 目视确认格式。
- 推送脚本/日报相关改动要同步进 `youchu-memory`（memory git），避免另一台机或定时任务仍用带头版本。
- agent-bus 派单：60 秒内先 `ack`，干完再 `reply` 结案；同一 Cursor 主会话处理，勿 spawn 新 Agent。
- 设计可视化审稿（如 metric_library v0.3 id=66）：对照源稿 id=67 逐条核对章节门禁（G1–G7、U1–U10、Phase 0–4+2.5），缺啥补啥，不要只回「主体达标」。
- 可视化 HTML 铁律：纯 HTML/SVG 手工 ER，禁 mermaid CDN；v0.3 新增字段用 `hi` 黄底高亮；浅色卡片底 `#f4f5f7`，TOC 可快跳。
- 审稿常见缺口清单（本轮）：§1 九数基线、§12.5 事件字典自增长、§13.3 四机制、§15 三表导出、§16 `metric_standard` 映射；ER 锚点错位需单独修。
- 「组合约束」类增补（C1–C4：dim_uses 成对、G7-b 三角等式、published 多件套等）与上述核心缺口**都要保留**，不能后补一版把前一轮补丁盖掉。
- 再审/补稿前**必须先对平台现网正文**（本地稿 ≠ 现网）；若现网已是较新的「组合约束」版，应以现网为底再 merge 缺失段落，而不是盲目重传本地旧补丁。
- 覆盖上传开发平台可视化后：reply 里写清改了哪些 §、平台路径/id，并提示可按审稿意见转交下一审核人（如知秋）。

[LESSON: daily-report|TG 重推日报前先确认推送脚本已改为只发正文无标题头，推完请主人在私聊目视验收]
[LESSON: dev-platform|补设计可视化前先拉平台现网正文作 merge 底稿，避免本地补丁被后续版本覆盖后再审仍报缺]
[LESSON: agent-bus|收到 mode=work 派单 60 秒内 ack、完工 reply 结案，审稿类任务按回执缺口清单逐条补齐再上传]
```

## 会话摘录（截断）

```
[user] [私聊#417] 重新推送一下昨天日报 我看看对不对

[assistant] 先找昨天（2026-08-24）的日报定稿，并核对推送脚本是否已去掉标题头，再重新推给你看。推送脚本还带着标题头；按你上午说的改成只推正文，再重推昨天日报。已重推 **2026-08-24** 日报到私聊，且已按上午说的去掉「📋 又初 · 日报 …（定稿自动推送）」头，现在只推正文。

请在 TG 里看一下这版格式对不对。昨天日报已重推到私聊，且已去掉标题头、只发正文；推送脚本改动也已同步到 memory。你在 TG 里看一下格式是否符合预期。memory 同步已完成，推送脚本改动已上到 youchu-memory 主分支。

[user] <timestamp>Tuesday, Aug 25, 2026, 12:36 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-25 12:35（周二），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-25 06:17:12 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/da…

[assistant] 先找昨天（2026-08-24）的日报定稿，并核对推送脚本是否已去掉标题头，再重新推给你看。

[assistant] 推送脚本还带着标题头；按你上午说的改成只推正文，再重推昨天日报。

[assistant] 已重推 **2026-08-24** 日报到私聊，且已按上午说的去掉「📋 又初 · 日报 …（定稿自动推送）」头，现在只推正文。

请在 TG 里看一下这版格式对不对。

[user] <timestamp>Tuesday, Aug 25, 2026, 12:39 PM (UTC+8)</timestamp>

<user_query>Briefly inform the user about the task result and perform any follow-up actions (if needed). If there's no follow-ups needed, don't explicitly say that.</user_query>

[assistant] 昨天日报已重推到私聊，且已去掉标题头、只发正文；推送脚本改动也已同步到 memory。你在 TG 里看一下格式是否符合预期。

[user] <timestamp>Tuesday, Aug 25, 2026, 12:41 PM (UTC+8)</timestamp>

<user_query>Briefly inform the user about the task result and perform any follow-up actions (if needed). If there's no follow-ups needed, don't explicitly say that.</user_query>

[assistant] memory 同步已完成，推送脚本改动已上到 youchu-memory 主分支。

[user] <timestamp>Tuesday, Aug 25, 2026, 7:07 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7418 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7418 --text "[ACK] bus#7418 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7418 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：【狂人→又初·审核回执】v0.3 完整设计可视化 (id=66) 对照源稿 (id=67) 审完。  ## 主体达标 ✅ - 纯 HTML/SVG 手工布局 ER (13 实体框 + 关系边 + 基数), 无 mermaid CDN 依赖 —— 符合画图铁律 - 底色 #f4f5f7 浅色, 卡片式清晰 - v0.3 新增字段 hi 高亮 (yellow bg) 一眼定位 - 章节 §0/§2/ER/字段卡/关系清单/§8门禁/§9用例/§10-11/§12/§13/§14/§17/自检 齐, TOC 快跳 - G1-G7 门禁全, U1-U10 用例全, Phase 0-4 + 2.5 全  ## 核心缺 5…

[assistant] 先 ACK，再读回执全文并对齐源稿，按缺的 5 条补设计可视化。

[assistant] ACK 已发。正在定位可视化稿与源稿，按 5 条缺口补齐。

[assistant] 正在按回执补齐 HTML 五处缺口，并顺手修 ER 三处错位。

[assistant] bus#7418 已结案。

按狂人审稿把可视化 **id=66** 补齐并覆盖上传：
- §1 存量基线（264/175/230/75/67/10/3/464/27）
- §12.5 事件字典自增长
- §13.3 四条关键机制
- §15 三表导出、§16 `metric_standard` 映射
- ER 三处锚点微调

平台：`/library/metric_library_v03_design_visual_20260825` · 已 reply bus#7419

[user] <timestamp>Tuesday, Aug 25, 2026, 7:29 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7421 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7421 --text "[ACK] bus#7421 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7421 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：【狂人→又初·再审 bus#7420】接你新版 metric_library_v03_design_visual_20260825 (48k, 之前 41k)。  ## 组合约束这轮补得好 ✅ - C1 dim_uses = (dim × role) 成对: 反例表 (4 条) + U8/U9 合法组合示例, 清楚 - C2 G7-b 三角等式: concept.entity = event_ext[main_event].entity, 单画 concept→glossary 边表达不出这条 —— 讲透了 - C3 published 多件套 (G7-a/G4/G2/G5/diverged SLA 5 组 A…

[assistant] 先 ACK，再拉完整再审正文核对还缺哪几条。

[assistant] 再审说 5 条仍缺——先对本地稿与平台现网正文，看是被后一版盖掉还是位置不对。

[assistant] 平台现网是带「组合约束」的后一版，盖掉了本地 5 条补丁。以现网为底合并补齐。

[assistant] 以现网「组合约束」版为底，一次性补齐 5 条核心缺失并重新上传。

[assistant] bus#7421 已结案。

根因是上一轮五条补丁被后续「组合约束」版盖掉了。现已以组合约束版为底，一次合并补齐并覆盖 **id=66**：

- §1 九数基线、§12.5、§13.3 四机制、§15、§16  
- C1–C4 保留  

现网已核到九数与 `metadata_event_publish` 等均在；已 reply，可按狂人说的送知秋。
```
