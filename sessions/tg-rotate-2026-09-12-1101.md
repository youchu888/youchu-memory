# TG 会话轮换归档 · 2026-09-12-1101

- 原因: daily(06:00)
- session_id: ses_1789138830660_iv8q87
- cursor_chat_id: 5282d8f9-b83a-44c4-b894-8d69de8da81e
- 蒸馏: AI+heuristic

## 携带要点

- > **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。
- `dim.dim_app_attribution_config.is_run`：**1 = 已启用**，**0 = 未启用 / 拉黑**；解释码值先查元数据字段注释，再答口径。
- 前端主列表过滤条件：`IFNULL(is_run, 0) = 1`；NULL 视同未启用。
- 日批 sync 对**新入围 app** 会自动写入 `is_run = 1`。
- sync **不会**把已有 `is_run = 0` 的行改回 1；拉黑/黑名单状态会保留。
- 现网**归因计算入围**已不靠 `is_run`，看**双 flag**；`is_run` 主要是前端展示开关。
- 排查「前端看不到 / 列表缺失」→ 先查 `is_run`；排查「归因没算 / 没入围」→ 查双 flag，别混用。
- 口径问答模板：`is_run` 管展示与黑名单；归因入围另有一套 flag，两者职责分离。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-12-is_run-attribution-frontend-口径.md`

## AI 蒸馏原文（截断）

```
## 携带要点

- `dim.dim_app_attribution_config.is_run`：**1 = 已启用**，**0 = 未启用 / 拉黑**；解释码值先查元数据字段注释，再答口径。
- 前端主列表过滤条件：`IFNULL(is_run, 0) = 1`；NULL 视同未启用。
- 日批 sync 对**新入围 app** 会自动写入 `is_run = 1`。
- sync **不会**把已有 `is_run = 0` 的行改回 1；拉黑/黑名单状态会保留。
- 现网**归因计算入围**已不靠 `is_run`，看**双 flag**；`is_run` 主要是前端展示开关。
- 排查「前端看不到 / 列表缺失」→ 先查 `is_run`；排查「归因没算 / 没入围」→ 查双 flag，别混用。
- 口径问答模板：`is_run` 管展示与黑名单；归因入围另有一套 flag，两者职责分离。

[LESSON: dim_app_attribution_config|is_run|attribution|frontend|口径] 归因是否入围看双 flag，不看 is_run；is_run 只管前端主列表展示与拉黑保留，sync 不会把 0 改回 1。
```

## 会话摘录（截断）

```
[user] [私聊#480] is_run的码值1是什么0是什么

[assistant] 先查元数据里 `is_run` 的字段注释，再给你准确定义。`dim.dim_app_attribution_config.is_run`：

- **1**：已启用（前端主列表只看 `IFNULL(is_run,0)=1`；日批 sync 对新入围 app 自动写成 1）
- **0**：未启用 / 拉黑（不进主列表；sync 不会把已有行改回 1，黑名单会保留）

补充：现网归因计算已不靠 `is_run` 卡入围，入围看双 flag；`is_run` 主要给前端展示用。

[user] <timestamp>Saturday, Sep 12, 2026, 10:26 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-12 10:26（周六），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-11 23:00:02 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/d…

[assistant] 先查元数据里 `is_run` 的字段注释，再给你准确定义。

[assistant] `dim.dim_app_attribution_config.is_run`：

- **1**：已启用（前端主列表只看 `IFNULL(is_run,0)=1`；日批 sync 对新入围 app 自动写成 1）
- **0**：未启用 / 拉黑（不进主列表；sync 不会把已有行改回 1，黑名单会保留）

补充：现网归因计算已不靠 `is_run` 卡入围，入围看双 flag；`is_run` 主要给前端展示用。
```
