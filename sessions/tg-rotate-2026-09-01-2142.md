# TG 会话轮换归档 · 2026-09-01-2142

- 原因: max_transcript(415KB>=400KB)
- session_id: ses_1788259441184_c7oci7
- cursor_chat_id: 7145abe9-ac95-4232-a836-140b772bf2b8
- 蒸馏: AI+heuristic

## 携带要点

- 工作簿三件套须同频**：`project_youchu_workbook_tasks.md`（权威任务板）、`workbook_supplemental.json`（自开/增补项）、`.cursor/work-log/当日.md` 必须按**当天实活**一起更新；只写 work-log 不回写 task 板，群/bus 进展就会和 reality 脱节。
- PINNED #13 铁律**：读到 09:00 群工作簿或当天有新进展 → **当天内整表覆盖** `project_youchu_workbook_tasks.md`；大活自开也要登「自开任务」，不能只记 work-log。
- 11:00 后仍有交付**：不要只依赖 09:01 一次；应自动补发或私聊一句实态摘要，避免下午活漏报。
- 代码与配置须真读 JSON**：`workbook_progress_service.py` 里 `_local_tracking_items()` 若硬编码旧标题，即使 `workbook_supplemental.json` 已更新，群文案也不会变——增补项必须从 JSON 读。
- 进展汇报答问顺序**：先核对 task 板、work-log、各专项实态，再汇总；主人追问「为什么不执行」时先定位根因与落点文件，再补救，不复读模板。
- Spark/复审链状态表达**：代码本地已改但未 push、等狂人 PASS 前 dim/下游 **HOLD**——进展汇报须区分「写法层通过 / 远程未 push / 下游 HOLD」，避免写「已完成」。
- [LESSON: workbook|读到群簿或有大活交付后，当天同步 task 板 + supplemental + work-log，大活后再补发进展，禁止只依赖 09:01 一次]
- [LESSON: workbook|workbook_progress_service 增补项必须读 workbook_supplemental.json，禁止 _local_tracking_items 硬编码]
- **工作簿三件套须同频**：`project_youchu_workbook_tasks.md`（权威任务板）、`workbook_supplemental.json`（自开/增补项）、`.cursor/work-log/当日.md` 必须按**当天实活**一起更新；只写 work-log 不回写 task 板，群/bus 进展就会和 reality 脱节。
- **PINNED #13 铁律**：读到 09:00 群工作簿或当天有新进展 → **当天内整表覆盖** `project_youchu_workbook_tasks.md`；大活自开也要登「自开任务」，不能只记 work-log。
- **09:01 自动汇报 ≠ 全天闭环**：定时发送若早于当天实活（work-log 仍空、下午才 push），主人看到的仍是旧态；不能发完一次就停。
- **大活交付须触发二次同步**：push / 交审 / 批处理 / bus 口径变更等「大活」完成后，**当天**再改 task 板 + supplemental + work-log，并补发进展（`--force-repost` 或等价机制）。
- **11:00 后仍有交付**：不要只依赖 09:01 一次；应自动补发或私聊一句实态摘要，避免下午活漏报。
- **代码与配置须真读 JSON**：`workbook_progress_service.py` 里 `_local_tracking_items()` 若硬编码旧标题，即使 `workbook_supplemental.json` 已更新，群文案也不会变——增补项必须从 JSON 读。
- **task 板 stale 自检**：`project_youchu_workbook_tasks.md` 整表更新时间 vs 当日 work-log / 群簿对比；超过 1 天未 sync 即执行缺口，不是信息缺口。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-读到群簿或有大活交付后-当天同步-task-板-supplemental-work-log-大活.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-workbook_progress_service-增补项必须读-workbook_supple.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-task-板与-work-log-超过-1-天未对齐即执行缺口-先刷新-task-板再答进展问.md`

## AI 蒸馏原文（截断）

```
- **工作簿三件套须同频**：`project_youchu_workbook_tasks.md`（权威任务板）、`workbook_supplemental.json`（自开/增补项）、`.cursor/work-log/当日.md` 必须按**当天实活**一起更新；只写 work-log 不回写 task 板，群/bus 进展就会和 reality 脱节。
- **PINNED #13 铁律**：读到 09:00 群工作簿或当天有新进展 → **当天内整表覆盖** `project_youchu_workbook_tasks.md`；大活自开也要登「自开任务」，不能只记 work-log。
- **09:01 自动汇报 ≠ 全天闭环**：定时发送若早于当天实活（work-log 仍空、下午才 push），主人看到的仍是旧态；不能发完一次就停。
- **大活交付须触发二次同步**：push / 交审 / 批处理 / bus 口径变更等「大活」完成后，**当天**再改 task 板 + supplemental + work-log，并补发进展（`--force-repost` 或等价机制）。
- **11:00 后仍有交付**：不要只依赖 09:01 一次；应自动补发或私聊一句实态摘要，避免下午活漏报。
- **代码与配置须真读 JSON**：`workbook_progress_service.py` 里 `_local_tracking_items()` 若硬编码旧标题，即使 `workbook_supplemental.json` 已更新，群文案也不会变——增补项必须从 JSON 读。
- **task 板 stale 自检**：`project_youchu_workbook_tasks.md` 整表更新时间 vs 当日 work-log / 群簿对比；超过 1 天未 sync 即执行缺口，不是信息缺口。
- **进展汇报答问顺序**：先核对 task 板、work-log、各专项实态，再汇总；主人追问「为什么不执行」时先定位根因与落点文件，再补救，不复读模板。
- **补救最小闭环**：整表刷新 task 板 → 更新 supplemental → 修服务读 JSON → 补发含下午实活的群+bus 进展；四步缺一仍算未闭环。
- **Spark/复审链状态表达**：代码本地已改但未 push、等狂人 PASS 前 dim/下游 **HOLD**——进展汇报须区分「写法层通过 / 远程未 push / 下游 HOLD」，避免写「已完成」。

[LESSON: workbook|读到群簿或有大活交付后，当天同步 task 板 + supplemental + work-log，大活后再补发进展，禁止只依赖 09:01 一次]
[LESSON: workbook|workbook_progress_service 增补项必须读 workbook_supplemental.json，禁止 _local_tracking_items 硬编码]
[LESSON: workbook|task 板与 work-log 超过 1 天未对齐即执行缺口，先刷新 task 板再答进展问]
```

## 会话摘录（截断）

```
[user] [私聊#447] 现在我们手头的任务 都什么进展，工作簿是否按照现在的实际进展每日汇报进度？

[assistant] 先核对工作簿、当日 work-log 和各专项实际状态，再汇总给你。
按今天实际状态汇总如下。

---

## 工作簿是否在按日更新？

**没有做到。** 权威任务板 `project_youchu_workbook_tasks.md` 上次整表更新是 **2026-08-10**，距今 22 天，和今天活态严重脱节：

| 文件 | 状态 |
|------|------|
| `project_youchu_workbook_tasks.md` | 仍写「页面停留已完成」「大漏斗 M 压测待推 stage4」，缺 9/1 Spark/指标库/bus#7787 |
| `.cursor/work-log/2026-09-01.md` | **今天在写**，有 Spark、指标库流水 |
| 狂人 09:00 群工作簿 | 本机未见 9 月新簿同步进上述 task 文件 |

按规则应是：**读到新簿 → 立即覆盖 task 文件**；大活自开也要登「自开任务」。今天只落了 work-log，**没回写 task 板**，群/bus 每日进展也就对不齐。

若你要，我可以按下面清单 **立刻刷新** `project_youchu_workbook_tasks.md`（不动 git commit，除非你点名）。

---

## 手头任务进展（2026-09-01 21:06）

### 1. 设备标签指纹 + uid_map（Spark · 最高优先）

| 项 | 状态 |
|----|------|
| 写法层（PK/GROUP BY/64 位/四路 `_r`） | 狂人 #7778 已通过 |
| 口径起点 | 知秋 #7787：**2026-08-01**，bus#7778 阻断作废 |
| 代码 | SQL/spec **本地已改**（四路 `dt >= '2026-08-01'`、字段语义改「自 08-01 起首次」） |
| 远程 | **未 push**；origin/dev 仍是 `2f95e122`（无 #7787 改动） |
| 下一步 | commit+push → bus 请狂人复审 → PASS 后再沙箱 explain |
| dim / 六张 dwm / dws 宽表 | **继续 HOLD** |

### 2. 大漏斗 Spark（#7735）

| 项 | 状态 |
|----|------|
| stage SQL + sandbox steps | 已在 `2f95e122` |
| 复审 | 与 uid_map 同链，**等狂人 PASS**（bus#7760/#7756） |
| prod full_chain | 未动（符合规则） |

### 3. 指标库 Phase1 数据层（今天主线）

| 项 |…

[user] [私聊#448] 我三令五申，工作簿每天按照实际进度汇报，为什么不执行？

[assistant] 你说得对，这是执行缺口，不是信息缺口。我先核对工作簿相关文件和规则落点，然后立刻把 task 板按今天实际进度刷新。
根因已定位：09:01 自动发过进展，但早于当天实活；task 板 22 天未 sync。正在刷新 task 板与增补项，并补发含下午 Spark/指标库进展的今日汇报。
发现根因：`workbook_progress_service.py` 里增补项被硬编码，未读 `workbook_supplemental.json`。正在修复。
你说得对，这是执行问题，不是你没说清楚。

## 为什么没做到

**不是完全没发。** 今天 09:01 群进展 + bus#7750 已自动发出。但那份内容**早于当天实活**——Spark 是 11:05 才开工，指标库是 16:50 才推，09:01 的 work-log 还是空的，所以你看到的仍是 8 月底那套探针数。

**又初侧没闭环的三处：**

1. **`project_youchu_workbook_tasks.md` 自 08-10 起没 sync**（PINNED #13 写了「读到新簿立刻改」，我只写了 work-log，没回写 task 板）
2. **`workbook_supplemental.json` 更新了也不进群文案**——代码里 `_local_tracking_items()` **硬编码**两项旧标题，**不读 JSON**（已修）
3. **没有「大活交付后再报一次」的机制**——09:01 发完就停了，下午 Spark/指标库/ bus#7787 都没触发补发

## 刚才已做的补救

| 动作 | 状态 |
|------|------|
| 整表刷新 `project_youchu_workbook_tasks.md`（对齐 09-01 团队簿 + Spark/指标库） | ✅ |
| 更新 `workbook_supplemental.json`（指纹 uid_map / 大漏斗 / 指标库 Phase1） | ✅ |
| 修 `workbook_progress_service.py`：增补项读 JSON + 指标库进展段 | ✅ |
| **补发**今日工作簿进展（含下午实活，`--force-repost`） | ✅ 已发群+bus |

## 以后怎么保证

- **09:00 群簿** → 当天内改 task 板  
- **有大活交付**（push/交审/批处理）→ **当天**改 task 板 + supplemental + work-log  
- **11:00 后还有大活** → 不再只依赖 09:01；自动补发或私聊你一句实态摘要  

今天 task 板路径：`~/.dc-platform/memory/project_youc…

[user] <timestamp>Tuesday, Sep 1, 2026, 9:10 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-01 21:10（周二），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-01 21:07:25 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/data…

[assistant] 你说得对，这是执行缺口，不是信息缺口。我先核对工作簿相关文件和规则落点，然后立刻把 task 板按今天实际进度刷新。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 根因已定位：09:01 自动发过进展，但早于当天实活；task 板 22 天未 sync。正在刷新 task 板与增补项，并补发含下午 Spark/指标库进展的今日汇报。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 发现根因：`workbook_progress_service.py` 里增补项被硬编码，未读 `workbook_supplemental.json`。正在修复。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 你说得对，这是执行问题，不是你没说清楚。

## 为什么没做到

**不是完全没发。** 今天 09:01 群进展 + bus#7750 已自动发出。但那份内容**早于当天实活**——Spark 是 11:05 才开工，指标库是 16:50 才推，09:01 的 work-log 还是空的，所以你看到的仍是 8 月底那套探针数。

**又初侧没闭环的三处：**

1. **`project_youchu_workbook_tasks.md` 自 08-10 起没 sync**（PINNED #13 写了「读到新簿立刻改」，我只写了 work-log，没回写 task 板）
2. **`workbook_supplemental.json` 更新了也不进群文案**——代码里 `_local_tracking_items()` **硬编码**两项旧标题，**不读 JSON**（已修）
3. **没有「大活交付后再报一次」的机制**——09:01 发完就停了，下午 Spark/指标库/ bus#7787 都没触发补发

## 刚才已做的补救

| 动作 | 状态 |
|------|------|
| 整表刷新 `project_youchu_workbook_tasks.md`（对齐 09-01 团队簿 + Spark/指标库） | ✅ |
| 更新 `workbook_supplemental.json`（指纹 uid_map / 大漏斗 / 指标库 Phase1） | ✅ |
| 修 `workbook_progress_service.py`：增补项读 JSON + 指标库进展段 | ✅ |
| **补发**今日工作簿进展（含下午实活，`--force-repost`） | ✅ 已发群+bus |

## 以后怎么保证

- **09:00 群簿** → 当天内改 tas…
```
