# TG 会话轮换归档 · 2026-08-29-1554

- 原因: max_transcript(402KB>=400KB)
- session_id: ses_1787784776434_aql7vo
- cursor_chat_id: adb506f4-ee21-4075-a03e-c8a6ba906d41
- 蒸馏: AI+heuristic

## 携带要点

- 狂人 #201 卡点：**数据层**（published=0、G4 空跑），不是 v0.3 结构层；解法是推已齐门禁的样例，不是改表结构。
- 260 条 draft 缺 `definition`/`req_ref`：**禁止**批量改 lifecycle；要上 published 须 P1 补齐再迁。
- 样例推送 SQL：`docs/metric_library_phase1_publish_samples_20260829.sql`；修完 bus 报三数请对方只读重验（#7602 → #7603 PASS）。
- G5 分两层**：库内 `diverged_pending` 且 `is_primary` 违规可为 0；**应用层**门禁（service/router 做 G4/G5 validate）当时**未落地**，勿混为一谈。
- 库内尚有 `avg×2`、`count_distinct×4` 同类禁项：须对方点头后再清，勿擅自批量改。
- [LESSON: metric-library,derived,ratio,whitelist|derived 比率指标禁止 `default_aggregation=ratio`；按 v0.3 白名单留空 agg，用分子/分母 FK 表达比率]
- Phase1 过关 ≠ 只灌 draft：test 上须真有 `lifecycle=published` 且逐条满足 G4，API 才能切概念层。
- 推 published 前先真 COUNT：`published / draft / orphaned`（本轮：10 / 260 / 10，库 `172.31.6.193/metadata`）。
- **G4 四件套**：`definition` + `req_ref` + ≥1 active impl + 白名单 `default_aggregation`；另核 **G7-a**：`entity_code`、`main_event_code`、`time_window`。
- Unblock 二选一：P1 迁存量，或先推 **Phase1.5 种子 10 条**（`seed:phase1.5`，如 `user_register_cnt` 等）。
- **G5 分两层**：库内 `diverged_pending` 且 `is_primary` 违规可为 0；**应用层**门禁（service/router 做 G4/G5 validate）当时**未落地**，勿混为一谈。
- **ratio 口径（v0.3 §11）**：白名单仅 `sum/count/bitmap_union_count/max/min`；**不含** `ratio`/`avg`/`count_distinct`。
- 比率类指标：`metric_kind=derived` + 分子/分母 FK；`default_aggregation` 应**留空**，不能写 `agg=ratio`（含已 published 的 `order_paid_rate`）。
- 已清 12 条 derived 的 `agg=ratio`：`docs/metric_library_phase1_clear_derived_ratio_agg_20260829.sql`；FK 保留。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-29-g5-库内违规-0-应用层已落地-复验须分开报库内数据与-service-router-是否做-.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-29-derived-比率指标禁止-default_aggregation-ratio-按-v0-3-.md`

## AI 蒸馏原文（截断）

```
- 狂人 #201 卡点：**数据层**（published=0、G4 空跑），不是 v0.3 结构层；解法是推已齐门禁的样例，不是改表结构。
- Phase1 过关 ≠ 只灌 draft：test 上须真有 `lifecycle=published` 且逐条满足 G4，API 才能切概念层。
- 推 published 前先真 COUNT：`published / draft / orphaned`（本轮：10 / 260 / 10，库 `172.31.6.193/metadata`）。
- **G4 四件套**：`definition` + `req_ref` + ≥1 active impl + 白名单 `default_aggregation`；另核 **G7-a**：`entity_code`、`main_event_code`、`time_window`。
- 260 条 draft 缺 `definition`/`req_ref`：**禁止**批量改 lifecycle；要上 published 须 P1 补齐再迁。
- Unblock 二选一：P1 迁存量，或先推 **Phase1.5 种子 10 条**（`seed:phase1.5`，如 `user_register_cnt` 等）。
- 样例推送 SQL：`docs/metric_library_phase1_publish_samples_20260829.sql`；修完 bus 报三数请对方只读重验（#7602 → #7603 PASS）。
- **G5 分两层**：库内 `diverged_pending` 且 `is_primary` 违规可为 0；**应用层**门禁（service/router 做 G4/G5 validate）当时**未落地**，勿混为一谈。
- **ratio 口径（v0.3 §11）**：白名单仅 `sum/count/bitmap_union_count/max/min`；**不含** `ratio`/`avg`/`count_distinct`。
- 比率类指标：`metric_kind=derived` + 分子/分母 FK；`default_aggregation` 应**留空**，不能写 `agg=ratio`（含已 published 的 `order_paid_rate`）。
- 已清 12 条 derived 的 `agg=ratio`：`docs/metric_library_phase1_clear_derived_ratio_agg_20260829.sql`；FK 保留。
- 库内尚有 `avg×2`、`count_distinct×4` 同类禁项：须对方点头后再清，勿擅自批量改。

[LESSON: metric-library,G5,application-layer|G5「库内违规=0」≠ 应用层已落地；复验须分开报库内数据与 service/router 是否做 G4/G5 validate]

[LESSON: metric-library,derived,ratio,whitelist|derived 比率指标禁止 `default_aggregation=ratio`；按 v0.3 白名单留空 agg，用分子/分母 FK 表达比率]
```

## 会话摘录（截断）

```
[user] [私聊#420] 处理#201狂人说的问题

[assistant] 理解为：`dbprogramming` · `test` · 指标库 Phase1 · published=0 → 把 concept 推到 published（或先出样例），解开狂人#201 复验卡点。先对齐现有材料与库内现状。先连 test metadata 实查，看哪些 concept 已齐 G4 四件套、卡在哪一步。10 条 phase1.5 种子已齐 G4 四件套。接着核 G7-a（entity/main_event）、time_window，并查 G5 是否已在应用层落地。确认：10 条种子齐 G4/G7-a；其余 260 条无 req_ref/definition，不能硬推 published。G5 应用层门禁未落地。先把 10 条样例推到 published。#201 已处理：test 上 **published 从 0 → 10**。

## 结论

狂人卡点是数据层，不是结构。按他给的二选一，先推 **Phase1.5 种子 10 条样例**（四件套已齐），没批量改 264 存量 lifecycle。

现网真 COUNT（`172.31.6.193/metadata`）：

| lifecycle | count |
|-----------|------:|
| published | **10** |
| draft | 260 |
| orphaned | 10 |

10 条 published（`user_register_cnt` 等）均满足：definition + req_ref + ≥1 active impl + 白名单聚合 + entity/main_event；无 diverged。

## G5（狂人追问）

- **库内**：diverged_pending 同时挂 is_primary 的违规 = **0**
- **应用层**：门禁 **未落地**（service/router 只有 CRUD，无 G4/G5 validate）

## 交付

- SQL：`docs/metric_library_phase1_publish_samples_20260829.sql`
- 已 bus 狂人（#7602）请按只读检查重验

剩余 260 条 draft 仍缺 definition/req_ref，要上 published 须走 P1 补齐再迁。需要的话我可以接着补 G5 API 门禁或开 P1 迁存量。

已沉淀经验：`.claude/memory/lessons/2026-08-29-指标库-phase1-published样例推送-禁批量改lifecycle.md`

[user] <timestamp>Saturday, Aug 29, 2026, 3:14 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-29 15:13（周六），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-29 15:11:12 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/da…

[assistant] 理解为：`dbprogramming` · `test` · 指标库 Phase1 · published=0 → 把 concept 推到 published（或先出样例），解开狂人#201 复验卡点。先对齐现有材料与库内现状。

[assistant] 先连 test metadata 实查，看哪些 concept 已齐 G4 四件套、卡在哪一步。

[assistant] 10 条 phase1.5 种子已齐 G4 四件套。接着核 G7-a（entity/main_event）、time_window，并查 G5 是否已在应用层落地。

[assistant] 确认：10 条种子齐 G4/G7-a；其余 260 条无 req_ref/definition，不能硬推 published。G5 应用层门禁未落地。先把 10 条样例推到 published。

[assistant] #201 已处理：test 上 **published 从 0 → 10**。

## 结论

狂人卡点是数据层，不是结构。按他给的二选一，先推 **Phase1.5 种子 10 条样例**（四件套已齐），没批量改 264 存量 lifecycle。

现网真 COUNT（`172.31.6.193/metadata`）：

| lifecycle | count |
|-----------|------:|
| published | **10** |
| draft | 260 |
| orphaned | 10 |

10 条 published（`user_register_cnt` 等）均满足：definition + req_ref + ≥1 active impl + 白名单聚合 + entity/main_event；无 diverged。

## G5（狂人追问）

- **库内**：diverged_pending 同时挂 is_primary 的违规 = **0**
- **应用层**：门禁 **未落地**（service/router 只有 CRUD，无 G4/G5 validate）

## 交付

- SQL：`docs/metric_library_phase1_publish_samples_20260829.sql`
- 已 bus 狂人（#7602）请按只读检查重验

剩余 260 条 draft 仍缺 definition/req_ref，要上 published 须走 P1 补齐再迁。需要的话我可以接着补 G5 API 门禁或开 P1 迁存量。

已沉淀经验：`.claude/memory/lessons/2026-08-29-指标库-phase1-published样例推送-禁批量改lifecycle.md`

[user] <timestamp>Saturday, Aug 29, 2026, 3:34 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7603 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7603 --text "[ACK] bus#7603 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7603 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：【复验通过 · 我在 test 库实测过, 你报的数属实】  lifecycle: published 10 / draft 260 / orphaned 10 — 与你一致。  G4 四件套逐条核过, 10 条全齐(definition·req_ref·active impl=1·白名单 agg 均无缺), entity_code / main_event_code 也都有, time_window 统一 day: ad_click_cnt · ad_impression_cnt · app_page_view_cnt · order_created_cnt · order_paid_cnt · order…

[assistant] 先 ACK bus#7603，再核对 `order_paid_rate` 那条确认项。

[assistant] ACK 被标成 silent 丢了，先重发 ACK，同时查 G2 对 `ratio` 的原文定论。

[assistant] 定论按 v0.3：`ratio` 不进白名单。正在清掉 12 条 derived 的 `agg=ratio`，再实质 reply。

[assistant] bus#7603 已结：复验 PASS 确认，并定了 `ratio` 口径。

**定论**：`ratio` **不算**白名单（v0.3 §11：只认 sum/count/bitmap_union_count/max/min）。比率靠 `derived` + 分子/分母 FK，不写 `agg=ratio`。

**已改库**（test）：12 条 derived 的 `default_aggregation` 已清空（含 published 的 `order_paid_rate`），FK 保留。  
SQL：`docs/metric_library_phase1_clear_derived_ratio_agg_20260829.sql`

**回执**：reply #7604。另提了库里还有 avg×2、count_distinct×4 同属禁项，等他点头再清。
```
