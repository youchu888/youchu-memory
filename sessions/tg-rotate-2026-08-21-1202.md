# TG 会话轮换归档 · 2026-08-21-1202

- 原因: max_transcript(440KB>=400KB)
- session_id: ses_1787241684325_17e6r1
- cursor_chat_id: 8cc0b0ff-4597-4af4-b1c1-6f405bc7763f
- 蒸馏: AI+heuristic

## 携带要点

- 页面访问「进入」改口径前必须先对齐现网**：旧 `entry_cnt` = 来路空/`unknown`（外部直达）；新口径 = 本页且 `referrer_page_key` 非空且 ≠ `unknown`（站内跳入），二者语义相反
- `wf_ads_日报表_日` DAG 顺序坑**：`ads_product_day_stat_d` 跑在 `dwm_user_video_d` 前面，当日 `pt` LEFT JOIN 上游为空 → 全 app `video_play_cnt` NULL；次日回写 `pt-1` 才会补上
- **页面访问「进入」改口径前必须先对齐现网**：旧 `entry_cnt` = 来路空/`unknown`（外部直达）；新口径 = 本页且 `referrer_page_key` 非空且 ≠ `unknown`（站内跳入），二者语义相反
- **口语「跳出」≠ 表字段 `dropout_*`**：用户说的离开本页去别页 ≈ 现网 `jump_cnt`（来路=本页、去向≠本页、均非空非 `unknown`）；`dropout_page_cnt` 是停留层末页/超 1800s，别混用
- **改口径标准链路**：改 ETL/SQL → test 发布 → 补约一个月 → 选代表性 app 做 DWS↔DWD 重算对账（进入/跳转 diff=0）→ 再 request-publish 生产
- **补数秒失败先查分区**：静态分区表缺目标分区（如 `p20260721`）会直接失败，补分区后再重跑
- **跨 app 对账来源/去向**：TJ-027 与 JHA-124 的 `page_key` 体系不同，只能看路径结构是否合理，不能按同名页硬对齐
- **自刷新（`referrer_page_key = page_key`）会显著抬高进入**：TJ-027 进入里约半数是自刷新；用户未确认前按字面口径跑，不擅自加 `referrer ≠ 本页`
- **日批 T-1**：问「今天有没有 21 号数据」→ 业务日最新一般是昨天；当天分区要等次日凌晨调度
- **页面访问日表**：`dws.dws_app_page_visit_d_d`，粒度 天 × app × `page_key`
- **又初不能直发 prod**：test 验过后走 `request-publish`，上次 prod 审核发布人是**野花**；发版前 prod 仍跑旧 SQL，历史对齐需另补数
- **`video_play_cnt` 当日 NULL 不一定是任务失败**：任务 SUCCESS 但字段全 NULL 时，先查同工作流上游依赖顺序，再查上游分区是否已有数
- **`wf_ads_日报表_日` DAG 顺序坑**：`ads_product_day_stat_d` 跑在 `dwm_user_video_d` 前面，当日 `pt` LEFT JOIN 上游为空 → 全 app `video_play_cnt` NULL；次日回写 `pt-1` 才会补上
- **依赖顺序类问题处理**：上游已齐后对下游 task 做 COMPLEMENT TASK_ONLY 补跑即可；结构性问题需同步调度/DAG 调整（已通过 agent-bus 同步狂人）

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-21-改进入-跳转口径前先对照现网-entry_cnt-与-站内跳入-语义相反-用户-跳出-对应-ju.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-21-补数秒失败先查目标静态分区是否存在-缺分区先-add-partition-再重跑.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-21-同工作流下游-success-但字段全-null-时优先查任务依赖顺序与上游分区就绪时间-别误判.md`

## AI 蒸馏原文（截断）

```
- **页面访问「进入」改口径前必须先对齐现网**：旧 `entry_cnt` = 来路空/`unknown`（外部直达）；新口径 = 本页且 `referrer_page_key` 非空且 ≠ `unknown`（站内跳入），二者语义相反
- **口语「跳出」≠ 表字段 `dropout_*`**：用户说的离开本页去别页 ≈ 现网 `jump_cnt`（来路=本页、去向≠本页、均非空非 `unknown`）；`dropout_page_cnt` 是停留层末页/超 1800s，别混用
- **改口径标准链路**：改 ETL/SQL → test 发布 → 补约一个月 → 选代表性 app 做 DWS↔DWD 重算对账（进入/跳转 diff=0）→ 再 request-publish 生产
- **补数秒失败先查分区**：静态分区表缺目标分区（如 `p20260721`）会直接失败，补分区后再重跑
- **跨 app 对账来源/去向**：TJ-027 与 JHA-124 的 `page_key` 体系不同，只能看路径结构是否合理，不能按同名页硬对齐
- **自刷新（`referrer_page_key = page_key`）会显著抬高进入**：TJ-027 进入里约半数是自刷新；用户未确认前按字面口径跑，不擅自加 `referrer ≠ 本页`
- **日批 T-1**：问「今天有没有 21 号数据」→ 业务日最新一般是昨天；当天分区要等次日凌晨调度
- **页面访问日表**：`dws.dws_app_page_visit_d_d`，粒度 天 × app × `page_key`
- **又初不能直发 prod**：test 验过后走 `request-publish`，上次 prod 审核发布人是**野花**；发版前 prod 仍跑旧 SQL，历史对齐需另补数
- **`video_play_cnt` 当日 NULL 不一定是任务失败**：任务 SUCCESS 但字段全 NULL 时，先查同工作流上游依赖顺序，再查上游分区是否已有数
- **`wf_ads_日报表_日` DAG 顺序坑**：`ads_product_day_stat_d` 跑在 `dwm_user_video_d` 前面，当日 `pt` LEFT JOIN 上游为空 → 全 app `video_play_cnt` NULL；次日回写 `pt-1` 才会补上
- **依赖顺序类问题处理**：上游已齐后对下游 task 做 COMPLEMENT TASK_ONLY 补跑即可；结构性问题需同步调度/DAG 调整（已通过 agent-bus 同步狂人）

[LESSON: page-visit,metric|改进入/跳转口径前先对照现网：`entry_cnt` 与「站内跳入」语义相反，用户「跳出」对应 `jump_cnt` 不是 `dropout_*`]
[LESSON: complement,partition|补数秒失败先查目标静态分区是否存在，缺分区先 ADD PARTITION 再重跑]
[LESSON: datacheck,dolphin,dag|同工作流下游 SUCCESS 但字段全 NULL 时优先查任务依赖顺序与上游分区就绪时间，别误判为 SQL 逻辑错误]
```

## 会话摘录（截断）

```
[user] [私聊#374] 页面访问的指标还是有点问题，按照下面这样你能理解吗？和现在的逻辑是不是不一样？
进入，就是pagekey或者currentpagekey = ‘xxx’ and referrer_page_key不等于空和不等‘unknown’

跳出：就是把xxx作为来路，referrer_page_key= ‘xxx’ 且pagekey/currentpagekey不等于‘xxx’ 且 pagekey/currentpagekey 不为空且不等于‘unknown’

[assistant] 先核对页面访问现有口径与你描述的进入/跳出定义是否一致。理解，按你这两句对过现网口径了。

## 你说的口径

- **进入（看本页 xxx）**：到了 xxx（`page_key`/`current_page_key`），且来路**有明确页**（`referrer_page_key` 非空且 ≠ `unknown`）→ 本质是「从站内别页跳进 xxx」
- **跳出（你口中的）**：来路是 xxx，去向不是 xxx，且去向非空、≠ `unknown` → 本质是「从 xxx 离开去别页」

## 和现在逻辑比：不一样

| | 你现在这版 | 现网 `dws_app_page_visit_d_d` |
|--|--|--|
| **进入 `entry_cnt`** | 来路**有**明确页 | 来路**空或 `unknown`**（外部直接进本页） |
| **离开本页** | 你叫「跳出」 | 现网叫 **`jump_cnt` 跳转**，条件基本同你说的离开（来路=本页、去向≠本页、来路非 unknown） |
| **现网「跳出」`dropout_page_cnt`** | — | 另一套：停留层 `is_dropout=1`（末页/超 1800s），**不是**跳转次数 |

结论：

1. **进入：和现网相反。** 你要「有明确来路才算进入」；现网是「无明确来路才算进入」。
2. **你说的「跳出」≈ 现网「跳转」`jump_cnt`，大体一致**；现网另有真正叫「跳出」的 `dropout_*`，别混。
3. 若按你这版改 `entry_cnt`，站内跳入会进「进入」，外部直达反而进不了「进入」——和指标库/文档里「外部直接进入」定案冲突，要改得先拍板。

你确认一下：是要把「进入」改成「有明确来路才算」，还是你其实想要的是「站内跳入次数」另开一列、现网外部进入保留？

[user] [私聊#375] 按我最新的规则来改吧，然后发布测试，补一个月的数据，比较一下tj-027和JHA-124进入次数、跳转次数和来源，去向数据是否一致

[assistant] 按你最新口径改进入/跳转，发 test、补约一个月，再对 tj-027 与 JHA-124 对数。先摸清现网 SQL、session 和来源/去向数据落在哪。正在改 SQL 与口径文档，随后发 test 并补近一个月。首个补数实例秒失败，先查日志根因。根因是静态分区 `p20260721` 不存在。先看表分区定义，再改写入方式或补分区。缺的是 `p20260721`。补上该分区后从 7/21 起重跑补数。补数 31 天已齐，开始对两 app 做进入/跳转与来源去向对账。已按你最新口径改完、发 test、补满一个月，并对过两 app。

## 已做
- ETL：进入 = 本页且来路非空非 `unknown`；跳转 = 来路=本页且去向非空非 `unknown` 且≠本页  
- test 发布：task `22599391396992` **v195→v196**  
- 补数：业务日 **2026-07-21～08-20**（先补了缺分区 `p20260721`），31 天 TASK_ONLY 全 SUCCESS  

## 对账（DWS ↔ DWD，diff 全 0）

| app | pv | 进入 | 跳转 |
|-----|-----|------|------|
| TJ-027 | 44,375,033 | 40,136,870 | 20,199,986 |
| JHA-124 | 39,841 | 37,499 | 35,850 |

两 app 的进入/跳转与源表重算**完全一致**，口径落地没问题。

## 来源→去向（跳转边 Top）
- **TJ-027**：`search↔archives`、`tag↔archives`、`wpcz↔archives`（围绕 archives）  
- **JHA-124**：`/read_ks_home↔/read_ks_video`、`video↔video_t`、splash→home  

两边路径结构都合理，但**不是同一套 page_key 体系**，不能按同名页对齐。

## 需你拍一下的一点
TJ-027 进入里约 **半数是自刷新**（`referrer = page_key`，约 1994 万），所以进入远大于跳转；JHA-124 自刷新很少，进入≈跳转。  
你原文没排除自刷新——现在是按字面实现的。若进入不应含刷新，我再加 `referrer ≠ 本页` 后重跑。

[user] [私聊#376] 自刷新的先不要管，确认了再说

[assistant] 好，自刷新先不动，等你确认后再改。当前口径就按现在线上这版跑。

[user] [私聊#377] 测试发版了 也补数了对吧？我要让产品确认了

[assistant] 对，test 已发版、也补完数了，可以拿去给产品确认。

· test 海豚 task `22599391396992` 已发到 **v196**（进入/跳转新口径）  
· 补数业务日 **2026-07-21～08-20**（约一个月），31 天全 SUCCESS  
· TJ-027 / JHA-124：DWS 与源表重算的进入、跳转 **diff=0**  

自刷新仍按你说的先不动，等产品拍板再说。

[user] [私聊#378] 页面访问有21号数据吗

[assistant] 没有。test / prod 目前最新都是 **2026-08-20**，没有 21 号分区数据。

日批按 T-1，21 号业务日要等明天凌晨跑完才会有。

[user] [私聊#379] dws_app_page_visit_d_d
这个是页面访问的表吧

[assistant] 对，就是页面访问日指标表：`dws.dws_app_page_visit_d_d`，粒度是天 × app × page_key，进进入/跳转、访问次数等。

[user] [私聊#380] 页面访问没问题的话 发布生产吧。之前是谁发布的

[assistant] 先查此前是谁发的、当前 session/提审状态，再走生产发布流程。上次审核人是野花。我这边不能直发 prod；先把新口径入库并重新提审给野花。确认：上次 prod 由野花审核并发布（2026-08-06）。正在提交新口径并重新提审。**之前是谁发的：** **野花**（`kakakh7787@gmail.com`）。2026-08-06 她审过并完成 prod 首发（publish-run #526/#527，task `180825664895104`）。

**当前状态：** 又初不能直发 prod。新口径已：
- test 验过（v196，对账 diff=0）
- 代码已 push（`845ded87`）
- **已重新 request-publish → 野花**（pending）

prod 上任务还在跑**旧口径**（进入=空/`unknown`）。野花发完新 SQL 后，如需对齐历史数再补。需要我催她或发完后帮补数，说一声即可。

[user] [私聊#381] select * from ads.ads_product_day_stat_d where dt ='2026-08-20' and app_id ='SF-68'   帮忙看下
video_play_cnt
 这个字段有没有值，看看是不是跑的任务有问题

[assistant] 理解为：`datacheck` · `prod` · `ads.ads_product_day_stat_d` · `dt=2026-08-20` · `SF-68` → 查 `video_play_cnt` 是否有值并判断任务是否异常。`video_play_cnt` 在 08-20 为 NULL，前几日有值。继续查上游与任务实例。**结论：`video_play_cnt` 没值（是 `NULL`），不是任务失败，是依赖顺序问题。**

| 项 | 结果 |
|---|---|
| SF-68 · 2026-08-20 | `video_play_cnt = NULL`（同行 `dau_count=42561`、`ad_click_cnt=30943` 正常） |
| 同 app 前几日 | 08-15～08-19 都有值（约 35万～47万） |
| 当日全表 | **768/768 app 的 `video_play_cnt` 全是 NULL** |
| 上游 `dwm.dwm_user_video_d` | SF-68 当日已有，`SUM(video_cnt)=399174` |
| 任务状态 | `ads_product_day_stat_d` 当日 SUCCESS |

**原因：** 现网 SQL 从 `dwm.dwm_user_video_d` 取播放，但同工作流里 `ads_product_day_stat_d`（07:31:39）跑在 `dwm_user_video_d`（07:32:29）**前面**，当日分区还没写完就 LEFT JOIN，结果落成 NULL。前一日能有值，是因为次日跑批会回写 `pt-1`。

**处理：** 现在上游已齐，对 `ads_product_day_stat_d` 用 `pt=2026-08-20` 补跑一次即可把 SF-68 补到约 **399174**；不补的话等明天日调度回写 `pt-1` 也会好。需要我补跑的话说一声。

[user] [私聊#382] 补跑，这个是哪个任务流？prod是不是也有同样的问题

[assistant] 补跑 prod 的 `ads_product_day_stat_d`；工作流是 `wf_ads_日报表_日`，刚才查的就是 prod，同样有依赖顺序问题。**已补跑完成（prod）。**

| 项 | 内容 |
|---|---|
| 工作流 | **`wf_ads_日报表_日`**（`174729508462400`） |
| 任务 | **`ads_product_day_stat_d`**（`174729507871563`） |
| 补跑 | PI **32240** COMPLEMENT TASK_ONLY · SUCCESS |
| SF-68 · 08-20 | `video_play_cnt`：**399174**（与上游 DWM 一致） |

**关于「prod 是不是也有同样问题」：**  
是的——刚才查的、补的都是 **prod**。根因也在 prod：同工作流里 `ads_product_day_stat_d` 排在 `dwm_user_video_d` **前面**，日调度当天 `pt` 的播放会先落成 NULL；次日回写 `pt-1` 才补上。这次是手动把 08-20 提前修好了。

[user] [私聊#383] 把这个问题同步给狂人

[assistant] 把依赖顺序导致 `video_play_cnt` 当日为空的问题，通过 agent-bus 同步给狂人。已同步狂人：agent-bus **#6963**（现象 / 根因 / 补跑结果 / DAG 调整建议）。

[user] <timestamp>Friday, Aug 21, 2026, 8:12 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-21 08:11（周五），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-21 00:00:53 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/data…

[assistant] 先核对页面访问现有口径与你描述的进入/跳出定义是否一致。

[assistant] 理解，按你这两句对过现网口径了。

## 你说的口径

- **进入（看本页 xxx）**：到了 xxx（`page_key`/`current_page_key`），且来路**有明确页**（`referrer_page_key` 非空且 ≠ `unknown`）→ 本质是「从站内别页跳进 xxx」
- **跳出（你口中的）**：来路是 xxx，去向不是 xxx，且去向非空、≠ `unknown` → 本质是「从 xxx 离开去别页」

## 和现在逻辑比：不一样

| | 你现在这版 | 现网 `dws_app_page_visit_d_d` |
|--|--|--|
| **进入 `entry_cnt`** | 来路**有**明确页 | 来路**空或 `unknown`**（外部直接进本页） |
| **离开本页** | 你叫「跳出」 | 现网叫 **`jump_cnt` 跳转**，条件基本同你说的离开（来路=本页、去向≠本页、来路非 unknown） |
| **现网「跳出」`dropout_page_cnt`** | — | 另一套：停留层 `is_dropout=1`（末页/超 1800s），**不是**跳转次数 |

结论：

1. **进入：和现网相反。** 你要「有明确来路才算进入」；现网是「无明确来路才算进入」。
2. **你说的「跳出」≈ 现网「跳转」`jump_cnt`，大体一致**；现网另有真正叫「跳出」的 `dropout_*`，别混。
3. 若按你这版改 `entry_cnt`，站内跳入会进「进入」，外部直达反而进不了「进入」——和指标库/文档里「外部直接进入」定案冲突，要改得先拍板。

你确认一下：是要把「进入」改成「有明确来路才算」，还是你其实想要的是「站内跳入次数」另开一列、现网外部进入保留？

[user] <timestamp>Friday, Aug 21, 2026, 8:17 AM (UTC+8)</timestamp>
<user_query>
[私聊#375] 按我最新的规则来改吧，然后发布测试，补一个月的数据，比较一下tj-027和JHA-124进入次数、跳转次数和来源，去向数据是否一致
</user_query>

[assistant] 按你最新口径改进入/跳转，发 test、补约一个月，再对 tj-027 与 JHA-124 对数。先摸清现网 SQL、session 和来源/去向数据落在哪。

[assistant] 正在改 SQL 与口径文档，随后发 test 并补近一个月。

[assistant] 首个补数实例秒失败，先查日志根因。

[assistant] 根因是静态分区 `p20260721` 不存在。先看表分区定义，再改写入方式或补分区。

[assistant] 缺的是 `p20260721`。补上该分区后从 7/21 起重跑补数。

[assistant] 补数 31 天已齐，开始对两 app 做进入/跳转与来源去向对账。

[assistant] 已按你最新口径改完、发 test、补满一个月，并对过两 app。

## 已做
- ETL：进入 = 本页且来路非空非 `unknown`；跳转 = 来路=本页且去向非空非 `unknown` 且≠本页  
- test 发布：task `22599391396992` **v195→v196**  
- 补数：业务日 **2026-07-21～08-20**（先补了缺分区 `p20260721`），31 天 TASK_ONLY 全 SUCCESS  

## 对账（DWS ↔ DWD，diff 全 0）

| app | pv | 进入 | 跳转 |
|-----|-----|------|------|
| TJ-027 | 44,375,033 | 40,136,870 | 20,199,986 |
| JHA-124 | 39,841 | 37,499 | 35,850 |

两 app 的进入/跳转与源表重算**完全一致**，口径落地没问题。

## 来源→去向（跳转边 Top）
- **TJ-027**：`search↔archives`、`tag↔archives`、`wpcz↔archives`（围绕 archives）  
- **JHA-124**：`/read_ks_home↔/read_ks_video`、`video↔video_t`、splash→home  

两边路径结构都合理，但**不是同一套 page_key 体系**，不能按同名页对齐。

## 需你拍一下的一点
TJ-027 进入里约 **半数是自刷新**（`referrer = page_key`，约 1994 万），所以进入远大于跳转；JHA-124 自刷新很少，进入≈跳转。  
你原文没排除自刷新——现在是按字面实现的。若进入不应含刷新，我再加 `referrer ≠ 本页` 后…

[user] <timestamp>Friday, Aug 21, 20
```
