# TG 会话轮换归档 · 2026-08-20-1912

- 原因: max_transcript(409KB>=400KB)
- session_id: ses_1787179159594_iae4g9
- cursor_chat_id: 06b05e20-25c5-4f02-9f81-42068919a51f
- 蒸馏: AI+heuristic

## 携带要点

- 指标库定位是「口径登记 + 物理绑定 + 发布门禁」，不是第四套 BI；ETL 仍在 StarRocks，旁路建新表，不破坏现网 `metric_standard`。
- 发布硬规矩：AI 只能 `propose`，`published` 必须人审；晋升要齐定义、绑定、合规聚合、`req_ref`；比率只存分子/分母；UV 聚合写死 `bitmap_union_count`。
- 指标边界：统计取值条件不同、结果不同 → 必须拆成不同指标，不能同名硬塞；别名可挂多表多列，但口径必须一致。
- 标签链只走 `wrapper_daily_v3.sh`，禁手工逐步跑 base；否则水位不提交会导致翻倍。
- prod 分区是否上线必须问知秋拍板，又初不替主人决策。
- [LESSON: spark|标签链 prod 只跑 wrapper_daily_v3.sh，禁手工逐步跑 base，否则水位不提交会翻倍]
- [LESSON: metric-library|取值条件不同导致结果不同即拆新指标，禁止同名不同义硬合并]
- prod 已确认存在 `dwd.dwd_app_page_view_d`、`ads.ads_product_day_stat_d`；查表存在性优先走 `sr_prod` 的 `information_schema`，别凭记忆答。
- 三层模型：`metric_concept`（业务口径/窗口/聚合，改口径升版）→ `metric_label`（多名字指向同一概念）→ `metric_implementation`（表.列 + formula + 是否主实现；天表/小时表是存储档位，不拆概念）。
- 知秋定调：指标库核心是数据模型，不是人工维护指标清单；日常新增不靠界面手填，走「需求进开发平台 → 程序+AI 对库复用或新建」。
- 人的角色是录需求、审发布；不应承担「指标大姨妈式」手工维稳——口径会变，不能指望人长期手工对齐。
- prod Spark 可用：`hadoop-1` · `/opt/bigdata/spark` · `--master yarn --deploy-mode client`；2026-08-20 03:57 已跑通 dt=2026-08-15 全链（天链→小时框架→结算）。
- 标签链错峰约 03:10；`dim_user_all_d` 下游次日 01:03 才跑，不必赶早。
- TG 在线绿点由 `should_appear_online` 控制：休息窗 13:00–15:00、19:00–20:00 可不绿，其余上班时段保持绿。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-20-标签链-prod-只跑-wrapper_daily_v3-sh-禁手工逐步跑-base-否则水位.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-20-取值条件不同导致结果不同即拆新指标-禁止同名不同义硬合并.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-20-休息窗-13-00-15-00-与-19-00-20-00-在-should_appear_on.md`

## AI 蒸馏原文（截断）

```
- prod 已确认存在 `dwd.dwd_app_page_view_d`、`ads.ads_product_day_stat_d`；查表存在性优先走 `sr_prod` 的 `information_schema`，别凭记忆答。
- 指标库定位是「口径登记 + 物理绑定 + 发布门禁」，不是第四套 BI；ETL 仍在 StarRocks，旁路建新表，不破坏现网 `metric_standard`。
- 三层模型：`metric_concept`（业务口径/窗口/聚合，改口径升版）→ `metric_label`（多名字指向同一概念）→ `metric_implementation`（表.列 + formula + 是否主实现；天表/小时表是存储档位，不拆概念）。
- 发布硬规矩：AI 只能 `propose`，`published` 必须人审；晋升要齐定义、绑定、合规聚合、`req_ref`；比率只存分子/分母；UV 聚合写死 `bitmap_union_count`。
- 知秋定调：指标库核心是数据模型，不是人工维护指标清单；日常新增不靠界面手填，走「需求进开发平台 → 程序+AI 对库复用或新建」。
- 指标边界：统计取值条件不同、结果不同 → 必须拆成不同指标，不能同名硬塞；别名可挂多表多列，但口径必须一致。
- 人的角色是录需求、审发布；不应承担「指标大姨妈式」手工维稳——口径会变，不能指望人长期手工对齐。
- prod Spark 可用：`hadoop-1` · `/opt/bigdata/spark` · `--master yarn --deploy-mode client`；2026-08-20 03:57 已跑通 dt=2026-08-15 全链（天链→小时框架→结算）。
- 标签链只走 `wrapper_daily_v3.sh`，禁手工逐步跑 base；否则水位不提交会导致翻倍。
- 标签链错峰约 03:10；`dim_user_all_d` 下游次日 01:03 才跑，不必赶早。
- prod 分区是否上线必须问知秋拍板，又初不替主人决策。
- TG 在线绿点由 `should_appear_online` 控制：休息窗 13:00–15:00、19:00–20:00 可不绿，其余上班时段保持绿。

[LESSON: spark|标签链 prod 只跑 wrapper_daily_v3.sh，禁手工逐步跑 base，否则水位不提交会翻倍]
[LESSON: metric-library|取值条件不同导致结果不同即拆新指标，禁止同名不同义硬合并]
[LESSON: tg-presence|休息窗 13:00–15:00 与 19:00–20:00 在 should_appear_online 放行变灰，其余时段保持在线绿点]
```

## 会话摘录（截断）

```
[user] [私聊#358] dwd_app_page_view_d
ads_product_day_stat_d
这两张表prod有的吧？

[assistant] 先核对元数据与 prod 是否真有这两张表。有，prod 都在：

- `dwd.dwd_app_page_view_d`
- `ads.ads_product_day_stat_d`

刚在 `sr_prod` 用 information_schema 核过，两张都存在。

[user] [私聊#359] 简单说下指标库的实现思路

[assistant] 先对照指标库现有设计稿，再用白话概括实现思路。一句话：指标库不管怎么出数，只管「口径登记 + 挂到哪张表哪列 + 能不能发布」。

实现思路分三层：

1. 概念层（metric_concept）  
   记业务指标本身：叫什么、口径、统计窗口（日/周/月）、聚合方式。  
   不是字段名，是「实际指标」。改口径就升版，不原地偷改。

2. 名字层（metric_label）  
   一个指标多个名字：英文码、中文、口语、扫表遗留名。  
   对外查数走名字，对内落到同一个 concept。

3. 实现层（metric_implementation）  
   挂物理表.列 + formula + 是否主实现。  
   同一指标可以有多处实现（不同 ADS/DWS）；天表/小时表是存储档位，不拆成两个概念。

门禁几条硬规矩：
- AI 只能 propose，published 必须人审  
- published 要齐：定义、绑定、合规聚合、需求出处 req_ref  
- 比率不落库，只存分子/分母，查询时现算  
- UV 类聚合写死 bitmap_union_count  

落地路径：先旁路建新表（不破坏现网 metric_standard），血缘回填 formula → 拆「同名不同义」→ 人审晋升 published。不做第四套 BI，ETL 仍在 StarRocks。

[user] [私聊#360] 简单说下指标库的实现思路，说人话

[assistant] 说白了就三件事：

1. 先把「业务上到底在说哪个指标」钉死  
   比如「日活」是什么意思、算哪一天、怎么去重。这是概念，不是某张表上的某个字段名。

2. 名字可以很多，指向同一个东西  
   中文叫日活、英文叫 DAU、口语乱叫都行，都挂到同一个指标上，别一人一套说法。

3. 再告诉系统这个数从哪算出来  
   挂到具体表、具体列、怎么写公式。同一指标可以有多处实现，但口径要统一；口径真不一样就拆成两个指标，别硬塞一个名。

发布要人审，不能 AI 随手写进正式库。比率（比如转化率）不存结果，只存分子分母，用的时候再除。改口径就升版，不许偷偷改旧定义。

一句话：指标库是口径说明书 + 挂表目录，不是又一套报表系统。

[user] [私聊#361] 读一下大数据开发群下午的消息，整理一下我和知秋讨论的关于指标库的内容

[assistant] 正在拉取今天下午大数据开发群里你和知秋关于指标库的讨论。今日群归档还没有，正在读热上下文并拉取下午全量消息。找到「大数据开发」群，正在拉取下午消息。已从「大数据开发」群（非机器人群）拉了今天下午原文，整理如下——只留你和知秋聊指标库的部分。

---

## 背景（16:47 起）

你说大漏斗要等结算、自己没开发了，又提「指标库也卡住了」。  
知秋问卡在哪，让你展开。你说其实不是卡死，不等他、自己先搞，有问题再改。  
知秋：需要讨论就说；有没有文档。你回早就发给狂人了。知秋说狂人好像看到了，在写程序，回头他也看。接着让你「简单说说想怎么搞」。

---

## 你提的三步（17:15）

1. 先确认各指标含义，定标准  
2. 多个名字指向同一标准  
3. 挂到具体表和列  

---

## 知秋定调（17:57～18:11）

核心不是人工维护一堆指标，而是把数据模型想清楚。

**怎么用（产品形态）**  
- 新需求在开发平台整理指标时去查指标库：有则复用，没有再新增  
- 开发仍是「录入需求」，不是打开界面手工填一大堆指标字段  
- 人工维护的往往都是「新指标」；应按需求内容自动判断是否新指标  
- 判断方式：程序 + AI（开发平台来干）

**一个指标长什么样**  
- 标准编号、英文标识、说明  
- 以及实际用到的字段（名所在表）；现在可能多处，和标准名不同的算别名  

**最关键的建模问题**  
- 先定：什么叫「一个指标」、应有哪些属性  
- 属性不同 → 就是新指标  
- 例：统计取值条件不同，算不算新指标？

---

## 你俩对齐的共识点

| 点 | 结论 |
|----|------|
| 取值条件不同 | 结果不同 → 不能当同一个指标（你明确说的；知秋用反问在推这个边界） |
| 谁来建指标 | 不应靠人在界面里新建；从需求进平台，自动对库、复用或新增 |
| 人的角色 | 录需求；不维护「指标大姨妈式」手工录入 |

知秋还补了一句：同一个人也会「今天这样想、明天那样想」，所以更不能指望人手工维稳。

---

## 一句话

指标库要做成：**需求进开发平台 → 自动/半自动对库（复用或新建）**；模型上先钉死「什么是一个指标、有哪些属性」；别名挂多表多列；人工不负责日常新增维护。

（18:13 之后群里转到结算/channel/dim_user，和指标库讨论无关，未并入。）那次后台探路径（找 Telethon / venv）失败了，不影响你要的结果：指标库讨论整理已按「大数据开发」群下午原文交过。若还要落成文档或对照现有 `docs/metric_library_*` 改一版，直接说一声即可。

[user] [私聊#362] 中午13.00到15.00，晚上19.00到20.00 是休息时间，tg可以不绿 其余时间要绿

[user] <timestamp>Thursday, Aug 20, 2026, 12:54 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-20 12:53（周四），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-20 06:38:57 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/d…

[assistant] 先核对元数据与 prod 是否真有这两张表。

[assistant] 有，prod 都在：

- `dwd.dwd_app_page_view_d`
- `ads.ads_product_day_stat_d`

刚在 `sr_prod` 用 information_schema 核过，两张都存在。

[user] <timestamp>Thursday, Aug 20, 2026, 4:53 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#6901 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 6901 --text "[ACK] bus#6901 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 6901 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：$(cat <<'TEXTEOF' [狂人→又初] Spark 可以跑。刚实查(2026-08-20 16:5x 北京):  1) 可用范围: prod 集群 hadoop-1。spark 在 /opt/bigdata/spark，提交走 --master yarn --deploy-mode client。今天 03:57 刚跑完一轮 dt=2026-08-15 的完整全链(天链→小时框架→结算)，链路是通的。test 侧我没查，你那边要 test 请单独说。  2) 集群余量: YARN default 队列当前占用 37.53%，余量六成多，节点全 RUNNING。同时跑着 7 个 Flink 常驻作业…

[assistant] 先 ACK bus#6901，再读完整正文并处理。

[assistant] ACK 回了 `dropped: silent`，先核对是否真发到，再沉淀要点并 reply 结案。

[assistant] 正在沉淀狂人交底，并 reply 结案。

[assistant] bus#6901 已 ACK + reply 结案（出站 #6902）。

狂人交底要点已锁：
1. **prod Spark**：`hadoop-1` · `/opt/bigdata/spark` · `--master yarn --deploy-mode client`；今天 03:57 已跑通 dt=2026-08-15 全链  
2. **标签链**：只走 `wrapper_daily_v3.sh`，禁手工逐步跑 base（水位不提交会翻倍）  
3. **错峰 03:10**；`dim_user_all_d` 下游次日 01:03，不用赶  
4. **prod 分区上不上** → 问知秋，不替拍板  

已沉淀：`lessons/2026-08-20-spark-prod-hadoop1-可用-标签链须走-wrapper_daily_v3-禁手工逐步跑.md`

[user] <timestamp>Thursday, Aug 20, 2026, 5:00 PM (UTC+8)</timestamp>
<user_query>
[私聊#359] 简单说下指标库的实现思路
</user_query>

[assistant] 先对照指标库现有设计稿，再用白话概括实现思路。

[assistant] 一句话：指标库不管怎么出数，只管「口径登记 + 挂到哪张表哪列 + 能不能发布」。

实现思路分三层：

1. 概念层（metric_concept）  
   记业务指标本身：叫什么、口径、统计窗口（日/周/月）、聚合方式。  
   不是字段名，是「实际指标」。改口径就升版，不原地偷改。

2. 名字层（metric_label）  
   一个指标多个名字：英文码、中文、口语、扫表遗留名。  
   对外查数走名字，对内落到同一个 concept。

3. 实现层（metric_implementation）  
   挂物理表.列 + formula + 是否主实现。  
   同一指标可以有多处实现（不同 ADS/DWS）；天表/小时表是存储档位，不拆成两个概念。

门禁几条硬规矩：
- AI 只能 propose，published 必须人审  
- published 要齐：定义、绑定、合规聚合、需求出处 req_ref  
- 比率不落库，只存分子/分母，查询时现算  
- UV 类聚合写死 bitmap_union_count  

落地路径：先旁路建新表（不破坏现网 metric_standard），血缘回填 formula → 拆「同名不同义」→ 人审晋升 published。不做第四套 BI，ETL 仍在 StarRocks。

[user] <timestamp>Thursday, Aug 20, 2026, 5:05 PM (UTC+8)</timestamp>
<user_query>
[私聊#360] 简单说下指标库的实现思路，说人话
</user_query>

[assistant] 说白了就三件事：

1. 先把「业务上到底在说哪个指标」钉死  
   比如「日活」是什么意思、算哪一天、怎么去重。这是概念，不是某张表上的某个字段名。

2. 名字可以很多，指向同一个东西  
   中文叫日活、英文叫 DAU、口语乱叫都行，都挂到同一个指标上，别一人一套说法。

3. 再告诉系统这个数从哪算出来  
   挂到具体表、具体列、怎么写公式。同一指标可以有多处实现，但口径要统一；口径真不一样就拆成两个指标，别硬塞一个名。

发布要人审，不能 AI 随手写进正式库。比率（比如转化率）不存结果，只存分子分母，用的时候再除。改口径就升版，不许偷偷改旧定义。

一句话：指标库是口径说明书 + 挂表目录，不是又一套报表系统。

[user] <timestamp>Thursday, Aug 20, 2026, 6:44 PM (UTC+8)</timestamp>
<user_query>
[私聊#361] 读一下大数据开发群下午的消息，整理一下我和知秋讨论的关于指标库的内容
</user_query>

[assistant] 正在拉取今天下午大数据开发群里你和知秋关于指标库的讨论。

[assistant] 今日群归档还没有，正在读热上下文并拉取下午全量消息。

[assistant] 找到「大数据开发」群，正在拉取下午消息。

[user] <mcp_meta_tools>
You have access to MCP (Model Context Protocol) tools through `GetMcpTools` and `CallMcpTool`.

## MCP Tool Discovery and Invocation

Use `GetMcpTools` to discover tool schemas, then `CallMcpTool` to invoke them. Aim to minimize round-trips: ideally one `GetMcpTools` call followed by one `CallMcpTool` call.

If the user mentions, references, or links to a product or service that corresponds to an available MCP server, and the request likely depends on information from that service, proactively inspect that MCP server before answering. Do not wait for the user to explicitly ask you to use MCP. If you are unsure which server matches, use `GetMcpTools` with a pattern based on the service name.

`GetMcpTools` supports four modes:

1. `{"server":"<id>"}`: returns full input sch…

[user] <timestamp>Thursday, Aug 20, 2026, 6:44 PM (UTC+8)</timestamp>
<user_query>
[私聊#361] 读一下大数据开发群下午的消息，整理一下我和知秋讨论的关于指标库的内容
</user_query>

[assistant] 已从「大数据开发」群（非机器人群）拉了今天下午原文，整理如下——只留你和知秋聊指标库的部分。

---

## 背景（16:47 起）

你说大漏斗要等结算、自己没开发了，又提「指标库也卡住了」。  
知秋问卡在哪，让你展开。你说其实不是卡死，不等他、自己先搞，有问题再改。  
知秋：需要讨论就说；有没有文档。你回早就发给狂人了。知秋说狂人好像看到了，在写程序，回头他也看。接着让你「简单说说想怎么搞」。

---

## 你提的三步（17:15）

1. 先确认各指标含义，定标准  
2. 多个名字指向同一标准  
3. 挂到具体表和列  

---

## 知秋定调（17:57～18:11）

核心不是人工维护一堆指标，而是把数据模型想清楚。

**怎么用（产品形态）**  
- 新需求在开发平台整理指标时去查指标库：有则复用，没有再新增  
- 开发仍是「录入需求」，不是打开界面手工填一大堆指标字段  
- 人工维护的往往都是「新指标」；应按需求内容自动判断是否新指标  
- 判断方式：程序 + AI（开发平台来干）

**一个指标长什么样**  
- 标准编号、英文标识、说明  
- 以及实际用到的字段（名所在表）；现在可能多处，和标准名不同的算别名  

**最关键的建模问题**  
- 先定：什么叫「一个指标」、应有哪些属性  
- 属性不同 → 就是新指标  
- 例：统计取值条件不同，算不算新指标？

---

## 你俩对齐的共识点

| 点 | 结论 |
|----|------|
| 取值条件不同 | 结果不同 → 不能当同一个指标（你明确说的；知秋用反问在推这个边界） |
| 谁来
```
