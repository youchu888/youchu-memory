# TG 会话轮换归档 · 2026-08-08-2153

- 原因: daily(06:00)
- session_id: ses_1786113909915_1wkfty
- cursor_chat_id: 09c8df87-a9c9-4810-8fc4-d8195e03ac3d
- 蒸馏: AI+heuristic

## 携带要点

- stage37 OOM 根因是单条超宽 `COUNT(DISTINCT CASE...)` 同时在单个 stage 维护过多 distinct 集合，内存峰值过高。
- 重构 ETL 时必须同步三处文档：`spec.md`（口径）、`design.md`（数据流/实现）、`memory.md`（进展与待验项），避免代码与口径脱节。
- [LESSON: spec|口径|is_new] 临时口径与目标口径并存时，必须在 spec 写明当前实现来源与未接入的上游（如 `dim_user_daily_snapshot`），避免验数按错标准。
- 大漏斗任务工作目录固定为 `ops_system/04.dws/dws_app_event_funnel_d_d/`；续做前先读 `spec.md` / `design.md` / `memory.md` / `task` 定位卡点，再动 SQL。
- 抗 OOM 改法：把「一条大聚合」拆成「按事件独立聚合 + 以 keys 外连接拼宽表」；先保口径不变，再验可跑性。
- 当前 Spark 版 `is_new` 临时口径：以当日 `user_register` 事件推导，不用 `uid=-1`；目标来源是 `dim_user_daily_snapshot`，但现阶段不直连，需在 spec 里显式标注。
- 改完 SQL 后先做本地 diff 自检，确认改动范围只在大漏斗目录、无旁路误改，再进入集群冒烟。
- 下一轮验收建议跑第 4 轮 SF-81 冒烟（S 档），示例命令：`bash ops_system/04.dws/dws_app_event_funnel_d_d/spark/scripts/run_yarn_daily_sql.sh 2026-08-03 S app_2556`。
- 冒烟验收应覆盖：耗时、输出行数、关键指标抽样；可沉淀到 `playbook.md` 便于与知秋对齐复验。
- 日报格式偏好：主人要求「今日结果」不写第二条、「明日动作」只保留 TOP1；改定稿后需重新推 TG 私聊。
- 大漏斗链路推进状态：平台 session 已建，Paimon 建表、日批 ETL、设计说明与核查剧本已落库；当前重点从「能跑通」转向「降 OOM 风险后复验」。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-08-funnel-oom-etl.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-08-口径-is_new.md`

## AI 蒸馏原文（截断）

```
- 大漏斗任务工作目录固定为 `ops_system/04.dws/dws_app_event_funnel_d_d/`；续做前先读 `spec.md` / `design.md` / `memory.md` / `task` 定位卡点，再动 SQL。
- stage37 OOM 根因是单条超宽 `COUNT(DISTINCT CASE...)` 同时在单个 stage 维护过多 distinct 集合，内存峰值过高。
- 抗 OOM 改法：把「一条大聚合」拆成「按事件独立聚合 + 以 keys 外连接拼宽表」；先保口径不变，再验可跑性。
- 重构 ETL 时必须同步三处文档：`spec.md`（口径）、`design.md`（数据流/实现）、`memory.md`（进展与待验项），避免代码与口径脱节。
- 当前 Spark 版 `is_new` 临时口径：以当日 `user_register` 事件推导，不用 `uid=-1`；目标来源是 `dim_user_daily_snapshot`，但现阶段不直连，需在 spec 里显式标注。
- 改完 SQL 后先做本地 diff 自检，确认改动范围只在大漏斗目录、无旁路误改，再进入集群冒烟。
- 下一轮验收建议跑第 4 轮 SF-81 冒烟（S 档），示例命令：`bash ops_system/04.dws/dws_app_event_funnel_d_d/spark/scripts/run_yarn_daily_sql.sh 2026-08-03 S app_2556`。
- 冒烟验收应覆盖：耗时、输出行数、关键指标抽样；可沉淀到 `playbook.md` 便于与知秋对齐复验。
- 日报格式偏好：主人要求「今日结果」不写第二条、「明日动作」只保留 TOP1；改定稿后需重新推 TG 私聊。
- 大漏斗链路推进状态：平台 session 已建，Paimon 建表、日批 ETL、设计说明与核查剧本已落库；当前重点从「能跑通」转向「降 OOM 风险后复验」。

[LESSON: spark|funnel|oom|etl] 漏斗类宽表若单 stage 堆过多 `COUNT DISTINCT CASE`，优先按事件拆聚合再 join 拼宽，口径不变先验内存峰值。
[LESSON: spec|口径|is_new] 临时口径与目标口径并存时，必须在 spec 写明当前实现来源与未接入的上游（如 `dim_user_daily_snapshot`），避免验数按错标准。
[LESSON: dev-session|文档同步] ETL 为性能重构时，spec/design/memory 与 SQL 同批更新，否则 stage 验收和交叉核查会对不上实现。
```

## 会话摘录（截断）

```
[user] [私聊#282] 继续推进大漏斗的任务吧

[assistant] 理解为继续推进「大漏斗」这条开发任务，我先快速读取启动记忆并定位当前工作区里与大漏斗相关的未完成文件，然后直接接着改到可提交状态。我已经定位到你这条「大漏斗」对应的目录是 `ops_system/04.dws/dws_app_event_funnel_d_d/`，下一步先读 `spec/design/memory/task` 把当前卡点找出来，然后直接补齐代码。我准备把日批 SQL 做一次“按事件拆分聚合”的重构，目标是把当前容易 OOM 的超大 `COUNT DISTINCT CASE...` 聚合拆成多个小聚合再汇总，先保口径不变、优先提升可跑性。我已经把 SQL 重构和文档口径同步改完了，接下来先做一次本地 diff 自检，确认改动范围只在大漏斗目录且没有误改。已继续推进到可直接做下一轮冒烟的状态，核心是先把 OOM 风险降下来。

- 已重构 `ops_system/04.dws/dws_app_event_funnel_d_d/spark/sql/dws_app_event_funnel_d_d_daily.sql`  
  - 把原来单条超宽 `COUNT(DISTINCT CASE...)` 聚合，改成“按事件拆分聚合 + keys 外连接拼宽表”。  
  - 口径保持不变，目标是降低单个 stage 同时维护大量 distinct 集合导致的内存峰值（对应你这条大漏斗当前的 stage37 OOM 问题）。
- 已同步更新 `ops_system/04.dws/dws_app_event_funnel_d_d/spec.md`  
  - 明确当前 Spark 版 `is_new` 以当日 `user_register` 事件推导（无 uid=-1），并标注 `dim_user_daily_snapshot` 为目标口径来源但当前不直连。
- 已同步更新 `ops_system/04.dws/dws_app_event_funnel_d_d/design.md`  
  - 数据流图和关键实现点改为“事件级独立聚合”实现，补充抗 OOM 设计说明。
- 已追加进展到 `ops_system/04.dws/dws_app_event_funnel_d_d/memory.md`，记录本轮重构与下一步验证点。

建议你下一步直接在集群跑第 4 轮 SF-81 冒烟（S 档）验证：
- `bash ops_system/04.dws/dws_app_event_funnel_d_d/spark/scripts/run_yarn_daily_sql.sh 2026-08-03 S app_2556`

如果你要，我可以继续把“第 4 轮冒烟验收 checklist（耗时/行数/关键指标抽样）”也补到 `playbook.md` 里，方便你和狂人对齐复…

[user] <timestamp>Saturday, Aug 8, 2026, 9:49 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-08 17:54（周六），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 多渠道统一上下文

含群旁听、私聊、agent-bus。判断要不要回：**仅显式 @youchu_ai_bot / @youchu8888 / @又初 / @初儿** 时在群里回复；裸写「又初」「初儿」或只 @ 别人 → 只作背景，**不要声明「我不插嘴」**——直接不回。

# TG / Cursor / agent-bus 统一上下文（最近消息，与 IDE 同一工作区）

**📥 收到** / **👁 旁听** / **📤 又初回复** 分块展示。

📥 **收到** · `2026-08-07 21:53:51` · uid=8740305767 · telegram_dm
> [私聊#279] 今日结果去掉top2 明日计划去掉top2/3 这个可以不用写

📤 **又初回复** · `2026-08-07 21:53:51`
> 按你的要求改日报：今日结果删第二条，明日动作只保留 TOP1。
已按你的要求改好，并重新推了 TG 私聊。

## 【今日结果】
- [TQ-002 | DMP系统] 大漏斗指标按已定口径推进 Spark 链路：平台 session 已建，Paimon 建表、日批 ETL、设计说明与核查剧本已落库，并登记工作簿自开任务，已完成；
- [TQ-002 | DMP系统] 按知秋安排核对近期上线与 session 分工并回执，含页面访问生产验数、名下任务状态与渠道汇总边界说明；同步核查用户充值订单发产卡点，已完成；
…

[assistant] 理解为继续推进「大漏斗」这条开发任务，我先快速读取启动记忆并定位当前工作区里与大漏斗相关的未完成文件，然后直接接着改到可提交状态。

**Starting bootstrap context read**

[assistant] 我已经定位到你这条「大漏斗」对应的目录是 `ops_system/04.dws/dws_app_event_funnel_d_d/`，下一步先读 `spec/design/memory/task` 把当前卡点找出来，然后直接补齐代码。

**Inspecting new untracked directory**

[assistant] **Preparing to optimize OOM issue**

[assistant] **Planning SQL and OOM optimizations**

[assistant] **Inspecting SQL patterns for optimization**

[assistant] **Assessing new version optimizations**

[assistant] 我准备把日批 SQL 做一次“按事件拆分聚合”的重构，目标是把当前容易 OOM 的超大 `COUNT DISTINCT CASE...` 聚合拆成多个小聚合再汇总，先保口径不变、优先提升可跑性。

**Designing multi-stage distinct aggregation**

[assistant] **Planning syntax verification**

[assistant] **Inspecting key references continuity**

[assistant] **Updating design and spec for snapshot changes**

[assistant] **Designing data flow patch**

[assistant] **Updating memory append with patch application**

[assistant] 我已经把 SQL 重构和文档口径同步改完了，接下来先做一次本地 diff 自检，确认改动范围只在大漏斗目录且没有误改。

**Considering git diff inspection**

[assistant] **Verifying removal with search**

[assistant] **Updating data flow diagram**

[assistant] 已继续推进到可直接做下一轮冒烟的状态，核心是先把 OOM 风险降下来。

- 已重构 `ops_system/04.dws/dws_app_event_funnel_d_d/spark/sql/dws_app_event_funnel_d_d_daily.sql`  
  - 把原来单条超宽 `COUNT(DISTINCT CASE...)` 聚合，改成“按事件拆分聚合 + keys 外连接拼宽表”。  
  - 口径保持不变，目标是降低单个 stage 同时维护大量 distinct 集合导致的内存峰值（对应你这条大漏斗当前的 stage37 OOM 问题）。
- 已同步更新 `ops_system/04.dws/dws_app_event_funnel_d_d/spec.md`  
  - 明确当前 Spark 版 `is_new` 以当日 `user_register` 事件推导（无 uid=-1），并标注 `dim_user_daily_snapshot` 为目标口径来源但当前不直连。
- 已同步更新 `ops_system/04.dws/dws_app_event_funnel_d_d/design.md`  
  - 数据流图和关键实现点改为“事件级独立聚合”实现，补充抗 OOM 设计说明。
- 已追加进展到 `ops_system/04.dws/dws_app_event_funnel_d_d/memory.md`，记录本轮重构与下一步验证点。

建议你下一步直接在集群跑第 4 轮 SF-81 冒烟（S 档）验证：
- `bash ops_system/04.dws/dws_app_event_funnel_d_d/spark/scripts/run_yarn_daily_sql.sh 2026-08-03 S app_2556`

如果你要，我可以继续把“第…
```
