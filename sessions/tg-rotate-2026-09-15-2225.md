# TG 会话轮换归档 · 2026-09-15-2225

- 原因: max_transcript(407KB>=400KB)
- session_id: ses_1789200139925_et7bpb
- cursor_chat_id: e286c4e6-208a-4186-9e9f-f416b8f3d5fb
- 蒸馏: AI+heuristic

## 携带要点

- 21:30 写稿前跑过 `prepare_daily_report_sync.sh` 且 hosts 齐全，仍可能漏活——根因常是冲刺 flush（21:20/18:20）没把实活写进 `.cursor/work-log/当日.md`，只留了 bus 噪音。
- 写稿不能只看合并 hosts：hosts 空或偏少时，必须再扫本机 **全部 agent transcript**、派单来源（`task_provenance.jsonl` / agent-bus inbox），以及 **开发平台 session、沙箱、群/bus 旁证**。
- 「不硬凑」铁律：只写当日已闭环、可标「已完成」的交付；他人线在推、验收未过、覆盖待拍板的项（如归因 1.0.11）**禁止**塞进「今日结果」。
- 主人点名漏记时：以实据还原（平台发布记录、测试进展、bus 回执）→ 改定稿 → **立刻重推 TG**；对话只确认路径，禁止再贴全文防双推。
- [LESSON: daily-report,new-mac,work-log|hosts 同步齐但 bullet≈0 时，不得仅凭合并稿定稿；必须扫 transcript + 派单 + 平台 session/沙箱旁证，再向主人核对漏记]
- [LESSON: daily-report,不硬凑|他人线未验收/自测未闭环只写明日动作，禁止为凑条数硬拆或标已完成]
- 日报「今日结果偏少」≠ 双机没同步：先核对 `new-mac`/`old-mac` 当日 hosts 是否都在，再看两边 `bullet` 是否几乎为 0；合并只能汇总已有流水，不能凭空补条。
- Cursor transcript **不跨机**；新机干的活若未落该机 work-log 并经 memory git 同步，旧机汇总时必然看不见。
- 进行中/未验收项（如大漏斗新逻辑还在测）应放 **【明日动作】**，不写进今日结果。
- 初稿偏少时主动向主人确认：「若还有已交办且已闭环的活，请点名」——避免把 flush 漏记误判为「今天确实没干多少」。
- 归因升级类「新机发布并推进同步」若 work-log 未记，仍可从 dev session / 海豚 test 发布旁证补进结果条。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-15-hosts-同步齐但-bullet-0-时-不得仅凭合并稿定稿-必须扫-transcript-派.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-15-新机实活交付后须写入该机-cursor-work-log-当日-md-并-sync-memory.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-15-他人线未验收-自测未闭环只写明日动作-禁止为凑条数硬拆或标已完成.md`

## AI 蒸馏原文（截断）

```
- 日报「今日结果偏少」≠ 双机没同步：先核对 `new-mac`/`old-mac` 当日 hosts 是否都在，再看两边 `bullet` 是否几乎为 0；合并只能汇总已有流水，不能凭空补条。
- 21:30 写稿前跑过 `prepare_daily_report_sync.sh` 且 hosts 齐全，仍可能漏活——根因常是冲刺 flush（21:20/18:20）没把实活写进 `.cursor/work-log/当日.md`，只留了 bus 噪音。
- Cursor transcript **不跨机**；新机干的活若未落该机 work-log 并经 memory git 同步，旧机汇总时必然看不见。
- 写稿不能只看合并 hosts：hosts 空或偏少时，必须再扫本机 **全部 agent transcript**、派单来源（`task_provenance.jsonl` / agent-bus inbox），以及 **开发平台 session、沙箱、群/bus 旁证**。
- 「不硬凑」铁律：只写当日已闭环、可标「已完成」的交付；他人线在推、验收未过、覆盖待拍板的项（如归因 1.0.11）**禁止**塞进「今日结果」。
- 进行中/未验收项（如大漏斗新逻辑还在测）应放 **【明日动作】**，不写进今日结果。
- 主人点名漏记时：以实据还原（平台发布记录、测试进展、bus 回执）→ 改定稿 → **立刻重推 TG**；对话只确认路径，禁止再贴全文防双推。
- 初稿偏少时主动向主人确认：「若还有已交办且已闭环的活，请点名」——避免把 flush 漏记误判为「今天确实没干多少」。
- 归因升级类「新机发布并推进同步」若 work-log 未记，仍可从 dev session / 海豚 test 发布旁证补进结果条。

[LESSON: daily-report,new-mac,work-log|hosts 同步齐但 bullet≈0 时，不得仅凭合并稿定稿；必须扫 transcript + 派单 + 平台 session/沙箱旁证，再向主人核对漏记]
[LESSON: daily-report,flush|新机实活交付后须写入该机 `.cursor/work-log/当日.md` 并 sync-memory-git，否则旧机 21:30 写稿必漏]
[LESSON: daily-report,不硬凑|他人线未验收/自测未闭环只写明日动作，禁止为凑条数硬拆或标已完成]
```

## 会话摘录（截断）

```
[user] [私聊#491] 没有和新机同步吗？为什么今日结果才这么点

[assistant] 今日结果偏少，多半是双机 work-log 没齐。我先跑同步，再对照新旧机流水和定稿查缺了什么。双机 hosts 都有，但两边流水几乎空。接着扫 transcript 和派单，把今天真实交付补进日报。同步过了，不是新机缺席。

21:30 写稿前已跑双机同步，核对是：`new-mac` / `old-mac` 当日 hosts 都在；我刚又重跑了一遍，结果一样。两边都是 `bullet≈0`——冲刺 flush 几乎没落实活，只剩旧 bus 噪音，所以合并不等于多出结果。

今日结果只写一条，是按「不硬凑」来的：早盘实时明细断流，又初侧只读复核到闭环，这一条能标已完成。晚上归因 1.0.11 是野花线在推，验收未过、覆盖还在等拍板，按规矩不能塞进「今日结果」。

若你记得还有别的已交办且已闭环的活，直接点名，我按定稿补进结果并重推 TG。双机同步已跑完：新旧机当日 hosts 都在，两边流水仍是空的，所以合并不会多出结果条。

[user] [私聊#492] 我在new mac不是发布了归因升级相关的内容，还在测试大漏斗的新逻辑吗

[assistant] 你说得对，我去新机侧核对归因发布和大漏斗测试的实据，再补进今日结果。新机流水没记下，我改从平台会话、沙箱和群/bus 旁证里还原这两块。对，新机这两块漏记了。按你说的补进定稿并重推 TG。对，是新机这两块。冲刺 flush 没把实活写进 hosts，初稿就漏了。

已按你说的改定稿并重推 TG：今日结果补上「归因升级：新机发布并推进同步」；大漏斗还在测、未闭环，放明日动作。稿在 `.cursor/work-log/reports/日报-2026-09-15.md`。改定稿已推到 TG 私聊，稿路径仍是 `.cursor/work-log/reports/日报-2026-09-15.md`。

[user] <timestamp>Tuesday, Sep 15, 2026, 9:42 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-15 21:41（周二），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-15 21:31:46 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/dat…

[assistant] 今日结果偏少，多半是双机 work-log 没齐。我先跑同步，再对照新旧机流水和定稿查缺了什么。

[assistant] 双机 hosts 都有，但两边流水几乎空。接着扫 transcript 和派单，把今天真实交付补进日报。

[assistant] 同步过了，不是新机缺席。

21:30 写稿前已跑双机同步，核对是：`new-mac` / `old-mac` 当日 hosts 都在；我刚又重跑了一遍，结果一样。两边都是 `bullet≈0`——冲刺 flush 几乎没落实活，只剩旧 bus 噪音，所以合并不等于多出结果。

今日结果只写一条，是按「不硬凑」来的：早盘实时明细断流，又初侧只读复核到闭环，这一条能标已完成。晚上归因 1.0.11 是野花线在推，验收未过、覆盖还在等拍板，按规矩不能塞进「今日结果」。

若你记得还有别的已交办且已闭环的活，直接点名，我按定稿补进结果并重推 TG。

[user] <timestamp>Tuesday, Sep 15, 2026, 9:46 PM (UTC+8)</timestamp>

<user_query>Briefly inform the user about the task result and perform any follow-up actions (if needed). If there's no follow-ups needed, don't explicitly say that.</user_query>

[assistant] 双机同步已跑完：新旧机当日 hosts 都在，两边流水仍是空的，所以合并不会多出结果条。

[user] <timestamp>Tuesday, Sep 15, 2026, 9:50 PM (UTC+8)</timestamp>
<user_query>
[私聊#492] 我在new mac不是发布了归因升级相关的内容，还在测试大漏斗的新逻辑吗
</user_query>

[assistant] 你说得对，我去新机侧核对归因发布和大漏斗测试的实据，再补进今日结果。

[assistant] 新机流水没记下，我改从平台会话、沙箱和群/bus 旁证里还原这两块。

[assistant] 对，新机这两块漏记了。按你说的补进定稿并重推 TG。

[assistant] 对，是新机这两块。冲刺 flush 没把实活写进 hosts，初稿就漏了。

已按你说的改定稿并重推 TG：今日结果补上「归因升级：新机发布并推进同步」；大漏斗还在测、未闭环，放明日动作。稿在 `.cursor/work-log/reports/日报-2026-09-15.md`。

[user] <timestamp>Tuesday, Sep 15, 2026, 9:55 PM (UTC+8)</timestamp>

<user_query>Briefly inform the user about the task result and perform any follow-up actions (if needed). If there's no follow-ups needed, don't explicitly say that.</user_query>

[assistant] 改定稿已推到 TG 私聊，稿路径仍是 `.cursor/work-log/reports/日报-2026-09-15.md`。
```
