# TG 会话轮换归档 · 2026-09-04-2115

- 原因: max_transcript(417KB>=400KB)
- session_id: ses_1788497336873_69govb
- cursor_chat_id: 53d4eae1-d08b-4362-a2d6-e47708665ec4
- 蒸馏: AI+heuristic

## 携带要点

- 大漏斗沙箱 **explain PASS ≠ 已出数**；explain 结束后必须立刻接 metric 真跑 → wide 真跑，中间不能停，否则 `test.dws` 宽表仍 0 行
- `hadoop-1` 直连常被拒，查 explain/YARN 实况应走**已知入口**，不要死磕直连
- 狂人 **stage_metrics 复审**对象：`563013e7` + 冻结 tag `8b613fb6`；PASS 5 条含 11 张 `_r` 全切、`${DT}` 裸串、18 路 `COUNT(DISTINCT event_id)`、`reg_uids` 换 `dwd_user_register_d_v2_r` 等
- 长时间无回可先查是否已有 reply，再发 **bus 催促**（如 #7911），写明「今天要出数、请优先审」+ 当前 test 进展 + 还差什么 PASS/打回
- [LESSON: funnel|explain PASS 后同一轮会话内立刻接 metric→wide 真跑，开跑前确认源表 T-1 分区有数且 `--dt` 任务日与业务日对齐]
- [LESSON: agent-bus|狂人侧消息常被压缩截断，reply 须贴回 #7900 等待审原文一字不动并写明 commit+tag，勿让进度汇报冒充复审单]
- 真跑前先核对 **源表分区有数**（如 `sdk_init` 只有 `09-04` 有分区）再开跑，避免空跑
- 大漏斗 `--dt` 是**任务日**，业务数据段 = T-1：任务日 `2026-09-05` → 业务日 `2026-09-04`，须与源表分区对齐
- 出数链路固定顺序：`dws_app_event_funnel_metric_stg_d`（stage_metrics）→ `dws_app_event_funnel_d_d`（stage_wide）；验 `sdk_init`/`video` 非全 0
- **#7900 复审不挡当天 test 出数**；可先冒烟/真跑，复审并行
- agent-bus 出站 `#7908` 等是 **reply 结案回执**，不是又初另起一单；收到 work 单 → 60s 内 ACK → 干完 reply
- 狂人侧 **上下文会被压缩丢原文**（#7906/#7900/#7911 均发生过）；reply 时须把待审/待确认内容**一字不动贴回**（纯文字、三条到齐），并明确复审对象 commit+tag
- 进度汇报（#7906 出数成功）与复审请求（#7899/#7900 三条）要**分开表述**，避免对方误当复审单
- 主人分工：**又初负责 test 全量跑到位**（非 SF-81 单 app 冒烟，`app_filter_e/v/n/c` 全空串全 app），狂人验数 → 挂调度 → 回写；用 `sandbox_steps_full.json` 槽 00，命令按 `_meta`「怎么跑」三行
- test 出数落 **Paimon `lakehouse_test`**；SR 尚未同步属预期，不等 SR 再报 progress

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-04-explain-pass-后同一轮会话内立刻接-metric-wide-真跑-开跑前确认源表-t.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-04-狂人侧消息常被压缩截断-reply-须贴回-7900-等待审原文一字不动并写明-commit-t.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-04-test-验收须全-app-空-filter-跑完-stage_metrics-stage_wi.md`

## AI 蒸馏原文（截断）

```
- 大漏斗沙箱 **explain PASS ≠ 已出数**；explain 结束后必须立刻接 metric 真跑 → wide 真跑，中间不能停，否则 `test.dws` 宽表仍 0 行
- 真跑前先核对 **源表分区有数**（如 `sdk_init` 只有 `09-04` 有分区）再开跑，避免空跑
- 大漏斗 `--dt` 是**任务日**，业务数据段 = T-1：任务日 `2026-09-05` → 业务日 `2026-09-04`，须与源表分区对齐
- `hadoop-1` 直连常被拒，查 explain/YARN 实况应走**已知入口**，不要死磕直连
- 出数链路固定顺序：`dws_app_event_funnel_metric_stg_d`（stage_metrics）→ `dws_app_event_funnel_d_d`（stage_wide）；验 `sdk_init`/`video` 非全 0
- 狂人 **stage_metrics 复审**对象：`563013e7` + 冻结 tag `8b613fb6`；PASS 5 条含 11 张 `_r` 全切、`${DT}` 裸串、18 路 `COUNT(DISTINCT event_id)`、`reg_uids` 换 `dwd_user_register_d_v2_r` 等
- **#7900 复审不挡当天 test 出数**；可先冒烟/真跑，复审并行
- agent-bus 出站 `#7908` 等是 **reply 结案回执**，不是又初另起一单；收到 work 单 → 60s 内 ACK → 干完 reply
- 狂人侧 **上下文会被压缩丢原文**（#7906/#7900/#7911 均发生过）；reply 时须把待审/待确认内容**一字不动贴回**（纯文字、三条到齐），并明确复审对象 commit+tag
- 进度汇报（#7906 出数成功）与复审请求（#7899/#7900 三条）要**分开表述**，避免对方误当复审单
- 长时间无回可先查是否已有 reply，再发 **bus 催促**（如 #7911），写明「今天要出数、请优先审」+ 当前 test 进展 + 还差什么 PASS/打回
- 主人分工：**又初负责 test 全量跑到位**（非 SF-81 单 app 冒烟，`app_filter_e/v/n/c` 全空串全 app），狂人验数 → 挂调度 → 回写；用 `sandbox_steps_full.json` 槽 00，命令按 `_meta`「怎么跑」三行
- test 出数落 **Paimon `lakehouse_test`**；SR 尚未同步属预期，不等 SR 再报 progress

[LESSON: funnel|explain PASS 后同一轮会话内立刻接 metric→wide 真跑，开跑前确认源表 T-1 分区有数且 `--dt` 任务日与业务日对齐]
[LESSON: agent-bus|狂人侧消息常被压缩截断，reply 须贴回 #7900 等待审原文一字不动并写明 commit+tag，勿让进度汇报冒充复审单]
[LESSON: funnel|test 验收须全 app 空 filter 跑完 stage_metrics→stage_wide（`sandbox_steps_full.json` 槽 00），单 SF-81 冒烟不能当交付]
```

## 会话摘录（截断）

```
[user] [私聊#467] 目前什么进度

[assistant] 先对一下大漏斗沙箱 explain / 出数实况，再回你进度。hadoop-1 直连被拒，换已知入口查 explain 实况。explain 已结束；接着确认是否已真跑、以及 `test.dws` 是否有数。explain 已 PASS，真跑还没起。正在核对 test 表行数并立刻开真跑。先确认业务日与源表有数分区，再开真跑。源表确认：`sdk_init` 只有 `09-04` 有数。真跑用 `--dt=2026-09-05`（任务日→数据段 T-1=`09-04`）。一句话：**代码齐了，explain 已过；真跑刚拉起，数据还没落完。**

· 已完成：摘 `new` 切 `_r` 已 push；沙箱 explain PASS（约 3 分钟，写 `test.dws`）
· 卡点：上一轮 explain 完后没接着真跑，所以宽表还是空的（SF-81 · 09-03/09-04 = 0 行）
· 刚开干：metric 真跑已起  
  · SF-81 · 业务日 `2026-09-04`（任务日 09-05 → slot 对上今天，`sdk_init` 今日有数）  
  · YARN `application_…_6804`  
  · 日志 `/tmp/funnel_real_metric_20260904.log`
· 接下来：metric 出数 → 再跑 wide → 验 sdk_init/video 非全 0 → 报你

狂人 #7900 复审不挡今天出数。有结果我再私聊你。

[user] [私聊#468] 出站 bus#7908 这是什么？怎么突然会发这个

[assistant] 这是刚回狂人的结案回执，不是我主动乱发的。

起因：狂人发来 **bus#7907**，说群里看到你 @ 他，但他那边压缩丢了 **#7906** 原文，让我把「要他复审/拍板的点」再贴一遍。

按 agent-bus 规矩：收到 work 单 → ACK → 干完 **reply**。我查完后确认 **#7906 其实是出数进度**（不是复审单），就回了 **#7908**，把原文 + 真要审的仍是 **#7899 / #7900 那 3 条** 说明清楚。

所以你会在出站里看到 #7908：是对狂人 #7907「把 #7906 贴过来」的答复，不是另起一单。

[user] [私聊#469] 收到回复了吗？没有的话催促狂人一下，告诉他今天要跑出数据的，优先处理一下啊

[assistant] 先查狂人有没有回 #7908，没有就立刻催。没收到对 #7908 的新回。正在 bus 催狂人优先审。没有。到现在狂人还没回 #7908。

已催 **bus#7911**：说明主人今天要出数、请优先审；并写明 test 侧 SF-81 已出数、还差 stage_metrics 那 3 条 PASS/打回。

他回了我再转你。

[user] <timestamp>Friday, Sep 4, 2026, 3:37 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-04 15:36（周五），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-04 12:47:48 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/datab…

[assistant] 先对一下大漏斗沙箱 explain / 出数实况，再回你进度。

[assistant] hadoop-1 直连被拒，换已知入口查 explain 实况。

[assistant] explain 已结束；接着确认是否已真跑、以及 `test.dws` 是否有数。

[assistant] explain 已 PASS，真跑还没起。正在核对 test 表行数并立刻开真跑。

[assistant] 先确认业务日与源表有数分区，再开真跑。

[assistant] 源表确认：`sdk_init` 只有 `09-04` 有数。真跑用 `--dt=2026-09-05`（任务日→数据段 T-1=`09-04`）。

[assistant] 一句话：**代码齐了，explain 已过；真跑刚拉起，数据还没落完。**

· 已完成：摘 `new` 切 `_r` 已 push；沙箱 explain PASS（约 3 分钟，写 `test.dws`）
· 卡点：上一轮 explain 完后没接着真跑，所以宽表还是空的（SF-81 · 09-03/09-04 = 0 行）
· 刚开干：metric 真跑已起  
  · SF-81 · 业务日 `2026-09-04`（任务日 09-05 → slot 对上今天，`sdk_init` 今日有数）  
  · YARN `application_…_6804`  
  · 日志 `/tmp/funnel_real_metric_20260904.log`
· 接下来：metric 出数 → 再跑 wide → 验 sdk_init/video 非全 0 → 报你

狂人 #7900 复审不挡今天出数。有结果我再私聊你。

[user] <timestamp>Friday, Sep 4, 2026, 4:30 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7907 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7907 --text "[ACK] bus#7907 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7907 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：收到你 TG 群里 @, 但我这边流里没保留 #7906 原文(压缩掉了)。你把 #7906 里要我复审/拍板的具体点(stage_metrics diff 3 条 or 别的)直接贴过来, 我这边继续。
</user_query>

[assistant] 先 ACK bus#7907，再找回 #7906 原文贴给狂人。

[assistant] #7906 是出数进度，不是复审请求。正在把原文和真正待审的 3 点贴回去。

[assistant] 已回 bus#7908 结案。

澄清给狂人：
· **#7906 不是复审单**，是出数进度（metric+宽表真跑成功，SF-81 非全 0）
· 真要审的是 **#7899**（`563013e7`）+ 他 **#7900** 那 3 条（`COUNT(DISTINCT)` / 11 事件切 `_r` / legacy 冻结）

顺带确认：大漏斗今天 **test 出数已成**（Paimon `lakehouse_test`；SR 尚未同步属预期）。

[user] <timestamp>Friday, Sep 4, 2026, 4:38 PM (UTC+8)</timestamp>
<user_query>
[私聊#468] 出站 bus#7908 这是什么？怎么突然会发这个
</user_query>

[assistant] 这是刚回狂人的结案回执，不是我主动乱发的。

起因：狂人发来 **bus#7907**，说群里看到你 @ 他，但他那边压缩丢了 **#7906** 原文，让我把「要他复审/拍板的点」再贴一遍。

按 agent-bus 规矩：收到 work 单 → ACK → 干完 **reply**。我查完后确认 **#7906 其实是出数进度**（不是复审单），就回了 **#7908**，把原文 + 真要审的仍是 **#7899 / #7900 那 3 条** 说明清楚。

所以你会在出站里看到 #7908：是对狂人 #7907「把 #7906 贴过来」的答复，不是另起一单。

[user] <timestamp>Friday, Sep 4, 2026, 8:24 PM (UTC+8)</timestamp>
<user_query>
[私聊#469] 收到回复了吗？没有的话催促狂人一下，告诉他今天要跑出数据的，优先处理一下啊
</user_query>

[assistant] 先查狂人有没有回 #7908，没有就立刻催。

[assistant] 没收到对 #7908 的新回。正在 bus 催狂人优先审。

[assistant] 没有。到现在狂人还没回 #7908。

已催 **bus#7911**：说明主人今天要出数、请优先审；并写明 test 侧 SF-81 已出数、还差 stage_metrics 那 3 条 PASS/打回。

他回了我再转你。

[user] <timestamp>Friday, Sep 4, 2026, 8:37 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7912 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7912 --text "[ACK] bus#7912 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7912 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：收到 #7911。test 侧 SF-81 跑成功那部分我看到了，先谢一下。  但我这边**上下文已被压缩**：#7900 我发出去后没留全文, #7911 只截到「① order_created/coin · COUNT(DISTINCT event_id) 是否每 st…」，后面看不到。你这边如果还留着 #7900 原文, 请**把三条一字不动地贴过来**(纯文字不要 asset), 3 条到齐我今晚给 PASS 或点位打回, 不再折返。  同时确认一下, 你要复审的对象就是 stage_metrics 摘 new 的 commit **563013e7** + 冻结 tag **8b613fb6** 吧, 我…

[assistant] 先 ACK，再把 #7900 三条原文一字不动贴回。

[assistant] 已回 **bus#7913** 结案。

· 确认复审对象：`563013e7` + `8b613fb6`
· **#7900 三条原文一字不动**已贴回，拆开便于他对着审

等他今晚 PASS / 打回点。

[user] <timestamp>Friday, Sep 4, 2026, 9:07 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7914 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7914 --text "[ACK] bus#7914 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7914 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：大漏斗 stage_metrics 摘 new 复审 · **PASS**（对象: commit 563013e7 + 冻结 tag 8b613fb6，绕开 #7900 三条原文，直接按判据现场复审）。  5 条全过: ① 主干 11 张 _r 全切，day_uids UNION 14 张（禁扫 new）✓ ② 所有 _r 表 dt 用裸串 '${DT}'，不再 DATE 字面量 ✓ ③ 18 路 metrics 全改 COUNT(DISTINCT event_id)（含 vid/nov/com 补 event_id 列）✓ ④ reg_uids 换 dwd_user_register_d_v2_r，is_new …

[assistant] 收到 PASS，先 ACK 再结案回执。

[assistant] **PASS 已收**，回执 **bus#7915** 已结案。

· 复审对象 `563013e7` + `8b613fb6`，5 条全过  
· test 侧 SF-81（业务日 09-04）此前已出数  
· 狂人放行「可投产」——要我接着挂产 / 写 `paimon.dws`，你说一声我就推

[user] <timestamp>Friday, Sep 4, 2026, 9:11 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7916 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7916 --text "[ACK] bus#7916 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7916 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：主人钦定的分工: **你把 test 全部跑到位, 我来验数据 → 挂调度 → 回写**。  请你完成:  ① **test.dws 侧全 app 完整跑一遍**(不是 SF-81 单 app 冒烟, 是 app_filter_e/v/n/c 全空串的全量), dt=2026-09-04    - 顺序: `dws_app_event_funnel_metric_stg_d` → `dws_app_event_funnel_d_d`(stage_metrics → stage_wide)    - 用 sandbox_steps_full.json 的槽 00 一把跑, 命令按 _meta 里"怎么跑"那三行走  …

[assistant] 先 ACK，补全 #7916 被截断的正文，再开全 app 真跑。

[user] <dynamic_tools>
You have access to tools through dynamic namespaces, e.g. MCP servers, using `GetDynamicTools` and `CallDynamicTool`.

## Dynamic Tool Discovery and Invocation

Use `GetDynamicTools` to discover tool schemas, then `CallDynamicTool` to invoke one tool. Aim to minimize round-trips: ideally one discovery call followed by one invocation.

If the user mentions a product or service represented by an available namespace, and the request likely depends on it, proactively inspect that namespace before answering. If you are unsure which namespace matches, search with a relevant pattern.

```
