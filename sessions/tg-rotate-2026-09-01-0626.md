# TG 会话轮换归档 · 2026-09-01-0626

- 原因: daily(06:00)
- session_id: ses_1788145817068_9meg22
- cursor_chat_id: 6d4d8294-c12f-4255-9ebd-a2add42243ff
- 蒸馏: AI+heuristic

## 携带要点

- 日报「上传云端」须以定稿 Markdown **原封不动**落盘（`.cursor/work-log/reports/日报-YYYY-MM-DD.md`）再跑 `upload_work_report.py`，禁止改写后再传
- Spark SQL 硬规矩：源表用 `_r` 版；`_r` 数值列 VARCHAR 须 CAST；天表只 `WHERE dt='${DT}'`；必须幂等；paimon 列顺序对齐；验收先 `run_test.sh run --step=... --explain`，**别动生产 `full_chain.json`**
- 大漏斗按已定稿口径写：`docs/event_dictionary_big_funnel_20260801.html` + 平台 `metric_big_funnel_event_dictionary`；仓库骨架在 `ops_system/04.dws/dws_app_event_funnel_d_d/spark/`（metrics + wide 两阶段），不重开口径
- agent-bus 派单：同一 Cursor 主会话处理，**60 秒内 ACK → 干完 reply 结案**；reply 成功前不 mark 结案；引用历史结论前须核对是否已被后续决策作废（如设备标签 #7735 过期说法）
- [LESSON: prod-monitor,oncall|prod 告警处置顺序：先分 env → 问此刻是否仍在发生（DS state=1，不信 monitor 快 1h 的时间戳）→ 追首个 FAILURE 真 task → download-log 取证，禁信根因字段与 DEPENDENT]
- [LESSON: spark-scheduler,pipeline-runner|加 Spark 任务只改 SQL + steps.json 挂槽位，tagTargets 必填，先 explain 试跑，禁止动生产 full_chain.json]
- Spark pipeline-runner **加任务 = 写 SQL 文件 + 在 `steps/full_chain.json`（或独立测试 json）追加一步并挂到 group/槽位**；驱动不认业务，**不改 Scala**
- 调度三层：`slots`（整点跑哪些组）→ `groups`（组内步顺序）→ `steps`（步定义）；Step 必填 `name/unit/trigger/probes/impl/params.sqlFile`，**`tagTargets` 必填**（回滚凭据）
- 设备标签：08-04「不上 prod」**作废**；08-11「捡回来」+ 知秋确认拦阻已解除，现状是 **等 pipeline-runner 就位后接上**；又初主责按规范改造
- 设备标签硬约束：主键/join 一律 **`device_fingerprint`（64 位，不看 fp_version）**；`device_id` 仅附属；六张 dwm + dim + dws + uid_map 全链改键
- 设备标签现状：paimon 建齐但数据停在 **07-27**；enroll 闸门从未生效；`dwm_device_uid_map_d` **只有 spec 无 SQL**；交付 **先出方案审过再动手**，方案须答指纹替换清单/覆盖率、`_r` 50 天保留、enroll 闸门+人口预估、uid_map SQL、pipeline-runner step 五问
- prod 值班六条：① **先分 env**，test 断流/僵尸 wf **一律不处理**；② 先问「**此刻还在发生吗**」（server_monitor 时间戳比 DS **快 1 小时**，须回 DS 查 **state=1**）；③ **DEPENDENT/「等_xxx」是果**，只追首个 **state=FAILURE** 真 SQL task；④ **禁信 server_monitor 根因**，用 `download-log?taskInstanceId=` 取证，**不用** `/log/detail`（会截断）；⑤ 行数涨跌跟**前一日同小时**比；⑥ 可能已自愈，看**当时**实例/日志
- **直接修复半径（知秋授权）**：判为 **prod 事故** → 立即处置（**含改代码**），处理完再报，事故窗口内不停下来请示；**非事故**的日常 prod 变更/口径设计/加表加字段 → 照旧等知秋 GO；先用六条判是不是事故，不确定先查清楚

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-prod-告警处置顺序-先分-env-问此刻是否仍在发生-ds-state-1-不信-monit.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-判为-prod-事故后可直接改代码并立即修复-处理完再报-非事故-prod-变更仍须等知秋-go.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-加-spark-任务只改-sql-steps-json-挂槽位-tagtargets-必填-先-.md`

## AI 蒸馏原文（截断）

```
- 日报「上传云端」须以定稿 Markdown **原封不动**落盘（`.cursor/work-log/reports/日报-YYYY-MM-DD.md`）再跑 `upload_work_report.py`，禁止改写后再传
- Spark pipeline-runner **加任务 = 写 SQL 文件 + 在 `steps/full_chain.json`（或独立测试 json）追加一步并挂到 group/槽位**；驱动不认业务，**不改 Scala**
- 调度三层：`slots`（整点跑哪些组）→ `groups`（组内步顺序）→ `steps`（步定义）；Step 必填 `name/unit/trigger/probes/impl/params.sqlFile`，**`tagTargets` 必填**（回滚凭据）
- Spark SQL 硬规矩：源表用 `_r` 版；`_r` 数值列 VARCHAR 须 CAST；天表只 `WHERE dt='${DT}'`；必须幂等；paimon 列顺序对齐；验收先 `run_test.sh run --step=... --explain`，**别动生产 `full_chain.json`**
- 大漏斗按已定稿口径写：`docs/event_dictionary_big_funnel_20260801.html` + 平台 `metric_big_funnel_event_dictionary`；仓库骨架在 `ops_system/04.dws/dws_app_event_funnel_d_d/spark/`（metrics + wide 两阶段），不重开口径
- 设备标签：08-04「不上 prod」**作废**；08-11「捡回来」+ 知秋确认拦阻已解除，现状是 **等 pipeline-runner 就位后接上**；又初主责按规范改造
- 设备标签硬约束：主键/join 一律 **`device_fingerprint`（64 位，不看 fp_version）**；`device_id` 仅附属；六张 dwm + dim + dws + uid_map 全链改键
- 设备标签现状：paimon 建齐但数据停在 **07-27**；enroll 闸门从未生效；`dwm_device_uid_map_d` **只有 spec 无 SQL**；交付 **先出方案审过再动手**，方案须答指纹替换清单/覆盖率、`_r` 50 天保留、enroll 闸门+人口预估、uid_map SQL、pipeline-runner step 五问
- prod 值班六条：① **先分 env**，test 断流/僵尸 wf **一律不处理**；② 先问「**此刻还在发生吗**」（server_monitor 时间戳比 DS **快 1 小时**，须回 DS 查 **state=1**）；③ **DEPENDENT/「等_xxx」是果**，只追首个 **state=FAILURE** 真 SQL task；④ **禁信 server_monitor 根因**，用 `download-log?taskInstanceId=` 取证，**不用** `/log/detail`（会截断）；⑤ 行数涨跌跟**前一日同小时**比；⑥ 可能已自愈，看**当时**实例/日志
- **直接修复半径（知秋授权）**：判为 **prod 事故** → 立即处置（**含改代码**），处理完再报，事故窗口内不停下来请示；**非事故**的日常 prod 变更/口径设计/加表加字段 → 照旧等知秋 GO；先用六条判是不是事故，不确定先查清楚
- agent-bus 派单：同一 Cursor 主会话处理，**60 秒内 ACK → 干完 reply 结案**；reply 成功前不 mark 结案；引用历史结论前须核对是否已被后续决策作废（如设备标签 #7735 过期说法）

[LESSON: prod-monitor,oncall|prod 告警处置顺序：先分 env → 问此刻是否仍在发生（DS state=1，不信 monitor 快 1h 的时间戳）→ 追首个 FAILURE 真 task → download-log 取证，禁信根因字段与 DEPENDENT]

[LESSON: prod-incident,authorization|判为 prod 事故后可直接改代码并立即修复，处理完再报；非事故 prod 变更仍须等知秋 GO]

[LESSON: spark-scheduler,pipeline-runner|加 Spark 任务只改 SQL + steps.json 挂槽位，tagTargets 必填，先 explain 试跑，禁止动生产 full_chain.json]
```

## 会话摘录（截断）

```
[user] [私聊#430] # 日报 · 又初·2026-08-31

[REPORT-ORG:天穹部门] [LEVEL:L1] [TYPE:日报] [DATE:2026-08-31]
> 提交人: 又初
> 工号: DN6517
> 岗位: 后端 BE
> 层级: L1
> 日期: 2026-08-31

## 【今日结果】
- [TQ-002 | DMP系统] 生产监控告警处置：按四条原则整理处置流程，写入告警剧本与经验库，后续 prod 故障按先验仍在发生、禁信根因字段、DEPENDENT 是果、download-log 取证执行，已完成；

## 【死锁阻碍】
- [TQ-002 | DMP系统] 

## 【专项复盘】
- 

## 【明日动作】
- TOP1: [TQ-002 | 设备标签] 按重新设计方向对接 Spark 调度写法并试跑（截止：明日）

按照这个上传云端

[assistant] 按你贴的定稿原样落盘并上传云端。
已按你贴的正文原封不动上传云端。

- **日期**：2026-08-31  
- **云端记录 ID**：85202  
- **状态**：inserted（新建）  
- **本地稿**：`.cursor/work-log/reports/日报-2026-08-31.md`

[user] <timestamp>Monday, Aug 31, 2026, 10:03 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-31 22:02（周一），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-31 21:32:52 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/dat…

[assistant] 按你贴的定稿原样落盘并上传云端。

[REDACTED]

[assistant] 已按你贴的正文原封不动上传云端。

- **日期**：2026-08-31  
- **云端记录 ID**：85202  
- **状态**：inserted（新建）  
- **本地稿**：`.cursor/work-log/reports/日报-2026-08-31.md`

[REDACTED]

[user] <timestamp>Monday, Aug 31, 2026, 10:09 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7735 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7735 --text "[ACK] bus#7735 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7735 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：回 #7734（狂人）。两问分开答。  ═══ 问1：spark 调度框架里怎么加任务、任务怎么写 ═══  【一句话】加一个任务 = 改两处，写一个 SQL 文件 + 在 steps.json 里加一条定义并挂到槽位。**驱动程序完全不认识业务，不用改任何 Scala 代码。**  ──── A. 三块配置（steps/full_chain.json，从粗到细）──── slots  : {"00":[组名...], ..., "23":[...]}   每个整点跑哪几组，数组顺序 = 执行顺序 groups : {组名: {说明, steps:[步名...]}}      组里有哪些步，顺序 = 执行顺序 …

[assistant] 先 ACK，再拉取 bus#7735 完整正文后分开答复。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 狂人已在 bus#7735 给出完整答复；正在整理成对 #7734 的两问分答并 reply 结案。

[REDACTED]

[assistant] 已 ACK 并 reply 结案（bus#7736 → 回 #7734）。

---

**问1：Spark 调度怎么加任务**

加一个任务 = **写一个 SQL 文件** + 在 `steps/full_chain.json`（或独立测试 json）里 **append 一步并挂到 group/槽位**。驱动不认业务，**不用改 Scala**。

- **三层**：`slots`（整点跑哪些组）→ `groups`（组内步顺序）→ `steps`（步定义）
- **Step 要点**：`name` 唯一；`unit` hour/day；`trigger` timer/probe；`probes` 是源表 watermark 就绪判据；`impl` spark_sql/sr_sql；`params.sqlFile`；**`tagTargets` 必填**（回滚凭据）
- **SQL 硬规矩**：源表用 `_r` 版；`_r` 数值列是 VARCHAR 须 CAST；天表只 `WHERE dt='${DT}'`；必须幂等；paimon 列顺序对齐
- **验收**：`run_test.sh run --step=... --explain` 先 explain 再真跑；别动生产 `full_chain.json`

---

**问2：大漏斗按框架写**

口径已定稿：`docs/event_dictionary_big_funnel_20260801.html` + 平台 `metric_big_funnel_event_dictionary`，**按那份写不重开**。仓库 Spark 骨架在 `ops_system/04.dws/dws_app_event_funnel_d_d/spark/`（metrics + wide 两阶段）。

易漏：行为取值 `UPPER…

[user] <timestamp>Monday, Aug 31, 2026, 10:33 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7738 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7738 --text "[ACK] bus#7738 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7738 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：设备标签 · 完整交底 + 改造派活（狂人）。知秋刚定：**设备标签整体交给你按规范改造，让大家都学，不能都由我们代做。**  ═══ 0. 先更正我上一条（#7735）═══  我在 #7735 里说「我这边生效的仍是 08-04 那条：设备标签先不上 prod」——**这条作废，是我引用了过期结论，对不住。**  真实状态：知秋 08-11 就说过「把设备标签的捡回来吧」，今晚（08-31）又亲口确认「我记得是能跑的，只是后面没继续跑了，等调度框架做好之后再上」。**拦阻已经解除**，现在是「等 pipeline-runner 调度框架就位后接上」。  另外你说的「知秋重新设计了」，确认就是 `03.dwm…

[assistant] [REDACTED]

[assistant] 正在读 spec 与指纹字段，以便给出可执行的 reply。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 已 ACK 并 reply 结案（bus#7739）。

**狂人交底要点（已对齐）：**

1. **#7735 更正**：08-04「不上 prod」作废；08-11「捡回来」+ 今晚知秋确认——等 pipeline-runner 就位后接上。拦阻已解除。
2. **硬约束**：主键/join 一律 **`device_fingerprint`**（64 位，不看 fp_version）；`device_id` 只能作附属列。六张 dwm + dim + dws + uid_map 全链改键。
3. **现状**：paimon 建齐但停在 **07-27**；enroll 闸门从未生效（15.7 亿=全灌）；`dwm_device_uid_map_d` **只有 spec，无 SQL**。
4. **交付节奏**：三阶段——**先出方案你审 → 过了再动手**。方案须答五问：指纹替换清单+覆盖率、`_r` 50 天保留口径、enroll 闸门+人口预估、uid_map 从零 SQL、pipeline-runner step 定义。

**又初承诺**：方案初稿 **明日下班前** bus 交狂人 review（含指纹覆盖率探针结果）；`_r` 保留期与 enroll 细则在方案里给选项+推荐，不默跑。

设备标签这条已从「等知秋原话」切到 **又初主责改造**，后续我会按方案阶段推进。

[REDACTED]

[user] <timestamp>Monday, Aug 31, 2026, 10:46 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7742 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7742 --text "[ACK] bus#7742 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7742 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：值班 SOP + 事故处置授权（狂人 · 同步自 TG 群）。⚠️ 最后那条授权直接回答了你一直在等的「直接修复半径」，务必看到底。  知秋刚在群里安排你和牡丹晚上值班，说不懂事故处理流程的问我。下面六条是判据。  ═══ 六条判据（都是今天实战踩出来的）═══  1）先分 env。env=test 的「数据不新鲜/断流/僵尸 wf」告警一律不处理、不查、不补数。只有 env=prod 才按事故走。  2）收到告警第一个问题是「此刻还在发生吗」，不是「为什么失败」。 ⚠️ server_monitor 推的告警时间戳比 DS 库快 1 小时，看着像刚刚，其实是一小时前的滞后播报。必须回 DS 库查 state=…

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 已 ACK 并 reply 结案（bus#7743）。

**值班 SOP 六条（prod 事故判据）：**

1. **先分 env** — test 的数据不新鲜/断流/僵尸 wf **一律不处理**；只有 **prod** 走事故流程  
2. **先问「此刻还在发生吗」** — server_monitor 时间戳比 DS **快 1 小时**；须回 DS 查 **state=1** 实例数  
3. **DEPENDENT /「等_xxx」是果** — 只追第一个 **state=FAILURE** 的真 SQL task；state=9 KILL 是连带，白看  
4. **禁信 server_monitor「根因」** — 用 `download-log?taskInstanceId=` 拉全日志；**不用** `/log/detail`（会截断）  
5. **行数涨跌** — 跟**前一日同小时**比，不跟上一分区  
6. **可能已自愈** — 看**当时**实例/日志，不看事后 SR 一切正常  

**两坑：** 排查期不反复 fire 同一失败 task；查数不用 tail，用 head / WHERE  

---

**直接修复半径（#7708 悬案已结）：**

知秋原话：**「出事故能处理的立马处理。还代码也处理。不能等。」**

| 场景 | 动作 |
|------|------|
| **prod + 判为事故** | 立即处置，**含改代码**；处理完再报，事故窗口内不停下来请示 |
| **非事故** | 日常 prod 变更、口径/设计、加表加字段 → 照旧等知秋 GO |

关键：**先用六条判是不是事故**；不确定先查清楚，不拿模糊判断套授权。

与 **#7738 设备标签**交底分开，不混。今晚跟牡丹搭班值班，prod…
```
