# TG 会话轮换归档 · 2026-08-10-0914

- 原因: daily(06:00)
- session_id: ses_1786238844059_m7ubcw
- cursor_chat_id: 8fdd9a07-d604-4690-a9a8-dbfa26f02525
- 蒸馏: AI+heuristic

## 携带要点

- 用户指出「拦截/说明」类问题时：先改脚本或流程堵住根因，再直接推进任务，禁止反复复读同一段拦截话术
- `app_2556` 源表 0 行会导致「任务成功但无产出」的假成功；已在 `run_yarn_daily_sql.sh` 硬拒绝该 app，后续 Yarn 日跑勿再误传
- 防踩坑文档应落在 `spark/README.md` + 会话 `memory.md`：写清冒烟命令、默认 app、下一步清单，避免下轮重踩
- 验数通过后再 commit 推仓，并推进 dev-session stage4；不要跳过 spot-check 直接上全量压测
- TG 群/旁听：仅被显式 `@youchu_ai_bot / @youchu8888 / @又初 / @初儿` 时才回复；裸提名字或只 @ 他人时直接不回，也不要声明「我不插嘴」
- [LESSON: agent-communication,feedback|收到「别一直发拦截了」类指摘时，立刻改工具/规则并继续干活，禁止重复发送同类说明]
- 大漏斗两阶段 ETL 顺序固定为 `metrics → wide`；冒烟默认用 `SF-81`，指定 `dt` 先跑通再扩 profile
- `stage_metrics` 是墙钟瓶颈（样例 ~6.3h），`stage_wide` 通常很快（样例 ~17s）；全量压测前先盯 metrics 阶段是否可接受
- 冒烟验收可看行数结构：宽表 `is_new = -1/0/1` 各 1 行（共 3 行），stg 约 39 行；OOM 未出现才算链路基本可用
- 全量 `M` profile 压 T-1 之前，先按 `playbook.md` 做 SF-81 口径 spot-check（如 `video_view` 大小写、`page_view` 过滤）

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-10-yarn-日跑脚本须拦截已知-0-行源表的-app-如-app_2556-否则-exit-0-也.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-10-收到-别一直发拦截了-类指摘时-立刻改工具-规则并继续干活-禁止重复发送同类说明.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-10-大漏斗扩全量前先用-sf-81-按-playbook-做口径-spot-check-宽表-is_.md`

## AI 蒸馏原文（截断）

```
- 用户指出「拦截/说明」类问题时：先改脚本或流程堵住根因，再直接推进任务，禁止反复复读同一段拦截话术
- 大漏斗两阶段 ETL 顺序固定为 `metrics → wide`；冒烟默认用 `SF-81`，指定 `dt` 先跑通再扩 profile
- `stage_metrics` 是墙钟瓶颈（样例 ~6.3h），`stage_wide` 通常很快（样例 ~17s）；全量压测前先盯 metrics 阶段是否可接受
- 冒烟验收可看行数结构：宽表 `is_new = -1/0/1` 各 1 行（共 3 行），stg 约 39 行；OOM 未出现才算链路基本可用
- `app_2556` 源表 0 行会导致「任务成功但无产出」的假成功；已在 `run_yarn_daily_sql.sh` 硬拒绝该 app，后续 Yarn 日跑勿再误传
- 防踩坑文档应落在 `spark/README.md` + 会话 `memory.md`：写清冒烟命令、默认 app、下一步清单，避免下轮重踩
- 全量 `M` profile 压 T-1 之前，先按 `playbook.md` 做 SF-81 口径 spot-check（如 `video_view` 大小写、`page_view` 过滤）
- 验数通过后再 commit 推仓，并推进 dev-session stage4；不要跳过 spot-check 直接上全量压测
- TG 群/旁听：仅被显式 `@youchu_ai_bot / @youchu8888 / @又初 / @初儿` 时才回复；裸提名字或只 @ 他人时直接不回，也不要声明「我不插嘴」

[LESSON: yarn-spark,datacheck,anti-pattern|Yarn 日跑脚本须拦截已知 0 行源表的 app（如 app_2556），否则 exit 0 也会形成假成功]
[LESSON: agent-communication,feedback|收到「别一直发拦截了」类指摘时，立刻改工具/规则并继续干活，禁止重复发送同类说明]
[LESSON: etl-validation,funnel|大漏斗扩全量前先用 SF-81 按 playbook 做口径 spot-check，宽表 is_new 三档各 1 行 + stg 行数对齐后再排 M 压测]
```

## 会话摘录（截断）

```
[user] [私聊#286] 有问题处理问题，不要一直重复发这个拦截了

[assistant] 收到。我先看大漏斗目录当前状态，接着直接推进，不再重复发拦截说明。
接下来补两处防踩坑改动：冒烟默认用 `SF-81`，并在脚本里拦截误传 `app_2556`。
大漏斗这边已经接上，不再复读拦截说明。

**当前状态（r5b 已闭环）**
- 两阶段 ETL（metrics → wide）在 SF-81、dt=2026-08-03 跑通，无 OOM
- 宽表 3 行（is_new = -1/0/1 各 1），stg 39 行
- stage_metrics ~6.3h，stage_wide ~17s

**刚补的两处**
1. `run_yarn_daily_sql.sh` 会直接拒绝 `app_2556`（源表 0 行，之前 r5 假成功的根因）
2. `spark/README.md` / `memory.md` 写了冒烟命令和下一步清单

**接下来**
1. SF-81 按 `playbook.md` 做口径 spot-check（video_view 大小写、page_view 过滤等）
2. `M` profile 全 app 压一轮 T-1，看 stage_metrics 墙钟是否可接受
3. 验数通过后 commit 推仓，推进 dev-session stage4

你要我先做 spot-check SQL，还是直接排全量 `M` 压测？

[user] <timestamp>Sunday, Aug 9, 2026, 9:28 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-09 09:27（周日），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 多渠道统一上下文

含群旁听、私聊、agent-bus。判断要不要回：**仅显式 @youchu_ai_bot / @youchu8888 / @又初 / @初儿** 时在群里回复；裸写「又初」「初儿」或只 @ 别人 → 只作背景，**不要声明「我不插嘴」**——直接不回。

# TG / Cursor / agent-bus 统一上下文（最近消息，与 IDE 同一工作区）

**📥 收到** / **👁 旁听** / **📤 又初回复** 分块展示。

👁 **旁听** · `2026-08-08 10:13:14` · telegram_group
**发言** @hull1889
> [群聊·@hull1889]: @worker_ant_bot 你带猫猫吧项目日报做吧，给猫猫派任务

📥 **收到** · `2026-08-08 10:15:43` · uid=0 · worker_ant_group
**发言** @worker_ant_bot
> [工作狂人] 收到，队列改过来了，项目日报排第一，coin-001 押后。已经派给猫猫（bus #6252），一次就这一件。

派的是：新开一个 session，只改 ads_product_day_stat_d.sql 第 288~299 行的 video_play 段，从 dwd 换成 dwm。别的一行都不动。

这一改同时解决两件事：
1. 性能 —— 这个 task 今早 06:31 跑…

[assistant] 收到。我先看大漏斗目录当前状态，接着直接推进，不再重复发拦截说明。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 接下来补两处防踩坑改动：冒烟默认用 `SF-81`，并在脚本里拦截误传 `app_2556`。

[REDACTED]

[assistant] [REDACTED]

[assistant] 大漏斗这边已经接上，不再复读拦截说明。

**当前状态（r5b 已闭环）**
- 两阶段 ETL（metrics → wide）在 SF-81、dt=2026-08-03 跑通，无 OOM
- 宽表 3 行（is_new = -1/0/1 各 1），stg 39 行
- stage_metrics ~6.3h，stage_wide ~17s

**刚补的两处**
1. `run_yarn_daily_sql.sh` 会直接拒绝 `app_2556`（源表 0 行，之前 r5 假成功的根因）
2. `spark/README.md` / `memory.md` 写了冒烟命令和下一步清单

**接下来**
1. SF-81 按 `playbook.md` 做口径 spot-check（video_view 大小写、page_view 过滤等）
2. `M` profile 全 app 压一轮 T-1，看 stage_metrics 墙钟是否可接受
3. 验数通过后 commit 推仓，推进 dev-session stage4

你要我先做 spot-check SQL，还是直接排全量 `M` 压测？

[REDACTED]
```
