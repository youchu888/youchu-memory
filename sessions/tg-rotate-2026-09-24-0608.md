# TG 会话轮换归档 · 2026-09-24-0608

- 原因: daily(06:00)
- session_id: ses_1790129306524_u5mpvo
- cursor_chat_id: 0aaf0a6f-3a09-48e2-bb14-6adbdd4e60f1
- 蒸馏: AI+heuristic

## 携带要点

- 修复要点：失败不占 20h 冷却；旧证复用不能当续期成功；续不上应报错/红灯，禁止假绿灯。
- [LESSON: vpn-sync,ovpn,launchd|仅当 ovpn 成功导入且 `imported_at` 更新后再写冷却；失败清冷却或勿写；过期旧证禁止「复用跳过导入」并打 ✅]
- [LESSON: cursor-agent|连接失败丢弃 resume 时勿假设上下文还在，须冷启动记忆后重跑或让用户重发/新开 agent]
- VPN「没自动续」先查 launchd 是否在跑；常见是脚本假成功，不是定时任务停了。
- 续期链路：`/request` → 生成 ovpn → 导入 → 拉起 OpenVPN；任何一步失败都不应记成「已续期成功」。
- **假成功模式**：失败仍写入 20h 冷却 → 后续轮询「距上次 /request 不足 20h」→ 复用本地旧 ovpn → 跳过导入 → 日志打 ✅，证书其实已过期。
- 判因要看「上次**真正导入**时间」（如 `imported_at`），不能只看「上次尝试过 /request」的时间戳。
- 卡死时：查完整 `/request` 日志 + 本地 ovpn 日期，先强制续一次，再改脚本逻辑。
- GitLab 命令行用 Token：网页建 PAT → 域名级代理（如需）→ `remote` 改 HTTPS → 钥匙串存凭证 → `git ls-remote` 验收。
- 对外说明**只写步骤**，不带任何真实 token；对方用自己账号生成 PAT。
- 私聊/bus 发失败或 Cursor「会话连接失败、丢弃旧 resume」时，改发机器人群（如 `@mudan99_bot`），并让用户重发或「重启 agent」。
- 同一 howto 可多渠道：先前 bus 已发过，仍可按主人要求再发群，内容保持一致、不含密钥。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-24-仅当-ovpn-成功导入且-imported_at-更新后再写冷却-失败清冷却或勿写-过期旧证禁.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-24-git-https-token-说明可发机器人群-正文零真实凭证-验收用-ls-remote.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-24-连接失败丢弃-resume-时勿假设上下文还在-须冷启动记忆后重跑或让用户重发-新开-agent.md`

## AI 蒸馏原文（截断）

```
- VPN「没自动续」先查 launchd 是否在跑；常见是脚本假成功，不是定时任务停了。
- 续期链路：`/request` → 生成 ovpn → 导入 → 拉起 OpenVPN；任何一步失败都不应记成「已续期成功」。
- **假成功模式**：失败仍写入 20h 冷却 → 后续轮询「距上次 /request 不足 20h」→ 复用本地旧 ovpn → 跳过导入 → 日志打 ✅，证书其实已过期。
- 判因要看「上次**真正导入**时间」（如 `imported_at`），不能只看「上次尝试过 /request」的时间戳。
- 卡死时：查完整 `/request` 日志 + 本地 ovpn 日期，先强制续一次，再改脚本逻辑。
- 修复要点：失败不占 20h 冷却；旧证复用不能当续期成功；续不上应报错/红灯，禁止假绿灯。
- GitLab 命令行用 Token：网页建 PAT → 域名级代理（如需）→ `remote` 改 HTTPS → 钥匙串存凭证 → `git ls-remote` 验收。
- 对外说明**只写步骤**，不带任何真实 token；对方用自己账号生成 PAT。
- 私聊/bus 发失败或 Cursor「会话连接失败、丢弃旧 resume」时，改发机器人群（如 `@mudan99_bot`），并让用户重发或「重启 agent」。
- 同一 howto 可多渠道：先前 bus 已发过，仍可按主人要求再发群，内容保持一致、不含密钥。

[LESSON: vpn-sync,ovpn,launchd|仅当 ovpn 成功导入且 `imported_at` 更新后再写冷却；失败清冷却或勿写；过期旧证禁止「复用跳过导入」并打 ✅]
[LESSON: git,collab,tg|Git HTTPS+Token 说明可发机器人群；正文零真实凭证，验收用 `ls-remote`]
[LESSON: cursor-agent|连接失败丢弃 resume 时勿假设上下文还在，须冷启动记忆后重跑或让用户重发/新开 agent]
```

## 会话摘录（截断）

```
[user] [私聊#511] 把git配置的token的方式bus给花儿

[assistant] ❌ Cursor 会话连接失败（已自动丢弃旧 resume）。请重发刚才那句；或发「重启 agent」强制新开。

[user] [私聊#512] vpn怎么没有自动续

[user] [新开agent·并行] [私聊#513] 发机器人群 里吧，把配置token的方式
（系统：长任务占着另一路 agent；本条独立处理，须先读记忆冷启动。）

[assistant] 私聊#512：先查 VPN 自动续期的 launchd 状态、最近一次跑没跑、失败原因。根因已露头：定时在跑，但卡在「要续期却复用旧 ovpn、又跳过导入」。继续核对状态文件和请求逻辑。定位到卡死逻辑了：今天发过 `/request`，但本地仍是 9/22 的旧 ovpn，被当成「已导入」跳过。正在查那次请求的完整日志并强制续一次。根因清楚了：失败的 `/request` 仍写入了冷却时间戳，后续轮询误以为刚请求过，一直复用旧证书。正在强制续期并修这个逻辑。修复两处逻辑漏洞，并立刻强制续期。定时任务一直在跑，不是停了。卡在逻辑 bug：

**原因**
- 上次真正导入是 **9/22 11:28**，今天 10:28 就该续
- 但 9/22 18:00 有一次 `/request` 失败（Bot 生成超时），脚本却先记了「已请求」
- 今天 14:06 到点再请求时 TG 连不上，又记了一次冷却
- 之后每 30 分钟都报「距上次 /request 不足 20h → 复用旧 ovpn → 跳过导入 → ✅」，其实是**假成功**

**已处理**
1. 刚强制续成功：新证已导入并拉起 OpenVPN（`imported_at` ≈ 20:51）
2. 修了脚本：失败不再占 20h 冷却；旧证不能当续期成功；真续不上会报错而不是假绿灯

下次计划约 **明天 19:51** 前自动续。若又断，说一声我再看。

[assistant] 理解为：把「git 配 token」的说明发到机器人群（#511 给花儿失败后改群发）。先查现成文档和发群方式，再发出去。先前 #511 那次会话也在查同一件事，接着把它查到哪一步、以及本机真实的 token 配法对上。#511 那次其实已整理出 GitLab HTTPS+Token 步骤并声称发了 bus#9188。我核对 bus 是否真发出，再把同一份说明发到机器人群。howto 已齐（此前 bus#9188 给过花儿）。正在发到机器人群。已发到机器人群（`@mudan99_bot`，msg_id=10082）。

内容就是 GitLab 命令行用 Personal Access Token 的配法：网页建 token → 域名级代理 → remote 改 HTTPS → 钥匙串存凭证 → `ls-remote` 验收。**没有带任何真实 token**，让花儿用自己账号生成。

先前 #511 其实已 bus#9188 给过花儿；这次按你说的改发群。

[user] <timestamp>Wednesday, Sep 23, 2026, 8:49 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-23 20:48（周三），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-23 20:29:45 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/d…

[assistant] 私聊#512：先查 VPN 自动续期的 launchd 状态、最近一次跑没跑、失败原因。

[assistant] 根因已露头：定时在跑，但卡在「要续期却复用旧 ovpn、又跳过导入」。继续核对状态文件和请求逻辑。

[assistant] 定位到卡死逻辑了：今天发过 `/request`，但本地仍是 9/22 的旧 ovpn，被当成「已导入」跳过。正在查那次请求的完整日志并强制续一次。

[assistant] 根因清楚了：失败的 `/request` 仍写入了冷却时间戳，后续轮询误以为刚请求过，一直复用旧证书。正在强制续期并修这个逻辑。

[assistant] 修复两处逻辑漏洞，并立刻强制续期。

[assistant] 定时任务一直在跑，不是停了。卡在逻辑 bug：

**原因**
- 上次真正导入是 **9/22 11:28**，今天 10:28 就该续
- 但 9/22 18:00 有一次 `/request` 失败（Bot 生成超时），脚本却先记了「已请求」
- 今天 14:06 到点再请求时 TG 连不上，又记了一次冷却
- 之后每 30 分钟都报「距上次 /request 不足 20h → 复用旧 ovpn → 跳过导入 → ✅」，其实是**假成功**

**已处理**
1. 刚强制续成功：新证已导入并拉起 OpenVPN（`imported_at` ≈ 20:51）
2. 修了脚本：失败不再占 20h 冷却；旧证不能当续期成功；真续不上会报错而不是假绿灯

下次计划约 **明天 19:51** 前自动续。若又断，说一声我再看。
```
