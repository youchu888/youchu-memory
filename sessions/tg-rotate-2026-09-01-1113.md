# TG 会话轮换归档 · 2026-09-01-1113

- 原因: max_transcript(438KB>=400KB)
- session_id: ses_1788215213418_n7gtwl
- cursor_chat_id: 54894139-10f9-485d-a06c-d7da9259b990
- 蒸馏: AI+heuristic

## 携带要点

- prod 盯盘口径是**告警驱动**，不是又初另起一套夜间全量巡检；检测已在 `server_monitor`（54.255.236.159），禁止再造监控
- 长任务按主人要求**定时私聊汇报**；卡点一次性列清选项私聊拍板，不要边做边猜
- [LESSON: device-fingerprint|设备标签/uid_map 主键用 device_fingerprint，无指纹丢弃；改造顺序 uid_map→dim/dwm/dws，以 bus#7738 覆盖旧 prod 禁令]
- 收到 **env=prod** 告警后固定链路：先验「此刻还在不在」→ 定位真失败 SQL → `download-log` 判因 → 从 playbook `part_01` 起处置
- bus#7742 事故授权：**确认是事故可立刻修（含改代码），修完再报**；日常非事故变更仍等知秋 GO
- 值班安排是又初+牡丹搭班，狂人下发六条判据与修复半径；没接到告警就不动，但告警漏接算值班未站稳
- 约定变更后须同步改 `MEMORY_OPEN` / playbook / feedback，并推到 youchu-memory 供双机拉齐
- Spark 加任务（bus#7735）：**只写 SQL + `steps.json` 挂槽位，不改 Scala**；源表用 `_r`、数值列入口 CAST、`tagTargets` 必填、`${DT}`/`${OUT_DB}`、行为键 `UPPER(TRIM)`
- 挂槽改造先用沙箱 `steps/sandbox_steps_fragment.json`，**未验证前别动 prod `full_chain.json`**
- 设备标签（bus#7738）：主键改 **`device_fingerprint`**，`device_id` 仅附属；无指纹行丢弃；拦阻已解除，等 pipeline-runner 接上
- bus#7738 覆盖了 #7735 里「先不上 prod」的过期说法，以较新交底为准
- 多步改造默认顺序：先落地缺的 **`uid_map`** → 再改 dim / 六张 dwm / dws 宽表 PK
- dim 重建若 `_r` 只保留约 50 天，07-13 前沉默设备的 `last_login_time` / `last_active_time` 需业务拍板（置空 / 保留旧 dim / 只认近 50 天）

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-告警驱动处置-prod-告警-验还在-找失败-sql-download-log-确认事故可立刻修.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-spark-新任务只写-sql-steps-json-挂槽不改-scala-源-_r-cast-.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-设备标签-uid_map-主键用-device_fingerprint-无指纹丢弃-改造顺序-u.md`

## AI 蒸馏原文（截断）

```
- prod 盯盘口径是**告警驱动**，不是又初另起一套夜间全量巡检；检测已在 `server_monitor`（54.255.236.159），禁止再造监控
- 收到 **env=prod** 告警后固定链路：先验「此刻还在不在」→ 定位真失败 SQL → `download-log` 判因 → 从 playbook `part_01` 起处置
- bus#7742 事故授权：**确认是事故可立刻修（含改代码），修完再报**；日常非事故变更仍等知秋 GO
- 值班安排是又初+牡丹搭班，狂人下发六条判据与修复半径；没接到告警就不动，但告警漏接算值班未站稳
- 约定变更后须同步改 `MEMORY_OPEN` / playbook / feedback，并推到 youchu-memory 供双机拉齐
- Spark 加任务（bus#7735）：**只写 SQL + `steps.json` 挂槽位，不改 Scala**；源表用 `_r`、数值列入口 CAST、`tagTargets` 必填、`${DT}`/`${OUT_DB}`、行为键 `UPPER(TRIM)`
- 挂槽改造先用沙箱 `steps/sandbox_steps_fragment.json`，**未验证前别动 prod `full_chain.json`**
- 设备标签（bus#7738）：主键改 **`device_fingerprint`**，`device_id` 仅附属；无指纹行丢弃；拦阻已解除，等 pipeline-runner 接上
- bus#7738 覆盖了 #7735 里「先不上 prod」的过期说法，以较新交底为准
- 多步改造默认顺序：先落地缺的 **`uid_map`** → 再改 dim / 六张 dwm / dws 宽表 PK
- 长任务按主人要求**定时私聊汇报**；卡点一次性列清选项私聊拍板，不要边做边猜
- dim 重建若 `_r` 只保留约 50 天，07-13 前沉默设备的 `last_login_time` / `last_active_time` 需业务拍板（置空 / 保留旧 dim / 只认近 50 天）

[LESSON: prod-monitor|告警驱动处置：prod 告警→验还在→找失败 SQL→download-log；确认事故可立刻修，非事故变更等知秋 GO]
[LESSON: spark-steps|Spark 新任务只写 SQL+steps.json 挂槽不改 Scala；源 `_r`、CAST、`tagTargets` 必填；先沙箱 fragment，别直接改 prod full_chain]
[LESSON: device-fingerprint|设备标签/uid_map 主键用 device_fingerprint，无指纹丢弃；改造顺序 uid_map→dim/dwm/dws，以 bus#7738 覆盖旧 prod 禁令]
```

## 会话摘录（截断）

```
[user] [私聊#431] 昨晚盯prod环境了吗

[assistant] 先核实昨晚是否有 prod 盯盘/告警处置记录，再据实回复。没有。昨晚（8/31）只收到值班安排和 SOP（bus#7742，约 22:45），playbook 已落盘；之后没有通宵盯盘记录，也没有处置过 prod 告警。

今天早上若要补一眼当前 prod 是否挂着，说一声我按 SOP 先看「此刻还在不在」。

[user] [私聊#432] 为什么不盯盘呢 你和狂人是怎么约定的

[assistant] 核对与狂人关于盯盘/值班的约定原文，再据实说明。和狂人的约定不是「通宵主动扫全盘」，是**告警驱动处置**：

- 检测已在 `server_monitor`（54.255.236.159），明确说了**别再造一套监控**
- 又初这边：收到 **env=prod** 告警 → 先验「此刻还在不在」→ 再找真失败 SQL → download-log 判因
- 写操作原先要请示；昨晚 bus#7742 知秋补了：**确认是事故就能马上修（含改代码），修完再报**

昨晚另有安排：知秋让我和牡丹搭班值班，狂人把六条判据 + 修复半径发到 bus#7742。  
我当时按「有告警才动」理解了，没有主动通宵巡检；若有推送没及时接住，是值班没站稳，这点算我这边没做到。

要改成「定时主动扫一轮」也可以，你定口径我按那个做。

[user] [私聊#433] 按照狂人的安排来吧

[assistant] 主人确认按狂人约定执行。正在把 MEMORY_OPEN / playbook 里过时的「写操作先请示」改成与 bus#7742 事故授权一致。好，按狂人安排执行：

· 不另建夜间全量扫  
· 收到 **prod** 告警 → playbook part_01 起处置  
· 确认事故 → 立刻修（含改代码），修完再报  
· 日常非事故变更仍等知秋 GO  

已写回 playbook / MEMORY_OPEN。之后有告警我按这套接。盯盘约定相关记忆已同步到 youchu-memory（含 playbook 更新与 feedback），双机可拉到。

[user] [私聊#434] spark相关的问题昨天收到狂人的回复了吧

[assistant] 核对昨晚 Spark 相关 bus 回执是否已收到。收到了。昨晚狂人回了两条：

· **bus#7735**（约 22:08）：Spark 调度怎么加任务——写 SQL + `steps.json` 挂槽位，不改 Scala；并带了一堆硬规矩（源表用 `_r`、数值列 CAST、`tagTargets` 必填等）  
· **bus#7738**（约 22:32）：设备标签交底——主键改 `device_fingerprint`，拦阻已解除，等 pipeline-runner 接上；并更正了 #7735 里「先不上 prod」的过期说法  

大漏斗按他给的调度写法接；设备标签按 #7738 那条线走。

[user] [私聊#435] 按照规则去改这两个任务吧，有卡点私聊询问我，定时汇报进度

[user] <timestamp>Tuesday, Sep 1, 2026, 9:26 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-01 09:25（周二），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-01 06:26:28 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/data…

[assistant] 先核实昨晚是否有 prod 盯盘/告警处置记录，再据实回复。

[assistant] 没有。昨晚（8/31）只收到值班安排和 SOP（bus#7742，约 22:45），playbook 已落盘；之后没有通宵盯盘记录，也没有处置过 prod 告警。

今天早上若要补一眼当前 prod 是否挂着，说一声我按 SOP 先看「此刻还在不在」。

[user] <timestamp>Tuesday, Sep 1, 2026, 9:56 AM (UTC+8)</timestamp>
<user_query>
[私聊#432] 为什么不盯盘呢 你和狂人是怎么约定的
</user_query>

[assistant] 核对与狂人关于盯盘/值班的约定原文，再据实说明。

[assistant] 和狂人的约定不是「通宵主动扫全盘」，是**告警驱动处置**：

- 检测已在 `server_monitor`（54.255.236.159），明确说了**别再造一套监控**
- 又初这边：收到 **env=prod** 告警 → 先验「此刻还在不在」→ 再找真失败 SQL → download-log 判因
- 写操作原先要请示；昨晚 bus#7742 知秋补了：**确认是事故就能马上修（含改代码），修完再报**

昨晚另有安排：知秋让我和牡丹搭班值班，狂人把六条判据 + 修复半径发到 bus#7742。  
我当时按「有告警才动」理解了，没有主动通宵巡检；若有推送没及时接住，是值班没站稳，这点算我这边没做到。

要改成「定时主动扫一轮」也可以，你定口径我按那个做。

[user] <timestamp>Tuesday, Sep 1, 2026, 10:52 AM (UTC+8)</timestamp>
<user_query>
[私聊#433] 按照狂人的安排来吧
</user_query>

[assistant] 主人确认按狂人约定执行。正在把 MEMORY_OPEN / playbook 里过时的「写操作先请示」改成与 bus#7742 事故授权一致。

[assistant] 好，按狂人安排执行：

· 不另建夜间全量扫  
· 收到 **prod** 告警 → playbook part_01 起处置  
· 确认事故 → 立刻修（含改代码），修完再报  
· 日常非事故变更仍等知秋 GO  

已写回 playbook / MEMORY_OPEN。之后有告警我按这套接。

[user] <timestamp>Tuesday, Sep 1, 2026, 10:55 AM (UTC+8)</timestamp>

<user_query>Briefly inform the user about the task result and perform any follow-up actions (if needed). If there's no follow-ups needed, don't explicitly say that.</user_query>

[assistant] 盯盘约定相关记忆已同步到 youchu-memory（含 playbook 更新与 feedback），双机可拉到。

[user] <timestamp>Tuesday, Sep 1, 2026, 11:00 AM (UTC+8)</timestamp>
<user_query>
[私聊#434] spark相关的问题昨天收到狂人的回复了吧
</user_query>

[assistant] 核对昨晚 Spark 相关 bus 回执是否已收到。

[assistant] 收到了。昨晚狂人回了两条：

· **bus#7735**（约 22:08）：Spark 调度怎么加任务——写 SQL + `steps.json` 挂槽位，不改 Scala；并带了一堆硬规矩（源表用 `_r`、数值列 CAST、`tagTargets` 必填等）  
· **bus#7738**（约 22:32）：设备标签交底——主键改 `device_fingerprint`，拦阻已解除，等 pipeline-runner 接上；并更正了 #7735 里「先不上 prod」的过期说法  

大漏斗按他给的调度写法接；设备标签按 #7738 那条线走。

[user] <timestamp>Tuesday, Sep 1, 2026, 11:03 AM (UTC+8)</timestamp>
<user_query>
[私聊#435] 按照规则去改这两个任务吧，有卡点私聊询问我，定时汇报进度
</user_query>

[assistant] 按 #7735/#7738 改大漏斗 Spark 挂槽与设备标签指纹键；先摸清现状与卡点，再动手，进度定时私聊汇报。

[user] <dynamic_tools>
You have access to tools through dynamic namespaces, e.g. MCP servers, using `GetDynamicTools` and `CallDynamicTool`.

## Dynamic Tool Discovery and Invocation

Use `GetDynamicTools` to discover tool schemas, then `CallDynamicTool` to invoke one tool. Aim to minimize round-trips: ideally one discovery call followed by one invocation.

If the user mentions a product or service represented by an available namespace, and the request likely depends on it, proactively inspect that namespace before answering. If you are unsure which namespace matches, search with a relevant pattern.

`GetDynamicTools` supports these modes:

1. `{"namespace":"<id>"}`: returns schemas and full descriptions for every tool in that namespace.
2. `{"namespace":"<id>","toolName":"<name>"}`: returns one …

[user] <timestamp>Tuesday, Sep 1, 2026, 11:03 AM (UTC+8)</timestamp>
<user_query>
[私聊#435] 按照规则去改这两个任务吧，有卡点私聊询问我，定时汇报进度
</user_query>

[assistant] 已按 #7735 / #7738 开工，进度如下。13:00 / 15:00 / 17:00 / 19:00 会继续私聊汇报。

### 大漏斗（挂框架）
- stage SQL 已按硬规矩改：源切 `_r`、`${DT}`、`${OUT_DB}`、数值列入口 CAST、行为键 `UPPER(TRIM)`
- 新增沙箱 `steps/sandbox_steps_fragment.json`（**未动** prod `full_chain.json`）
- README 补了 pipeline-runner 挂槽说明；旧单文件标为 legacy

### 设备标签 / uid_map（#7738）
- `dwm_device_uid_map_d` DDL + daily：**PK → `device_fingerprint`**，`device_id` 仅附属列；无指纹行丢弃
- 沙箱 steps 片段已写
- 8 月 register 指纹规模摸底：约 **2356 万对 / 1722 万指纹**（远小于 device_id 口径的 12 亿）

---

### 卡点请拍板（私聊回这三条即可）

1. **dim 重建时** login / page_view `_r` **只约 50 天保留**：07-13 前沉默设备的 `last_login_time` / `last_active_time` 怎么定？  
   - A 置空 · B 保留旧 dim 值（读停更非 `_r`）· C 只认近 50 天
2. **改造顺序**：先落地 `uid_map`（真正缺表）→ 再改 dim / 六张 dwm / dws 宽表 PK——是否同意？
3. 沙箱 **explain**：我直接 SSH 集群跑，还是 SQL 先交狂人 review 再挂？

覆盖率低按 #7738 *…
```
