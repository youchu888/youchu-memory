# TG 会话轮换归档 · 2026-08-01-1002

- 原因: daily(06:00)
- session_id: ses_1785451226608_3r313b
- cursor_chat_id: 8786a48e-39ec-40af-895e-4c8e03fcb2b3
- 蒸馏: AI+heuristic

## 携带要点

- 停留时长链路改审核人时，要扫**同链路所有 pending 的 request-publish**，不要只改当前 session；`dev-20260729-002` / `dev-20260711-002` / `dev-20260716-001` 三处曾一并从野花改到蓝猫（`hull367660@gmail.com`）。
- 他人已在 prod 发版后本地跟 session：**先 GET 服务端最新 state，再 merge PUT**；禁止整包覆盖，避免冲掉狂人写的 prod 信息。
- 2026-07 prod 结论：30 业务日全覆盖、分档符合设计、07-30 与上游对账无差；本地大改后**未 commit**，推 `origin/dev` 需主人明示。
- [LESSON: dev-session|session 被 admin 用 fix-metadata 摘表后，本地须同步改 task.yaml、文档口径，设备文件移 `_parked_*` 并为摘出范围新建独立 session，勿把已摘表推回 PUT `/full`]
- 已是 **approved** 的旧 session（如 `dev-20260711-001` DWD page_stay）不会随 pending 批量改审；若 prod 发版也要换人，需**单独处理**。
- `dev-20260729-002` 已收敛为**单目标表** `dws.dws_session_duration_user_d`；设备侧 `dws_session_duration_device_d` 从 outputs / `dolphin_owned_tasks` 摘掉，本地设备文件**移入** `ops_system/04.dws/_parked_session_duration_device_d/`，不要直接删。
- 设备 DWM 独立成新 session **`dev-20260731-001`**（`job_dwm_app_session_sid_device_d`）；用户侧 session 的 `task.yaml` / spec / README / playbook / design / memory 都要改成**单表口径**。
- 平台 session 同步用 **PUT `/full`**，并更新 `related_tables`、`title`、`target.note`；设备侧标 **`stage5.upstream_device=OUT_OF_SCOPE_SEPARATE_SESSION`**。
- prod 海豚已挂：`运营系统` → `wf_dws_汇总_日` → **task_code `180283360953472`**，链尾在 `dws_ad_request_metric_d` 之后；`task_user.yaml` 要补 prod task 绑定。
- DDL 对齐 prod：`start=-10000`、`history_partition_num=30`；`design.md` 补 §10 字段字典、§11 口径说明、§12 prod 发布记录。
- `dolphin_owned_tasks` 可 **test + prod 两条并存**；`related_tables` 仍只保留 user 表。
- prod 验 `dws.dws_session_duration_user_d` 本月：看**分区覆盖**（T-1 下当月最新业务日 0 行正常）、**缺天**、日量级波动。
- 质量抽查：**daily 无 `duration_bucket=0`**（应为 1~5）；**session 为 0~5**；T-1 日与上游 `dwm_app_session_sid_d` 对 `SUM(session_cnt)` / bucket 行数。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-01-session-被-admin-用-fix-metadata-摘表后-本地须同步改-task-y.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-01-验用户停留时长-默认-t-1-查当月分区覆盖-daily-无-bucket0-session-0.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-01-改链路审核人时扫同链路全部-pending-rp-approved-旧-session-不自动跟.md`

## AI 蒸馏原文（截断）

```
- 停留时长链路改审核人时，要扫**同链路所有 pending 的 request-publish**，不要只改当前 session；`dev-20260729-002` / `dev-20260711-002` / `dev-20260716-001` 三处曾一并从野花改到蓝猫（`hull367660@gmail.com`）。
- 已是 **approved** 的旧 session（如 `dev-20260711-001` DWD page_stay）不会随 pending 批量改审；若 prod 发版也要换人，需**单独处理**。
- `dev-20260729-002` 已收敛为**单目标表** `dws.dws_session_duration_user_d`；设备侧 `dws_session_duration_device_d` 从 outputs / `dolphin_owned_tasks` 摘掉，本地设备文件**移入** `ops_system/04.dws/_parked_session_duration_device_d/`，不要直接删。
- 设备 DWM 独立成新 session **`dev-20260731-001`**（`job_dwm_app_session_sid_device_d`）；用户侧 session 的 `task.yaml` / spec / README / playbook / design / memory 都要改成**单表口径**。
- 平台 session 同步用 **PUT `/full`**，并更新 `related_tables`、`title`、`target.note`；设备侧标 **`stage5.upstream_device=OUT_OF_SCOPE_SEPARATE_SESSION`**。
- 他人已在 prod 发版后本地跟 session：**先 GET 服务端最新 state，再 merge PUT**；禁止整包覆盖，避免冲掉狂人写的 prod 信息。
- prod 海豚已挂：`运营系统` → `wf_dws_汇总_日` → **task_code `180283360953472`**，链尾在 `dws_ad_request_metric_d` 之后；`task_user.yaml` 要补 prod task 绑定。
- DDL 对齐 prod：`start=-10000`、`history_partition_num=30`；`design.md` 补 §10 字段字典、§11 口径说明、§12 prod 发布记录。
- `dolphin_owned_tasks` 可 **test + prod 两条并存**；`related_tables` 仍只保留 user 表。
- prod 验 `dws.dws_session_duration_user_d` 本月：看**分区覆盖**（T-1 下当月最新业务日 0 行正常）、**缺天**、日量级波动。
- 质量抽查：**daily 无 `duration_bucket=0`**（应为 1~5）；**session 为 0~5**；T-1 日与上游 `dwm_app_session_sid_d` 对 `SUM(session_cnt)` / bucket 行数。
- 2026-07 prod 结论：30 业务日全覆盖、分档符合设计、07-30 与上游对账无差；本地大改后**未 commit**，推 `origin/dev` 需主人明示。

[LESSON: dev-session|session 被 admin 用 fix-metadata 摘表后，本地须同步改 task.yaml、文档口径，设备文件移 `_parked_*` 并为摘出范围新建独立 session，勿把已摘表推回 PUT `/full`]

[LESSON: datacheck,dws_session_duration_user_d|验用户停留时长：默认 T-1；查当月分区覆盖 + daily 无 bucket0 + session 0~5 + 与 dwm_app_session_sid_d 对 session_cnt]

[LESSON: dev-session,request-publish|改链路审核人时扫同链路全部 pending RP；approved 旧 session 不自动跟随，prod 发版换人须单开处理]
```

## 会话摘录（截断）

```
[user] [私聊#257] 停留时长的发布人改为蓝猫吧

[assistant] 正在查找停留时长相关 session 的发布人配置。
停留时长链路上有 3 个 pending 的发布申请仍指向野花，正在一并改给蓝猫（`hull367660@gmail.com`）。
已改好。停留时长链路上 **3 个 pending 的 request-publish** 审核人都已从野花换成蓝猫（`hull367660@gmail.com`）：

| session | 表/环节 | 状态 |
|---------|---------|------|
| `dev-20260729-002` | DWS 五档合表（user/device） | pending → 蓝猫审 |
| `dev-20260711-002` | DWM sid 宽表 | pending → 蓝猫审 |
| `dev-20260716-001` | DWS 用户日停留汇总 | pending → 蓝猫审 |

补充：`dev-20260711-001`（DWD page_stay）之前已是 **approved + 野花**，这次没动。如果 prod 发版也要蓝猫接手，说一声我再单独处理这条。

[user] [私聊#258] 核对一下停留时长本月数据

[assistant] 正在 prod 上核对 `dws_session_duration_user_d` 本月（2026-07）分区覆盖与出数情况。
已在 **prod** 核对 `dws.dws_session_duration_user_d` 本月（2026-07）数据，结论如下。

## 覆盖情况

| 项 | 结果 |
|----|------|
| 有数据天数 | **30 天**（07-01 ~ 07-30） |
| 07-31 | **0 行**（日批 T-1，今天 31 号，最新业务日应是 30 号，正常） |
| 缺天 | **无**（1~30 日每天都有数） |

## 量级（prod 汇总）

| 指标 | 7 月合计 |
|------|----------|
| session 启动次数（`stat_grain=session`） | **8.68 亿** |
| daily 账号人次（`stat_grain=daily` 的 `user_cnt` 加总） | **3.97 亿** |
| 单日明细行数 | 约 **16.8 万 ~ 18.1 万** / 天（两 grain 合计） |

单日 session 启动量在 **2,580 万 ~ 3,196 万** 之间，环比最大波动约 **-9.7%**（07-06），在正常业务波动范围内。

## 质量抽查（07-30）

| 检查项 | 结果 |
|--------|------|
| daily 出现 `duration_bucket=0` | **0**（全月 daily 均无 bucket0） |
| session 分档 | bucket **0~5** |
| daily 分档 | bucket **1~5**（无 0 档，符合设计） |
| 与上游对账 | DWS `SUM(session_cnt)` = **29,982,556**，与 `dwm_app_session_sid_d` 当天 bucket 0~5 行数 **完全一致**（与狂人补数结论一致） |

## 结论

**7 月 prod 用户侧停留时长数据完整、质量正常**：30 个业务日全覆盖，分档规则符合设计，07-30 与上游对账无差。

如需再按某个 `app_id` 拆开看，或和 test 对比，说一声我接着查。

[assistant] 停留时长链路上有 3 个 pending 的发布申请仍指向野花，正在一并改给蓝猫（`hull367660@gmail.com`）。

[REDACTED]

[assistant] [REDACTED]

[assistant] 已改好。停留时长链路上 **3 个 pending 的 request-publish** 审核人都已从野花换成蓝猫（`hull367660@gmail.com`）：

| session | 表/环节 | 状态 |
|---------|---------|------|
| `dev-20260729-002` | DWS 五档合表（user/device） | pending → 蓝猫审 |
| `dev-20260711-002` | DWM sid 宽表 | pending → 蓝猫审 |
| `dev-20260716-001` | DWS 用户日停留汇总 | pending → 蓝猫审 |

补充：`dev-20260711-001`（DWD page_stay）之前已是 **approved + 野花**，这次没动。如果 prod 发版也要蓝猫接手，说一声我再单独处理这条。

[REDACTED]

[user] <timestamp>Friday, Jul 31, 2026, 4:47 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#5789 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 5789 --text "[ACK] bus#5789 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 5789 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：【狂人→又初】dev-20260729-002 已收敛为单目标表, 设备侧摘出 · 你需要跟着改 3 处  ■ 我做了什么 (知秋 2026-07-31 17:5x 钦定) 我用 admin 调 /dev-sessions/dev-20260729-002/fix-metadata, 把 outputs 和 dolphin_owned_tasks 里的 dws_session_duration_device_d 摘掉了, 现在这个 session 只剩 dws_session_duration_user_d 一张目标表。stage_status / task_version 都没动, 仍是 stage7 pend…

[assistant] 先 ack bus#5789，再核对 session 元数据与本地仓库，按狂人要求把设备侧从停留时长 session 摘掉。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] bus#5789 已处理并 reply 结案，按狂人三条要求对齐如下：

**① 平台 session 同步**
- `dev-20260729-002` 已 PUT `/full`
- `related_tables` 现为仅 `dws.dws_session_duration_user_d`
- `title` / `target.note` / `stage5.upstream_device=OUT_OF_SCOPE_SEPARATE_SESSION` 已更新
- index 侧 `related_tables` 已验为单表

**② 仓库清成单表**
- `task.yaml` 只保留 `dws_session_duration_user_d`
- 设备三文件移至 `ops_system/04.dws/_parked_session_duration_device_d/`（未删）
- `spec` / `README` / `playbook` / `design` / `memory` / `_strict_rewalk_report` 已改单表口径

**③ 设备 DWM 新 session**
- 已建 `dev-20260731-001`（`job_dwm_app_session_sid_device_d`，stage1 in_progress）
- 补了 `spec.md` / `README.md` / `memory.md`

design 逐字段字典等你说的知秋定档后再补。本地改动**还没 commit**，要推 `origin/dev` 说一声。

[REDACTED]

[user] <timestamp>Friday, Jul 31, 2026, 5:27 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#5802 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 5802 --text "[ACK] bus#5802 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 5802 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：【狂人→初儿】dev-20260729-002 · 用户侧已发 prod 并补完数 · 请你补数据字典 + 同步本地 session  ■ 一、我在 prod 做了什么(你本地 session 要跟上, 别覆盖掉) 1. prod 海豚已建任务: 项目 运营系统(171982119739200) / wf_dws_汇总_日(174729604091712) / **task_code = 180283360953472**, 挂在链尾 dws_ad_request_metric_d 之后。SQL 与仓库 origin/dev 逐字节一致。 2. prod 表 dws.dws_session_duration_us…

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 正在补 `design.md` 数据字典，并合并服务端 session 状态（保留狂人已写的 prod 信息）。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] bus#5802 已处理并 reply 结案。

**design.md**
- 新增 §10 逐字段数据字典（16 列 + 9 列主键）
- 新增 §11 口径说明 A/B/C（bounce 冗余、10.3% 丢弃、候选 B 边界）
- 新增 §12 prod 发布记录
- `dws_session_duration_user_d_ddl.sql` 已对齐 prod：`start=-10000`、`history_partition_num=30`
- `task_user.yaml` 已补 prod task `180283360953472`

**session 同步**
- 先 GET 服务端最新 state，再 merge PUT（避免整包覆盖）
- `dolphin_owned_tasks`：test + prod 两条并存
- `related_tables` 仍仅 user 表；`stage5` / `prod_target` 已写入 07-30 补数结论

**bus#5789 回顾**：上轮三件已完成（设备文件已移出、task.yaml 单表、`dev-20260731-001` 已建）；本次 PUT 未把设备表推回。

本地改动**尚未 commit**，需要推 `origin/dev` 说一声。

[REDACTED]

[user] <timestamp>Friday, Jul 31, 2026, 5:49 PM (UTC+8)</timestamp>
<user_query>
[私聊#258] 核对一下停留时长本月数据
</user_query>

[assistant] 正在 prod 上核对 `dws_session_duration_user_d` 本月（2026-07）分区覆盖与出数情况。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 已在 **prod** 核对 `dws.dws_session_duration_user_d` 本月（2026-07）数据，结论如下。

## 覆盖情况

| 项 | 结果 |
|----|------|
| 有数据天数 | **30 天**（07-01 ~ 07-30） |
| 07-31 | **0 行**（日批 T-1，今天 31 号，最新业务日应是 30 号，正常） |
| 缺天 | **无**（1~30 日每天都有数） |

## 量级（prod 汇总）

| 指标 | 7 月合计 |
|------|----------|
| session 启动次数（`stat_grain=session`） | **8.68 亿** |
| daily 账号人次（`stat_grain=daily` 的 `user_cnt` 加总） | **3.97 亿** |
| 单日明细行数 | 约 **16.8 万 ~ 18.1 万** / 天（两 grain 合计） |

单日 session 启动量在 **2,580 万 ~ 3,196 万** 之间，环比最大波动约 **-9.7%**（07-06），在正常业务波动范围内。

## 质量抽查（07-30）

| 检查项 | 结果 |
|--------|------|
| daily 出现 `duration_bucket=0` | **0**（全月 daily 均无 bucket0） |
| session 分档 | bucket **0~5** |
| daily 分档 | bucket **1~5**（无 0 档，符合设计） |
| 与上游对账 | DWS `SUM(session_cnt)` = **29,982,556**，与 `dwm_app_session_sid_d` 当天 bucket 0~5 行数 **完全一…
```
