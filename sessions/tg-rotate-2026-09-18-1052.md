# TG 会话轮换归档 · 2026-09-18-1052

- 原因: daily(06:00)
- session_id: ses_1789654413250_y095tm
- cursor_chat_id: 3558b5a4-b051-44d6-a883-b6f7b3fd11e3
- 蒸馏: AI+heuristic

## 携带要点

- 大漏斗 prod **不在海豚挂 task**，是 **Spark 链路**；核查跑批状态应查 **SR 分区数据 + `update_time`**，不要先在海豚里找 workflow。
- 后续深挖方向：把 SF-81 的 `video_play` 与详情页 **交集逻辑拆开** 查根因，而非重跑全链路。
- 主表：`dws.dws_app_event_funnel_d_d`；口语「大漏斗今天正常跑了没」默认 **datacheck · prod · dt=T-1**。
- 跑通粗判：`dt` 分区有数、`update_time` 落在今早例行窗口（如 06:57）、行数/app 数与近一日环比量级正常。
- 2026-09-17 样例：**5,597 行 / 737 app**，`update_time=2026-09-18 06:57:46`；环比 09-16 注册 +1.2%、PV +2.3%、视频浏览 +2.4%，全站 video/novel/comic VIEW 非 0，new/old/-1 分区均有行 → **产线已跑通**。
- playbook 硬规则：**`video_play` 加总 ≤ 详情页 UV**；单 app 对照不过 ≠ 整表没跑，要分开报。
- SF-81 异常模式：`video_play` **61,646 > 详情页 UV 55,242**；T-2 同口径曾过（51,435 ≤ 54,368），T-1 仅 play 跳涨约 20%、详情页几乎不动 → 优先怀疑 **口径/交集实现**，不是调度断流。
- 核查报告落盘：`.claude/database/reports/dws.dws_app_event_funnel_d_d/validate__<dt>__<timestamp>.md`。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-18-大漏斗-prod-核查跳过海豚-改查-sr-分区行数-update_time-与-playboo.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-18-play-详情页-uv-时先比-t-2-同口径-若仅-play-跳涨而-detail-不动-查交.md`

## AI 蒸馏原文（截断）

```
- 大漏斗 prod **不在海豚挂 task**，是 **Spark 链路**；核查跑批状态应查 **SR 分区数据 + `update_time`**，不要先在海豚里找 workflow。
- 主表：`dws.dws_app_event_funnel_d_d`；口语「大漏斗今天正常跑了没」默认 **datacheck · prod · dt=T-1**。
- 跑通粗判：`dt` 分区有数、`update_time` 落在今早例行窗口（如 06:57）、行数/app 数与近一日环比量级正常。
- 2026-09-17 样例：**5,597 行 / 737 app**，`update_time=2026-09-18 06:57:46`；环比 09-16 注册 +1.2%、PV +2.3%、视频浏览 +2.4%，全站 video/novel/comic VIEW 非 0，new/old/-1 分区均有行 → **产线已跑通**。
- playbook 硬规则：**`video_play` 加总 ≤ 详情页 UV**；单 app 对照不过 ≠ 整表没跑，要分开报。
- SF-81 异常模式：`video_play` **61,646 > 详情页 UV 55,242**；T-2 同口径曾过（51,435 ≤ 54,368），T-1 仅 play 跳涨约 20%、详情页几乎不动 → 优先怀疑 **口径/交集实现**，不是调度断流。
- 核查报告落盘：`.claude/database/reports/dws.dws_app_event_funnel_d_d/validate__<dt>__<timestamp>.md`。
- 后续深挖方向：把 SF-81 的 `video_play` 与详情页 **交集逻辑拆开** 查根因，而非重跑全链路。

[LESSON: datacheck,funnel,spark|大漏斗 prod 核查跳过海豚，改查 SR 分区行数、`update_time` 与 playbook 对照项]
[LESSON: datacheck,funnel,video_play|play > 详情页 UV 时先比 T-2 同口径；若仅 play 跳涨而 detail 不动，查交集/口径实现而非判跑批失败]
```

## 会话摘录（截断）

```
[user] [私聊#495] 核对一下大漏斗今天prod正常跑了没 数据对不对

[assistant] 理解为：`datacheck` · `prod` · 大漏斗 · `dt=T-1(2026-09-17)` → 核对今日产线是否跑通且数据正常。先定位表/剧本与调度，再查 prod。大漏斗在 prod 海豚里没有挂 task，是 Spark 链路。接着查 SR 分区与集群跑批状态。结论：**今天产线跑通了**；业务日 **2026-09-17（T-1）** 已在今早写入。总量看着正常，但 **SF-81 的 video_play 对照没过**。

· 跑批：`dws.dws_app_event_funnel_d_d` · dt=2026-09-17 · **5,597 行 / 737 app** · `update_time=2026-09-18 06:57:46`（今早例行）
· 环比 09-16：行数持平，注册 +1.2%、PV +2.3%、视频浏览 +2.4%，量级正常
· 视频/小说/漫画 VIEW 全站均非 0，分区 new/old/-1 都有行
· 异常：SF-81 `video_play` 加总 **61,646** > 详情页 UV **55,242**（playbook 要求 play ≤ detail）。**昨天同口径是过的**（play 51,435 ≤ detail 54,368），今天 play 跳涨约 20%、详情页几乎没动——像口径/交集实现问题，不是“没跑”

报告：`.claude/database/reports/dws.dws_app_event_funnel_d_d/validate__2026-09-17__20260918_100951.md`

要不要我接着把 SF-81 的 `video_play` 和详情页交集拆开查根因？

[user] <timestamp>Friday, Sep 18, 2026, 10:07 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-18 10:06（周五），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-17 22:13:06 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/dat…

[assistant] 理解为：`datacheck` · `prod` · 大漏斗 · `dt=T-1(2026-09-17)` → 核对今日产线是否跑通且数据正常。先定位表/剧本与调度，再查 prod。

[assistant] 大漏斗在 prod 海豚里没有挂 task，是 Spark 链路。接着查 SR 分区与集群跑批状态。

[assistant] 结论：**今天产线跑通了**；业务日 **2026-09-17（T-1）** 已在今早写入。总量看着正常，但 **SF-81 的 video_play 对照没过**。

· 跑批：`dws.dws_app_event_funnel_d_d` · dt=2026-09-17 · **5,597 行 / 737 app** · `update_time=2026-09-18 06:57:46`（今早例行）
· 环比 09-16：行数持平，注册 +1.2%、PV +2.3%、视频浏览 +2.4%，量级正常
· 视频/小说/漫画 VIEW 全站均非 0，分区 new/old/-1 都有行
· 异常：SF-81 `video_play` 加总 **61,646** > 详情页 UV **55,242**（playbook 要求 play ≤ detail）。**昨天同口径是过的**（play 51,435 ≤ detail 54,368），今天 play 跳涨约 20%、详情页几乎没动——像口径/交集实现问题，不是“没跑”

报告：`.claude/database/reports/dws.dws_app_event_funnel_d_d/validate__2026-09-17__20260918_100951.md`

要不要我接着把 SF-81 的 `video_play` 和详情页交集拆开查根因？
```
