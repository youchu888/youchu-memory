# TG 会话轮换归档 · 2026-09-06-0607

- 原因: daily(06:00)
- session_id: ses_1788560252232_8ummvl
- cursor_chat_id: 33ed5def-fa0b-4b49-90f0-0005fa303bbb
- 蒸馏: AI+heuristic

## 携带要点

- 用户问「为什么失败」时，先对齐**失败对象**（日报上传 / 大漏斗 / 打卡 / TG 会话状态），不要默认某一种。
- 回报结论要带可核验字段：日期、云端 record ID、最终状态（inserted/updated），便于用户自助核对。
- [LESSON: daily-report,upload|同日重复上传云端是 updated 覆盖同一条记录，inserted→updated 属正常，勿误判为异常]
- 排查顺序：终端输出 → 相关任务日志（如 explain）→ 最近建表/跑数记录 → 再查对应 API/脚本结果。
- 日报「上传云端」成功判定：查 `upload_work_report.py` 执行结果；`code=0` 且有云端记录 ID 即成功。
- 同日同类型日报重复上传会走 **update 覆盖**（先 `inserted` 后 `updated` 是正常行为，不是失败）。
- 云端填报页看不到记录时，优先核对**日期筛选**（如 `2026-09-05`）和页面缓存，让用户刷新后再查。
- TG 里 agent 会话**变红/显示失败**，常与真实 API 结果脱钩；需单独核对实际上传/任务日志，不能只看 TG 状态。
- 若排除上传问题后仍不明，应**追问具体场景**（大漏斗 / 打卡 / 其它），再定向查对应链路。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-06-用户说-失败-先查-upload-日志与云端-record-id-tg-会话红状态-上传-api.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-06-同日重复上传云端是-updated-覆盖同一条记录-inserted-updated-属正常-勿.md`

## AI 蒸馏原文（截断）

```
## 携带要点

- 用户问「为什么失败」时，先对齐**失败对象**（日报上传 / 大漏斗 / 打卡 / TG 会话状态），不要默认某一种。
- 排查顺序：终端输出 → 相关任务日志（如 explain）→ 最近建表/跑数记录 → 再查对应 API/脚本结果。
- 日报「上传云端」成功判定：查 `upload_work_report.py` 执行结果；`code=0` 且有云端记录 ID 即成功。
- 同日同类型日报重复上传会走 **update 覆盖**（先 `inserted` 后 `updated` 是正常行为，不是失败）。
- 云端填报页看不到记录时，优先核对**日期筛选**（如 `2026-09-05`）和页面缓存，让用户刷新后再查。
- TG 里 agent 会话**变红/显示失败**，常与真实 API 结果脱钩；需单独核对实际上传/任务日志，不能只看 TG 状态。
- 回报结论要带可核验字段：日期、云端 record ID、最终状态（inserted/updated），便于用户自助核对。
- 若排除上传问题后仍不明，应**追问具体场景**（大漏斗 / 打卡 / 其它），再定向查对应链路。

## 新 lesson

[LESSON: daily-report,upload,tg|用户说「失败」先查 upload 日志与云端 record ID；TG 会话红状态≠上传 API 失败，须分开核对]

[LESSON: daily-report,upload|同日重复上传云端是 updated 覆盖同一条记录，inserted→updated 属正常，勿误判为异常]
```

## 会话摘录（截断）

```
[user] [私聊#472] 为什么失败？

[assistant] 先查最近失败现场：终端、漏斗 explain 日志，以及刚改过的建表/跑数记录。最可能是刚说的「上传云端」失败了，我先核对那次上传日志。刚核过：**日报上传云端没有失败**。

你 18:58 那次「按这个上传云端」API 已成功：
- 日期：`2026-09-05`
- 云端记录 ID：`89902`
- 状态：先是 `inserted`，我刚复核又是 `updated`（同一条）

填报页按 **2026-09-05** 查应能看到。若页面没有，多半是筛选/缓存，刷新一下。

你若看到的「失败」是 TG 里 agent 状态红了，那是会话展示问题，不是上传 API 挂了。若指的是别的（大漏斗 / 打卡），说一下具体哪条，我对着查。

[user] <timestamp>Saturday, Sep 5, 2026, 7:02 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-05 19:02（周六），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-05 18:58:37 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/dat…

[assistant] 先查最近失败现场：终端、漏斗 explain 日志，以及刚改过的建表/跑数记录。

[assistant] 最可能是刚说的「上传云端」失败了，我先核对那次上传日志。

[assistant] 刚核过：**日报上传云端没有失败**。

你 18:58 那次「按这个上传云端」API 已成功：
- 日期：`2026-09-05`
- 云端记录 ID：`89902`
- 状态：先是 `inserted`，我刚复核又是 `updated`（同一条）

填报页按 **2026-09-05** 查应能看到。若页面没有，多半是筛选/缓存，刷新一下。

你若看到的「失败」是 TG 里 agent 状态红了，那是会话展示问题，不是上传 API 挂了。若指的是别的（大漏斗 / 打卡），说一下具体哪条，我对着查。
```
