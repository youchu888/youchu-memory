# TG 会话轮换归档 · 2026-08-18-2130

- 原因: max_transcript(405KB>=400KB)
- session_id: ses_1787020248469_gqeyw8
- cursor_chat_id: 618c5501-29c3-4bfe-a140-0552dfdb9bae
- 蒸馏: AI+heuristic

## 携带要点

- 狂人 bus#6676 U1~U7 评审结论：三层 concept/label/implementation 方向对；staging + 5 条门禁可挡 U5；**MySQL 8 无部分索引时用生成列 NULL 不参与唯一**（`primary_slot`/`biz_term_key`）实现条件唯一
- 设计稿补充铁律：`orphaned` 仅 implementation 层派生禁双写；G6 复核队列 + `v_metric_impl_candidate_rejected_review`；**granularity 是 concept 固有属性**；`diverged_since`/`diverged_owner` + 7 工作日 SLA
- [LESSON: metric-library|DDL 评审后改稿 push 不等于建表；Phase0 test DDL 须等 5 条拍板 + 知秋别名真源/lifecycle 两项，禁止抢跑]
- 指标库分「概念设计 v0.2」与「现网 metadata 存量」两条线；设计交付 ≠ 可发布口径库，264 条存量仍处止血态治理阶段
- 概念层设计三件套：`metric_library_concept_model_v0.2`、`metric_library_system_v0.2`（#6552 四件套/75 条拆条流）、`metric_library_concept_model_ddl_draft`（旁路四表，**未执行**）
- 又初侧设计闭环后仍须等外部拍板 5 项（test Phase0 建表、别名真源、264 条 lifecycle 批量改、derived 存法、是否与 Phase2 并行）才能动 DDL / dev-session
- 现网 `metric_standard` 快照：264 条；175 有 formula；**75 条同名多实现**是 Phase2 拆条输入；89 仍缺 formula；27 绑定软下线
- 跨角色协作：设计交 bus#6662/#6664 给知秋/狂人；超时无回执用 bus#6673 合并催办，材料固定指向 `origin/dev` commit + 三份文档路径
- DDL 硬伤定案（commit `4d15c1ed`）：`metric_label` 改 `UNIQUE(concept, kind, text)` + `label_primary_slot`；去掉 `canonical_code`；legacy 解析须带 `table_fqn`，多条命中 **409 消歧**
- 狂人点头改稿后 Phase2 语义归纳（75 条拆条、U1/U3 优先）可启动；**test/prod DDL 与现网写操作仍等知秋两项拍板**
- 大漏斗 test 建表：表 `dws.dws_app_event_funnel_d_d`（test SR）；DDL 源 `ops_system/04.dws/dws_app_event_funnel_d_d/`；须绑 dev session `dev-20260807-big-funnel-001` 经 MCP `db.run_ddl_etl` 执行
- 大漏斗宽表结构：主键 `(dt, app_id, is_new)`；18 事件 × 3 指标 = 54 列；动态日分区近 30 天～未来 3 天；建完表为空，日批走 Spark/Paimon 另链路
- Paimon/staging（如 `dws_app_event_funnel_metric_stg_d`）不在 SR DDL 一步搞定；需在 Hadoop 跑 `run_paimon_ddl.sh`，与 SR 宽表分开交付说明

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-18-ddl-评审后改稿-push-不等于建表-phase0-test-ddl-须等-5-条拍板-知秋.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-18-mysql-8-条件唯一用生成列-null-不参与-unique-metric_label-唯一.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-18-test-sr-建表走-dev-session-mcp-ddl-paimon-staging-另.md`

## AI 蒸馏原文（截断）

```
- 指标库分「概念设计 v0.2」与「现网 metadata 存量」两条线；设计交付 ≠ 可发布口径库，264 条存量仍处止血态治理阶段
- 概念层设计三件套：`metric_library_concept_model_v0.2`、`metric_library_system_v0.2`（#6552 四件套/75 条拆条流）、`metric_library_concept_model_ddl_draft`（旁路四表，**未执行**）
- 又初侧设计闭环后仍须等外部拍板 5 项（test Phase0 建表、别名真源、264 条 lifecycle 批量改、derived 存法、是否与 Phase2 并行）才能动 DDL / dev-session
- 现网 `metric_standard` 快照：264 条；175 有 formula；**75 条同名多实现**是 Phase2 拆条输入；89 仍缺 formula；27 绑定软下线
- 跨角色协作：设计交 bus#6662/#6664 给知秋/狂人；超时无回执用 bus#6673 合并催办，材料固定指向 `origin/dev` commit + 三份文档路径
- 狂人 bus#6676 U1~U7 评审结论：三层 concept/label/implementation 方向对；staging + 5 条门禁可挡 U5；**MySQL 8 无部分索引时用生成列 NULL 不参与唯一**（`primary_slot`/`biz_term_key`）实现条件唯一
- DDL 硬伤定案（commit `4d15c1ed`）：`metric_label` 改 `UNIQUE(concept, kind, text)` + `label_primary_slot`；去掉 `canonical_code`；legacy 解析须带 `table_fqn`，多条命中 **409 消歧**
- 设计稿补充铁律：`orphaned` 仅 implementation 层派生禁双写；G6 复核队列 + `v_metric_impl_candidate_rejected_review`；**granularity 是 concept 固有属性**；`diverged_since`/`diverged_owner` + 7 工作日 SLA
- 狂人点头改稿后 Phase2 语义归纳（75 条拆条、U1/U3 优先）可启动；**test/prod DDL 与现网写操作仍等知秋两项拍板**
- 大漏斗 test 建表：表 `dws.dws_app_event_funnel_d_d`（test SR）；DDL 源 `ops_system/04.dws/dws_app_event_funnel_d_d/`；须绑 dev session `dev-20260807-big-funnel-001` 经 MCP `db.run_ddl_etl` 执行
- 大漏斗宽表结构：主键 `(dt, app_id, is_new)`；18 事件 × 3 指标 = 54 列；动态日分区近 30 天～未来 3 天；建完表为空，日批走 Spark/Paimon 另链路
- Paimon/staging（如 `dws_app_event_funnel_metric_stg_d`）不在 SR DDL 一步搞定；需在 Hadoop 跑 `run_paimon_ddl.sh`，与 SR 宽表分开交付说明

[LESSON: metric-library|DDL 评审后改稿 push 不等于建表；Phase0 test DDL 须等 5 条拍板 + 知秋别名真源/lifecycle 两项，禁止抢跑]
[LESSON: metric-library|MySQL 8 条件唯一用生成列 NULL 不参与 UNIQUE；`metric_label` 唯一键是 `(concept, kind, text)` + `label_primary_slot`，不用 `canonical_code`]
[LESSON: dbprogramming|test SR 建表走 dev session + MCP DDL；Paimon/staging 另跑 Hadoop `run_paimon_ddl.sh`，交付时分环境说明]
```

## 会话摘录（截断）

```
[user] [私聊#346] 指标库怎么样了

[assistant] 先核对指标库的真实进展，再给你准确答复。
**08-18 核对结论**：指标库分两条线在走，今天没有新动作。

---

## 一、体系设计（概念层 v0.2）——设计已交，等拍板

**08-17 晚上已闭环又初侧设计交付**（`origin/dev` commit `5921b51d`）：

| 交付物 | 路径 | 状态 |
|--------|------|------|
| 概念层三层模型（U1–U7、迁移路径、§0 业界对齐） | `docs/metric_library_concept_model_v0.2_20260817.md` | ✅ 已 push |
| 止血态门禁（#6552 四件套、75 条拆条流） | `docs/metric_library_system_v0.2_20260815.md` | ✅ 已 push |
| DDL 草案（四表旁路新建，**未执行**） | `docs/metric_library_concept_model_ddl_draft.md` | ✅ 已 push |

**已发 bus：**
- **bus#6662** → 知秋：设计稿已交，请开 Phase2 语义归纳（75 条拆条、U1/U3 优先）
- **bus#6664** → 请转知秋：全套设计 + DDL 草案，5 条待拍板

**截至今天 11:30，未见狂人/知秋对 #6662/#6664 的回执。**

**卡点（等外部拍板，不是又初欠账）：**
1. test 元数据库是否执行 Phase0 建表
2. 口语别名真源：`metric_label.biz_term` vs `aliases.md`
3. 264 条存量能否批量改 `lifecycle_status`
4. derived 只存分子/分母、比率查询侧现算
5. 是否与狂人 Phase2 并行（先建空表再写入）

**又初未动：** test/prod DDL、现网 `metric_standard` 写操作、dev-session（设计阶段，知秋点头后再建）。

---

## 二、现网 metadata 存量（08-17 狂人血缘回填后）

| 项 | 数值 |
|----|------|
| `metric_standard` 总量 | 264 条 |
| 有 formula | 175/264（血缘 run36 回填） |
| 同名多实现 | **75 条**（Phase2 拆条输入） |
| 仍缺 formula | 89 条 |
| 绑定软下线 | 27 条 |

结论：从「264 条全空」升到「可治理止血态」，**还不是可发布口径库**。狂人 Phase2 语义归纳要等模型定稿后启动——设计稿 08-17 已交，归纳侧尚未见启动回执。

---

## 三、…

[user] [私聊#347] 催一下他

[assistant] 先核对 bus#6662/#6664 的回执状态，再经 agent-bus 发催办。
已核对并催办。

**核对**：bus#6662（请开 Phase2）和 bus#6664（转知秋拍板 5 条）都是 08-17 21:40~21:52 发出的，到 11:40 仍无狂人回执。

**已发 bus#6673** → 狂人，催两条：
1. #6662：设计稿评审结论 + Phase2 何时启动  
2. #6664：是否已转知秋、DDL 5 条拍板进展  

材料仍指向 `origin/dev` `5921b51d` 三份文档；又初侧未动 test DDL。

有回执我私聊同步你。

[user] [私聊#348] 测试环境创建一张大漏斗的表吧

[assistant] 先查大漏斗现有设计稿和 DDL，再在 test 环境建表。
测试环境尚无大漏斗表，用 dev session 在 test SR 执行建表 DDL。
私聊#348 已处理：测试环境 StarRocks 大漏斗宽表已建好。

**表名**：`dws.dws_app_event_funnel_d_d`  
**环境**：test SR（`sr_test`）  
**DDL 来源**：`ops_system/04.dws/dws_app_event_funnel_d_d/dws_app_event_funnel_d_d_ddl.sql`  
**dev session**：`dev-20260807-big-funnel-001`

**表结构摘要**：
- 主键：`(dt, app_id, is_new)`
- 18 个事件 × 3 指标（user / session / event）= 54 个指标列
- 动态日分区（近 30 天 ~ 未来 3 天），`replication_num=3`

当前表为空，尚无数据。日批主链路走 Spark/Paimon（`spark/scripts/run_yarn_daily_sql.sh`）；若还要在测试集群建 Paimon 表（含 staging `dws_app_event_funnel_metric_stg_d`），需在 Hadoop 上跑 `run_paimon_ddl.sh`，说一声我再帮你跑。

[user] <timestamp>Tuesday, Aug 18, 2026, 11:41 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-18 11:40（周二），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-18 11:30:39 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/da…

[assistant] 先核对 bus#6662/#6664 的回执状态，再经 agent-bus 发催办。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 已核对并催办。

**核对**：bus#6662（请开 Phase2）和 bus#6664（转知秋拍板 5 条）都是 08-17 21:40~21:52 发出的，到 11:40 仍无狂人回执。

**已发 bus#6673** → 狂人，催两条：
1. #6662：设计稿评审结论 + Phase2 何时启动  
2. #6664：是否已转知秋、DDL 5 条拍板进展  

材料仍指向 `origin/dev` `5921b51d` 三份文档；又初侧未动 test DDL。

有回执我私聊同步你。

[REDACTED]

[user] <timestamp>Tuesday, Aug 18, 2026, 12:05 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#6676 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 6676 --text "[ACK] bus#6676 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 6676 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：[狂人 · U1~U7 逐条核对结论 · 08-18 · 承诺兑现]  先说结论：模型能装下 U1~U7，三层拆分(concept/label/implementation)方向正确，staging 候选表 + 5 条门禁把 U5 从事后清理改成入库挡住，这是对的。DDL 里两个生成列 trick 用对了——primary_slot 和 biz_term_key 靠 NULL 不参与唯一去重来实现条件唯一，这正是 MySQL 8 没有部分索引时的标准解法，也正好收掉我 #6552 提的那个 NULL 坑。这块做得好。  下面是必须改的和必须答的。  【硬伤 1 · 必改】metric_label 的唯一键跟 …

[assistant] 先 60 秒内 ACK，再读完整 bus 正文和设计稿。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 正在按 bus#6676 修改 DDL 与设计稿，改完 push 后 reply 结案。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] bus#6676 已处理并结案（reply **bus#6677**）。

**已改并 push**（`origin/dev` `4d15c1ed`）：

| 狂人项 | 处理 |
|--------|------|
| **硬伤1** | `metric_label` 改为 `UNIQUE(concept, kind, text)` + `label_primary_slot`；去掉 `canonical_code`（`concept_code` 即 canonical） |
| **硬伤2** | §4.1 定案 **(a)**：legacy 名解析须带 `table_fqn`，无上下文多条命中 → 409 要求消歧 |
| **问题3** | `orphaned` 由 implementation 层派生，禁止手工双写 |
| **问题4** | G6 复核队列 + `v_metric_impl_candidate_rejected_review` 视图 |
| **问题5** | 不冗余存 canonical label |
| **问题6** | §0.2 写死：granularity 是 concept 固有属性，非查询参数 |
| **问题7** | `diverged_since`/`diverged_owner` + 7 工作日 SLA |

**未动**：test DDL 未执行；知秋两项（别名真源 / 264 批量 status）仍等拍板。

狂人侧可以开 Phase2 语义归纳了。

[REDACTED]

[user] <timestamp>Tuesday, Aug 18, 2026, 9:02 PM (UTC+8)</timestamp>
<user_query>
[私聊#348] 测试环境创建一张大漏斗的表吧
</user_query>

[assistant] 先查大漏斗现有设计稿和 DDL，再在 test 环境建表。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[user] <mcp_meta_tools>
You have access to MCP (Model Context Protocol) tools through `GetMcpTools` and `CallMcpTool`.

## MCP Tool Discovery and Invocation

Use `GetMcpTools` to discover tool schemas, then `CallMcpTool` to invoke them. Aim to minimize round-trips: ideally one `GetMcpTools` call followed by one `CallMcpTool` call.

If the user mentions, references, or links to a product or service that corresponds to an available MCP server, and the request likely depends on information from that service, proactively inspect that MCP server before answering. Do not wait for the user to explicitly ask you to use MCP. If you are unsure which server matches, use `GetMcpTools` with a pattern based on the service name.

`GetMcpTools` supports four modes:

1. `{"server":"<id>"}`: returns full input sch…

[user] <timestamp>Tuesday, Aug 18, 2026, 9:02 PM (UTC+8)</timestamp>
<user_query>
[私聊#348] 测试环境创建一张大漏斗的表吧
</user_query>

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 测试环境尚无大漏斗表，用 dev session 在 test SR 执行建表 DDL。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 私聊#348 已处理：测试环境 StarRocks 大漏斗宽表已建好。

**表名**：`dws.dws_app_event_funnel_d_d`  
**环境**：test SR（`sr_test`）  
**DDL 来源**：`ops_system/04.dws/dws_app_event_funnel_d_d/dws_app_event_funnel_d_d_ddl.sql`  
**dev session**：`dev-20260807-big-funnel-001`

**表结构摘要**：
- 主键：`(dt, app_id, is_new)`
- 18 个事件 × 3 指标（user / session / event）= 54 个指标列
- 动态日分区（近 30 天 ~ 未来 3 天），`replication_num=3`

当前表为空，尚无数据。日批主链路走 Spark/Paimon（`spark/scripts/run_yarn_daily_sql.sh`）；若还要在测试集群建 Paimon 表（含 staging `dws_app_event_funnel_metric_stg_d`），需在 Hadoop 上跑 `run_paimon_ddl.sh`，说一声我再帮你跑。

[REDACTED]
```
