# TG 会话轮换归档 · 2026-09-01-2244

- 原因: max_transcript(411KB>=400KB)
- session_id: ses_1788270209035_gyiwlp
- cursor_chat_id: 119d9c93-f6ee-4b97-8a5c-d58c6af53d04
- 蒸馏: AI+heuristic

## 携带要点

- 工作簿口径**：09:01 群进展只写「截至汇报日之前」的累计状态（等同 T-1 截止）；当天新干的活进 work-log，**次日**工作簿再报，禁止混进当天那份。
- 进度汇报约定**（#450）：说人话；每项写「节点 / 卡点 / 要不要主人拍板」；实查 task 板、work-log、本地代码、git，禁止凭印象。
- 设备指纹 + uid 映射**（最高优先）：bus#7787 定起点 **2026-08-01**；本地 SQL/spec 已改，远程仍 `2f95e122`；push → 请知秋再审 → PASS → 沙箱四步；dim / 六张 dwm / 宽表 **全 HOLD**。
- 大漏斗 Spark**：与 uid_map 同交审链（bus#7756/#7760）；等 PASS，prod 未动。
- 「要你拍板：不用」也是有效结论**：口径已定（如 8/1 起点、Phase1 顺序）时明确写，避免主人重复确认。
- [LESSON: daily-report,workbook,work-log|09:01 群进展/工作簿只写 T-1 截止累计进度；当天实活写 work-log，次日工作簿再纳入，禁止当天混报]
- [LESSON: progress-report,agent-bus|主人要任务进度时用人话逐项写节点/卡点/需确认项，并实查 task 板+work-log+git，禁止流水账或凭会话记忆]
- [LESSON: fingerprint,uid-map,HOLD|指纹/uid_map 本地改完须 push 并请知秋再审 PASS 后才可沙箱四步；未 PASS 前 dim/dwm/宽表一律 HOLD]
- **工作簿口径**：09:01 群进展只写「截至汇报日之前」的累计状态（等同 T-1 截止）；当天新干的活进 work-log，**次日**工作簿再报，禁止混进当天那份。
- **举例**：9/1 晚上 push uid_map → 9/1 工作簿不报；9/2 写「9/1 截止：本地已改完，待 push / 待再审」。
- **task 板分两块**：「截至汇报前」vs「当日实活（不进当天群进展）」，整理时先对齐口径再填。
- **进度汇报约定**（#450）：说人话；每项写「节点 / 卡点 / 要不要主人拍板」；实查 task 板、work-log、本地代码、git，禁止凭印象。
- **设备指纹 + uid 映射**（最高优先）：bus#7787 定起点 **2026-08-01**；本地 SQL/spec 已改，远程仍 `2f95e122`；push → 请知秋再审 → PASS → 沙箱四步；dim / 六张 dwm / 宽表 **全 HOLD**。
- **大漏斗 Spark**：与 uid_map 同交审链（bus#7756/#7760）；等 PASS，prod 未动。
- **指标库 Phase1**：test published 约 10→106；**diverged 全 HOLD**，只 enrich 不升正式；4 批脚本在 `docs/` 未 commit；video ~29、other ~112 待归类；顺序 legacy req_ref → user/login → ad → page → video…

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-09-01-群进展-工作簿只写-t-1-截止累计进度-当天实活写-work-log-次日工作簿再.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-主人要任务进度时用人话逐项写节点-卡点-需确认项-并实查-task-板-work-log-git.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-01-指纹-uid_map-本地改完须-push-并请知秋再审-pass-后才可沙箱四步-未-pass.md`

## AI 蒸馏原文（截断）

```
## 携带要点

- **工作簿口径**：09:01 群进展只写「截至汇报日之前」的累计状态（等同 T-1 截止）；当天新干的活进 work-log，**次日**工作簿再报，禁止混进当天那份。
- **举例**：9/1 晚上 push uid_map → 9/1 工作簿不报；9/2 写「9/1 截止：本地已改完，待 push / 待再审」。
- **task 板分两块**：「截至汇报前」vs「当日实活（不进当天群进展）」，整理时先对齐口径再填。
- **进度汇报约定**（#450）：说人话；每项写「节点 / 卡点 / 要不要主人拍板」；实查 task 板、work-log、本地代码、git，禁止凭印象。
- **设备指纹 + uid 映射**（最高优先）：bus#7787 定起点 **2026-08-01**；本地 SQL/spec 已改，远程仍 `2f95e122`；push → 请知秋再审 → PASS → 沙箱四步；dim / 六张 dwm / 宽表 **全 HOLD**。
- **大漏斗 Spark**：与 uid_map 同交审链（bus#7756/#7760）；等 PASS，prod 未动。
- **指标库 Phase1**：test published 约 10→106；**diverged 全 HOLD**，只 enrich 不升正式；4 批脚本在 `docs/` 未 commit；video ~29、other ~112 待归类；顺序 legacy req_ref → user/login → ad → page → video…
- **页面访问**：prod 到 8/31、停留五档在出数；**等产品答「离开事件埋点」** 前口径 HOLD，不能报完成。
- **Agent 重启**：Cursor resume 失败时用户会发「重启 agent」；须冷启动后合并待处理私聊，口径与实态重新核对再回。
- **「要你拍板：不用」也是有效结论**：口径已定（如 8/1 起点、Phase1 顺序）时明确写，避免主人重复确认。

## 新 lesson

[LESSON: daily-report,workbook,work-log|09:01 群进展/工作簿只写 T-1 截止累计进度；当天实活写 work-log，次日工作簿再纳入，禁止当天混报]

[LESSON: progress-report,agent-bus|主人要任务进度时用人话逐项写节点/卡点/需确认项，并实查 task 板+work-log+git，禁止流水账或凭会话记忆]

[LESSON: fingerprint,uid-map,HOLD|指纹/uid_map 本地改完须 push 并请知秋再审 PASS 后才可沙箱四步；未 PASS 前 dim/dwm/宽表一律 HOLD]
```

## 会话摘录（截断）

```
[user] [私聊#449] 工作簿肯定截止到当前的进展，你管他今天有什么进展干什么？今天的进展是明天要汇报的，懂吗？所以你现在需要做的事情就是每天工作簿整理的时候 按照我们实际负责的模块，整理截止到汇报之前的进度，这样说你能懂了吗？

[user] [新开agent·并行] [私聊#450] 把现在每项任务的进度，当前处于那个节点，有什么卡点，需要我确认什么，整理一下发给我。记住说人话，这是我们的约定
（系统：长任务占着另一路 agent；本条独立处理，须先读记忆冷启动。）

[assistant] 正在实查各专项的当前状态，随后用人话整理每项进度、节点、卡点和需你确认的事项。
按今天实查（task 板、work-log、本地代码与 git）整理如下。工作簿口径按你刚说的：**汇报的是「截止到汇报日之前」的进度**；今天 9/1 下午的活，进 **明天 9/2 的工作簿**，不混进今天那份。

---

## 又初主责 · 当前每项任务

### 1. 设备标签指纹 + uid 映射（Spark，最高优先）

**到哪了：** 代码写法狂人之前已审过（主键改指纹、四路 `_r` 等）。知秋在 bus#7787 定了口径起点 **2026-08-01**，本地 SQL 和 spec 已按这个改完（四路加 `dt >= '2026-08-01'`，「首次/换号」改成「自 8 月 1 日起」）。  
**还没做：** 改动 **没 push**，线上仓库仍是 `2f95e122`；也 **没再请狂人复审**。  
**后面：** 狂人 PASS → 沙箱四步测试 → 才能动 dim / 六张 dwm / 宽表（现在一律 HOLD）。

**卡点：** 卡在 **等狂人再审一遍**，不是口径未定。  
**要你拍板：** **不用**。8 月 1 日起点已定。

---

### 2. 大漏斗 Spark

**到哪了：** stage SQL + 沙箱 steps 已在 `2f95e122`，和 uid_map 同一套交审链（bus#7756/#7760）。  
**还没做：** 等狂人 PASS；prod 调度 **没动**（符合规矩）。  
**卡点：** 同第 1 项，等狂人。  
**要你拍板：** **不用**。

---

### 3. 指标库 Phase1（test 元库灌数）

**到哪了：** 结构层 v0.3 早就有；今天把 draft 往 published 推了一截——订单、用户、页面、广告几批脚本都跑过 test；模型文档补了 Phase1 用 `legacy:metric_standard/*` 当过渡 `req_ref` 的说明。  
**粗数（test，今天流水）：** published 大约从 10 涨到 **106** 左右；有语义对不上的 **diverged 全部 HOLD**，只 enrich 不升正式。  
**还没做：** 4 批 Python/SQL 还在 `docs/`，**没 commit**；video 约 29 条、other 约 112 条还没归类推；狂人还没做新一轮只读复验。  
**卡点：** 无硬阻断；主要是 **继续分批推 + 脚本入库**。  
**要你拍板：** **不用**。你 17:32 定的顺序（legacy req_ref → user/login → ad → page → vi…

[assistant] ❌ Cursor 会话连接失败（已自动丢弃旧 resume）。请重发刚才那句；或发「重启 agent」强制新开。

[user] [私聊#451] 重启 agent

[assistant] 会话已重启。正在核对工作簿口径与各项任务的实态，随后用人话汇总。
Agent 已重启，两条私聊一并回。

---

## 工作簿口径（#449）——懂了

规则就一句：

**每天 09:01 群进展 / 工作簿，只写「截至汇报日之前」的累计状态（相当于 T-1 截止）；当天新干的活写 work-log，进「次日」工作簿，不混进当天那份。**

举例：9/1 晚上 push 了 uid_map，**9/1 那份不报**；**9/2 工作簿**里写「9/1 截止：本地已按 8/1 起点改完，待 push / 待狂人再审」。

task 板已按这个口径分两块：**「截至汇报前」** 和 **「当日实活（不进当天群进展）」**。

---

## 又初主责 · 当前每项（说人话）

### 1. 设备标签指纹 + uid 映射（Spark，最高优先）

**节点：** 写法狂人之前审过了；知秋 bus#7787 定了起点 **2026-08-01**，本地 SQL/spec 已改完（四路加下界、「首次/换号」改成「自 8 月 1 日起」）。

**卡点：** 改动还在本机，**没 push**（远程仍是 `2f95e122`），也**没再请狂人复审**。

**下一步：** push → bus 请狂人再审 → PASS 后沙箱四步测试 → 才能动 dim / dwm / 宽表（现在全 HOLD）。

**要你拍板：** 不用，8/1 起点已定。

---

### 2. 大漏斗 Spark

**节点：** SQL + 沙箱 steps 已在 `2f95e122`，和 uid_map 同一套交审（bus#7756/#7760）。

**卡点：** 等狂人 PASS；prod 没动。

**要你拍板：** 不用。

---

### 3. 指标库 Phase1（test 灌数）

**节点：** 结构层 v0.3 早就有；今天订单/用户/页面/广告几批在 test 推过，published 大约 **10 → 106**；语义对不上的 diverged 全 HOLD；模型文档补了 Phase1 过渡 `req_ref` 说明。

**卡点：** 4 批脚本还在 `docs/` **没 commit**；video 约 29 条、other 约 112 条还没归类推；狂人还没新一轮只读复验。

**要你拍板：** 不用，你 17:32 定的顺序已在走。

---

### 4. 页面统计 + 页面访问

**节点：** 页面访问日表 prod 在跑，最新到 **8/31**；停留五档 prod 也在出数。

**卡点：** **等产品答「离开事件埋点」**——没答之前口径 HOLD，不能报这块做完。

**要你拍板：** 若能催产品给结论，这块就能往下；否则只能维护…

[user] <timestamp>Tuesday, Sep 1, 2026, 9:56 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-01 21:55（周二），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-01 21:47:29 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/data…

[assistant] 会话已重启。正在核对工作簿口径与各项任务的实态，随后用人话汇总。

[REDACTED]

[assistant] [REDACTED]

[assistant] Agent 已重启，两条私聊一并回。

---

## 工作簿口径（#449）——懂了

规则就一句：

**每天 09:01 群进展 / 工作簿，只写「截至汇报日之前」的累计状态（相当于 T-1 截止）；当天新干的活写 work-log，进「次日」工作簿，不混进当天那份。**

举例：9/1 晚上 push 了 uid_map，**9/1 那份不报**；**9/2 工作簿**里写「9/1 截止：本地已按 8/1 起点改完，待 push / 待狂人再审」。

task 板已按这个口径分两块：**「截至汇报前」** 和 **「当日实活（不进当天群进展）」**。

---

## 又初主责 · 当前每项（说人话）

### 1. 设备标签指纹 + uid 映射（Spark，最高优先）

**节点：** 写法狂人之前审过了；知秋 bus#7787 定了起点 **2026-08-01**，本地 SQL/spec 已改完（四路加下界、「首次/换号」改成「自 8 月 1 日起」）。

**卡点：** 改动还在本机，**没 push**（远程仍是 `2f95e122`），也**没再请狂人复审**。

**下一步：** push → bus 请狂人再审 → PASS 后沙箱四步测试 → 才能动 dim / dwm / 宽表（现在全 HOLD）。

**要你拍板：** 不用，8/1 起点已定。

---

### 2. 大漏斗 Spark

**节点：** SQL + 沙箱 steps 已在 `2f95e122`，和 uid_map 同一套交审（bus#7756/#7760）。

**卡点：** 等狂人 PASS；prod 没动。

**要你拍板：** 不用。

---

### 3. 指标库 Phase1（test 灌数）

**节点：** 结构层 v0.3 早就有；今天订单/用户/页面/…
```
