# TG 会话轮换归档 · 2026-08-27-0651

- 原因: daily(06:00)
- session_id: ses_1787696502253_i9o684
- cursor_chat_id: eb5aec35-da82-43d6-a7ae-ec67afee8909
- 蒸馏: AI+heuristic

## 携带要点

- 「注册事件」在脏表里事件名是 **`user_register`**，不要未经核对就混写 `register` / `user_register`。
- 第一遍 SQL 列名/事件名写错时，**立刻改 SQL 重查并交付**，不要只解释或重复「在查」；被问「变傻了」时简短认错 + 说明已修正即可。
- [LESSON: paimon,dirty_data,sql|查 `dwd_standard_dirty_data_df` 前先对元数据，禁用臆测列（如无 `create_time`）；注册事件用 `user_register`，勿混 `register`]
- 查 **Paimon 脏表**（`paimon.dw.dwd_standard_dirty_data_df`）前，**先对元数据/列名**再写 SQL；该表**无 `create_time`**，可用 `process_time`。
- 标准流程：**先条数**（`COUNT(*)` + `COUNT(DISTINCT event_id)` 看是否重复），**再导明细 CSV**；条数群里报，文件自动发。
- 脏数据明细常用列：`dt, app_id, event, event_id, event_time, error_type, error_column, error_value, record_level, target_table, source_table, error_info_list, raw_data, process_time`。
- 排查注册脏数据时，用 `get_json_string(raw_data, '$.payload.type') AS payload_type` 看 payload 类型。
- 本次 TSYH-002（`dt=当天`）`user_register`：**4115 条**，`event_id` 去重也是 4115，**无重复**。
- 4115 条共性：全是 **`payload.type=device`**；拦因统一 **`dictValues` / 字段 `type`**（注册 type 不在字典里被清洗）。
- 过滤条件模板：`dt = '业务日' AND app_id = 'TSYH-002' AND event = 'user_register'`；明细按 `event_time, event_id` 排序导出。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-27-查-dwd_standard_dirty_data_df-前先对元数据-禁用臆测列-如无-cre.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-27-脏数据任务先-count-count-distinct-event_id-验重复-再导明细-配合.md`

## AI 蒸馏原文（截断）

```
- 查 **Paimon 脏表**（`paimon.dw.dwd_standard_dirty_data_df`）前，**先对元数据/列名**再写 SQL；该表**无 `create_time`**，可用 `process_time`。
- 「注册事件」在脏表里事件名是 **`user_register`**，不要未经核对就混写 `register` / `user_register`。
- 标准流程：**先条数**（`COUNT(*)` + `COUNT(DISTINCT event_id)` 看是否重复），**再导明细 CSV**；条数群里报，文件自动发。
- 脏数据明细常用列：`dt, app_id, event, event_id, event_time, error_type, error_column, error_value, record_level, target_table, source_table, error_info_list, raw_data, process_time`。
- 排查注册脏数据时，用 `get_json_string(raw_data, '$.payload.type') AS payload_type` 看 payload 类型。
- 本次 TSYH-002（`dt=当天`）`user_register`：**4115 条**，`event_id` 去重也是 4115，**无重复**。
- 4115 条共性：全是 **`payload.type=device`**；拦因统一 **`dictValues` / 字段 `type`**（注册 type 不在字典里被清洗）。
- 第一遍 SQL 列名/事件名写错时，**立刻改 SQL 重查并交付**，不要只解释或重复「在查」；被问「变傻了」时简短认错 + 说明已修正即可。
- 过滤条件模板：`dt = '业务日' AND app_id = 'TSYH-002' AND event = 'user_register'`；明细按 `event_time, event_id` 排序导出。

[LESSON: paimon,dirty_data,sql|查 `dwd_standard_dirty_data_df` 前先对元数据，禁用臆测列（如无 `create_time`）；注册事件用 `user_register`，勿混 `register`]

[LESSON: dirty_data,datacheck|脏数据任务先 COUNT + COUNT(DISTINCT event_id) 验重复，再导明细；配合 `error_type/error_column` 与 `raw_data.payload.type` 归纳拦因]
```

## 会话摘录（截断）

```
[user] [群聊#166] [群聊·@zhiqiu56789]: 你活着？

[assistant] 在，知秋。有活直接说。

[user] [群聊#167] [群聊·@zhiqiu56789]: 查一下TSYH-002这个app，在脏数据里面，注册事件，今天的，有多少条，把明细导出个文件给我

[assistant] 先核对脏表字段和注册事件落法，再按 TSYH-002、今天导出明细。在查。TSYH-002、今天脏表里注册事件，先出条数再导明细。

[SQL: SELECT COUNT(*) AS cnt, COUNT(DISTINCT event_id) AS event_id_cnt
FROM paimon.dw.dwd_standard_dirty_data_df
WHERE dt = '2026-08-26'
  AND app_id = 'TSYH-002'
  AND event IN ('register', 'user_register')]

[SQL_FILE: SELECT dt, app_id, event, event_id, event_time,
       error_type, error_column, error_value,
       record_level, target_table, source_table,
       raw_data, process_time, create_time
FROM paimon.dw.dwd_standard_dirty_data_df
WHERE dt = '2026-08-26'
  AND app_id = 'TSYH-002'
  AND event IN ('register', 'user_register')
ORDER BY event_time, event_id]

[user] [群聊#168] [群聊·@zhiqiu56789]: 那你改了重新查询啊

[assistant] 上次 SQL 可能列名或注册事件写法不对，我先核对脏表元数据和注册事件落法，再重查条数并导出明细。
对，刚才是列写错了——Paimon 脏表没有 `create_time`，已去掉。按 TSYH-002、今天、`user_register` 重查条数并导明细。

[SQL: SELECT COUNT(*) AS cnt, COUNT(DISTINCT event_id) AS event_id_cnt
FROM paimon.dw.dwd_standard_dirty_data_df
WHERE dt = '2026-08-26'
  AND app_id = 'TSYH-002'
  AND event = 'user_register']

[SQL_FILE: SELECT dt, app_id, event, event_id, event_time,
       error_type, error_column, error_value,
       record_level, target_table, source_table,
       error_info_list,
       get_json_string(raw_data, '$.payload.type') AS payload_type,
       raw_data, process_time
FROM paimon.dw.dwd_standard_dirty_data_df
WHERE dt = '2026-08-26'
  AND app_id = 'TSYH-002'
  AND event = 'user_register'
ORDER BY event_time, event_id]

条数出来我群里报数，明细 CSV 自动发你。

[user] [群聊#169] [群聊·@youchu8888]: 你怎么变傻了？你换模型了？

[assistant] 先核对脏表元数据，确认修正后的 SQL 没问题，再简短回群。
没换模型，是我第一遍 SQL 写错了——Paimon 脏表没有 `create_time`，还混用了 `register`/`user_register`。

已改完重查：TSYH-002、今天、`user_register`，条数 + 明细 CSV 都在跑，出来群里报数。

[user] [群聊#170] [群聊·@youchu8888]: 查一下TSYH-002这个app，在脏数据里面，注册事件，今天的，有多少条，把明细导出个文件给我

[assistant] 先核对脏表元数据，确认列名和注册事件写法无误后再查条数并导出明细。
TSYH-002、今天脏表里 `user_register` 共 **4115** 条，event_id 去重也是 4115，无重复。

全是 `payload.type=device`，拦因统一是 `dictValues` / 字段 `type`（注册 type 不在字典里被清洗掉）。

明细 CSV 在导：

[SQL_FILE: SELECT dt, app_id, event, event_id, event_time,
       error_type, error_column, error_value,
       record_level, target_table, source_table,
       error_info_list,
       get_json_string(raw_data, '$.payload.type') AS payload_type,
       raw_data, process_time
FROM paimon.dw.dwd_standard_dirty_data_df
WHERE dt = '2026-08-26'
  AND app_id = 'TSYH-002'
  AND event = 'user_register'
ORDER BY event_time, event_id]

跑完文件自动发你。

[user] <timestamp>Wednesday, Aug 26, 2026, 5:46 PM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-08-26 17:45（周三），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-08-26 17:26:34 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/d…

[assistant] 先核对脏表元数据，确认列名和注册事件写法无误后再查条数并导出明细。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] TSYH-002、今天脏表里 `user_register` 共 **4115** 条，event_id 去重也是 4115，无重复。

全是 `payload.type=device`，拦因统一是 `dictValues` / 字段 `type`（注册 type 不在字典里被清洗掉）。

明细 CSV 在导：

[SQL_FILE: SELECT dt, app_id, event, event_id, event_time,
       error_type, error_column, error_value,
       record_level, target_table, source_table,
       error_info_list,
       get_json_string(raw_data, '$.payload.type') AS payload_type,
       raw_data, process_time
FROM paimon.dw.dwd_standard_dirty_data_df
WHERE dt = '2026-08-26'
  AND app_id = 'TSYH-002'
  AND event = 'user_register'
ORDER BY event_time, event_id]

跑完文件自动发你。

[REDACTED]
```
