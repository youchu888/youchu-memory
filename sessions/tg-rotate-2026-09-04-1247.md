# TG 会话轮换归档 · 2026-09-04-1247

- 原因: max_transcript(408KB>=400KB)
- session_id: ses_1788475679588_8w2rkd
- cursor_chat_id: 621fa3f4-f81f-4e79-882b-401cfb6ca876
- 蒸馏: AI+heuristic

## 携带要点

- `_h_r` 做天计次必须用 **`COUNT(DISTINCT event_id)`**，且 `dt` 用裸串；这是狂人复审三条里的硬检查点
- 沙箱跑数前必须 **同步集群 SQL**；本地已 push 但集群还是旧版时，yarn 壳/SQL 宏会对不上
- 今天收口路径：explain PASS → 去 `--explain` 真跑 metrics→wide → 验 `test.dws` 宽表有行且 `sdk_init/video` 非全 0
- 催狂人审改 SQL 时，模板要带：**commit、文件路径、选表/计次规则摘要、请回 PASS 或打回点**；bus 已发过仍可在群里补一句
- [LESSON: 大漏斗|摘 new 改 stage_metrics 时 _h_r 天计次固定 COUNT(DISTINCT event_id)，legacy daily.sql 用 task.yaml disabled 冻结勿删]
- 大漏斗摘 `new` 的卡点常不是 `_r` 表有没有，而是 **#7880 改造清单/选表规则是否对齐**；表齐（如 `dwd_sdk_init_d_r`）只代表最后一环就绪，不等于整链可改 SQL
- 主人拍板「我们自己干」后，可按 **#7880 已透出的选表规则** 先改 `stage_metrics`，不必死等狂人统一清单；改完 push 并 bus 请审即可
- 主干改造范围：`stage_metrics` 里 **11 个主干事件 + page_view** 全切 `paimon.dwd.*_r`，**零引用** `dw.dw_user_event_detail_new`
- 选表口径：`register/login` → `*_v2_r`；`order_paid` → `d_r`；`sdk_init` → `dwd_sdk_init_d_r`；`order_created/coin` 等小时表 → `_h_r`
- **legacy `daily.sql` 冻结不投**：仍含 `new`，主线只动 `stage_metrics`；冻结方式是 **仓库保留 + `task.yaml` 显式 `disabled`**，不是删文件
- 开干前先 **prod 实查 `_r` 表**：表在不在、当日分区行数是否合理（新表昨日分区为 0 很常见）
- 狂人 bus 复审（如 #7900）可与出数 **并行**：「等 #2287 落地后再复审」**不挡** 今天 SF-81 沙箱 explain / test 出数
- agent-bus **ACK 可能被服务端 silent 丢弃**；实质回执靠 reply 结案，必要时 ACK 用 `--no-dedup` 重发

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-09-04-摘-new-改-stage_metrics-时-_h_r-天计次固定-count-distinc.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-04-_r-表齐后仍须对齐-7880-选表规则再改-sql-主人拍板可自行推进-改完-push-bus.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-09-04-ack-被-silent-丢时用-no-dedup-重发-以-reply-成功为结案依据.md`

## AI 蒸馏原文（截断）

```
- 大漏斗摘 `new` 的卡点常不是 `_r` 表有没有，而是 **#7880 改造清单/选表规则是否对齐**；表齐（如 `dwd_sdk_init_d_r`）只代表最后一环就绪，不等于整链可改 SQL
- 主人拍板「我们自己干」后，可按 **#7880 已透出的选表规则** 先改 `stage_metrics`，不必死等狂人统一清单；改完 push 并 bus 请审即可
- 主干改造范围：`stage_metrics` 里 **11 个主干事件 + page_view** 全切 `paimon.dwd.*_r`，**零引用** `dw.dw_user_event_detail_new`
- 选表口径：`register/login` → `*_v2_r`；`order_paid` → `d_r`；`sdk_init` → `dwd_sdk_init_d_r`；`order_created/coin` 等小时表 → `_h_r`
- `_h_r` 做天计次必须用 **`COUNT(DISTINCT event_id)`**，且 `dt` 用裸串；这是狂人复审三条里的硬检查点
- **legacy `daily.sql` 冻结不投**：仍含 `new`，主线只动 `stage_metrics`；冻结方式是 **仓库保留 + `task.yaml` 显式 `disabled`**，不是删文件
- 开干前先 **prod 实查 `_r` 表**：表在不在、当日分区行数是否合理（新表昨日分区为 0 很常见）
- 狂人 bus 复审（如 #7900）可与出数 **并行**：「等 #2287 落地后再复审」**不挡** 今天 SF-81 沙箱 explain / test 出数
- 沙箱跑数前必须 **同步集群 SQL**；本地已 push 但集群还是旧版时，yarn 壳/SQL 宏会对不上
- 今天收口路径：explain PASS → 去 `--explain` 真跑 metrics→wide → 验 `test.dws` 宽表有行且 `sdk_init/video` 非全 0
- 催狂人审改 SQL 时，模板要带：**commit、文件路径、选表/计次规则摘要、请回 PASS 或打回点**；bus 已发过仍可在群里补一句
- agent-bus **ACK 可能被服务端 silent 丢弃**；实质回执靠 reply 结案，必要时 ACK 用 `--no-dedup` 重发

[LESSON: 大漏斗|摘 new 改 stage_metrics 时 _h_r 天计次固定 COUNT(DISTINCT event_id)，legacy daily.sql 用 task.yaml disabled 冻结勿删]
[LESSON: 大漏斗|_r 表齐后仍须对齐 #7880 选表规则再改 SQL；主人拍板可自行推进，改完 push+bus 请审，复审不挡沙箱 explain]
[LESSON: agent-bus|ACK 被 silent 丢时用 --no-dedup 重发，以 reply 成功为结案依据]
```

## 会话摘录（截断）

```
[user] [私聊#461] paimon.dwd.dwd_sdk_init_d_r  好了，大漏斗是不是可以开干了

[assistant] 理解为：`explain` · 大漏斗 · `dwd_sdk_init_d_r` 就绪 → 确认能否开干。先对一下 OPEN 交接与 `_r` 实况。关键是 bus#7880：等 `_r` 表 + 狂人改造清单。正在核对原文和表是否真有数。表说好了；按 #7880 还差狂人统一改造清单。先实查表是否真有数，并看清单有没有到。表已确认有数；按 #7880 还差狂人改造清单。正在催清单，并结案相关 bus。**表好了，但还不能整链开干。**

prod 刚核过：
· `paimon.dwd.dwd_sdk_init_d_r` 已在
· 今日 `dt=2026-09-04` 约 **81.8 万行**
· 昨日分区暂 0（多半刚起写）

按狂人 #7880：11 个主干事件里原先只差 `sdk_init`；表齐之后要把 `daily`/`stage` 里读 `dw.dw_user_event_detail_new` 的几处换成对应 `_r`，**new 表从主干摘掉**。改造清单由他统一下发——**清单还没到，禁抢跑改 SQL**。

已 bus 催清单（出站 #7896）。清单一到立刻改。

现在能动的：explain 线可续跑；**别再深挖 legacy daily.sql**。

[user] [私聊#462] 现在表有了阿

[user] [新开agent·并行] [私聊#463] 我们自己干
（系统：长任务占着另一路 agent；本条独立处理，须先读记忆冷启动。）

[assistant] 表齐了这点没异议。我先看狂人清单有没有回，并摸清现在还在扫 `new` 的事件，能开就开。对，表有了，这点没拦着。

prod 已核过：`dwd_sdk_init_d_r` 在，今日分区约 81.8 万行。

现在卡的不是表，是狂人 #7880 说的「表好了由他统一发改造清单」——清单还没到。我刚已 bus 催过（#7896）。

你要是让我**先按已确认选表起草改 SQL**（不等清单），直接说一声，我马上开改。

[assistant] 主人说「我们自己干」——不等狂人清单，自己把大漏斗主干从 `new` 切到 `_r`。先对齐 #7880 改造点与本地 SQL，再动手。接着读 #7880 原文和现网 metrics SQL，摸清 11 个事件该怎么换 `_r`。主人拍板自己干。按 #7880 已给的选表规则，先把 11 个事件对上 `_r` 表和列，再改 `stage_metrics`。表映射齐了。正在改 `stage_metrics`：主干全部切 `_r`，小时表用 `COUNT(DISTINCT event_id)`。收到，**自己干**——已按 #7880 选表把主干从 `new` 摘掉。

**已改（本地未 commit）**
· `stage_metrics`：11 个主干事件 + page_view 全读 `paimon.dwd.*_r`（含刚好的 `dwd_sdk_init_d_r`）
· register/login → `*_v2_r`；order_paid → `d_r`；order_created/coin → `_h_r` + `COUNT(DISTINCT event_id)`
· 同步了 spec / task.yaml / steps probes / spot_check / design
· legacy `daily.sql` **冻结**（仍含 new，不投工）

**已通知狂人** bus#7897（说明主人令自行改 + 选表清单）

下一步：你说一声我就 **commit + push**，然后开沙箱 explain。

[user] [私聊#464] 继续下一步

[assistant] 理解为：按已确认选表推进大漏斗摘 `new`——先改 `stage_metrics`，核对各 `_r` 列后再动 SQL。`stage_metrics` 已按 `_r` 改过。正在核对还剩什么，以及 git 状态。本地摘 `new` 已改完但未入库。先核对 diff，再 commit+push，并 bus 请狂人审。下一步已做完。

· `stage_metrics` 已摘掉 `dw_user_event_detail_new`，11 主干 + page_view 全改读对应 `*_r`（含 `dwd_sdk_init_d_r`）
· `_h_r` 天计次用 `COUNT(DISTINCT event_id)`；`dt` 裸串
· legacy `daily.sql` 冻结不投
· 已 push：`563013e7`
· 已 bus 狂人请审（#7899）；PASS 后开 SF-81 sandbox explain

你这边不用再等清单了。

[user] [私聊#465] 现在需要我催狂人什么内容，发给我，我去催

[assistant] 直接转给狂人即可：

---

狂人，大漏斗摘 `new` 这轮帮我审一下。

· commit：`563013e7`（已 push origin/dev）  
· 文件：`ops_system/04.dws/dws_app_event_funnel_d_d/spark/sql/dws_app_event_funnel_d_d_daily_stage_metrics.sql`  
· 按你 #7880 已透选表改的：register/login=`*_v2_r`，order_paid=`d_r`，`_h_r` 天计次 `COUNT(DISTINCT event_id)`，sdk_init=`dwd_sdk_init_d_r`，stage 零引用 `dw_user_event_detail_new`；legacy daily 冻结不投  

请回：**选表/计次 PASS 或打回点**。PASS 了我马上开 SF-81 sandbox explain。

（bus 已发过 #7899，群里催一句也行）

---

[user] [私聊#466] 现在到哪了，大漏斗这个今天要搞定数据的

[assistant] 先对一下现状和卡点，再判断今天怎么把数据跑出来。代码已齐；主人要今天出数。狂人审可并行，我这边先开沙箱 explain / SF-81 试跑。注意到 yarn 壳与 SQL 宏可能对不上。先同步集群代码并核对宏，再开跑。集群 SQL 还是旧版。正在同步最新代码并开 SF-81 沙箱 explain。**进度一句话：代码齐了，正在出数路上（explain 已起）。**

已完成
· 摘 `new`、全切 `*_r`：`563013e7`；legacy 冻结：`8b613fb6`
· 集群 SQL 已同步（含 `dwd_sdk_init_d_r`）
· 狂人 #7900：他等 #2287 再复审——**不挡我们今天出数**

正在做
· 沙箱 **explain** 已起：SF-81 · `dt=2026-09-04` · 写 `test.dws`
· 日志：`/tmp/funnel_explain_20260904.log`（hadoop-1）
· 选 09-04：`sdk_init_d_r` 今日约 211 万行，昨日分区还是 0

接下来（今天收口）
1. explain PASS → 去掉 `--explain` 真跑 metrics→wide  
2. 验 `test.dws` 宽表有行、sdk_init/video 非全 0  
3. 再报你；写 prod 另说一声

不用催狂人也能推进今天出数。explain 完我继续真跑。

[user] <timestamp>Friday, Sep 4, 2026, 10:44 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-09-04 10:43（周五），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 记忆冷启动（必读 · 记忆系统 v2）

你是**新开的 agent**（或本轮任务开工）。先读本节再干活：
- 红线 PINNED · 未结 MEMORY_OPEN · 按时间最近动过 · high 标题
- 全量 MEMORY.md / lessons 只当索引，不整包灌入；按任务 tags 再深读
- 打开≠用了；真改做法才 touch

# 又初 · 会话记忆启动包

> **生成时间**：2026-09-04 06:47:23 CST
> **维护人**：又初 · **来源**：`PINNED.md` + `MEMORY_OPEN.md` + recent-by-mtime + high 标题
> **指令**：先读本文件 → 再按任务 tags `memory.read` / 读完整 lesson。全量索引见 `MEMORY.md`（不整包注入）。
> **体积策略**：硬注入小而准；禁止只看 hot（须同时看「按时间最近动过」）。

## 0. 检索捷径

## 7. 检索捷径

| 我想… | 先去哪 |
|--------|--------|
| 找某张表的 ETL | `rg "表名" ops_system/ db/` → `program_mappings.md` |
| 跑数据核查 | `.claude/database/playbooks/<db>.<table>.md` |
| 查业务别名 | `.claude/data…

[assistant] 理解为：`explain` · 大漏斗 · `dwd_sdk_init_d_r` 就绪 → 确认能否开干。先对一下 OPEN 交接与 `_r` 实况。

[assistant] 关键是 bus#7880：等 `_r` 表 + 狂人改造清单。正在核对原文和表是否真有数。

[assistant] 表说好了；按 #7880 还差狂人统一改造清单。先实查表是否真有数，并看清单有没有到。

[assistant] 表已确认有数；按 #7880 还差狂人改造清单。正在催清单，并结案相关 bus。

[assistant] **表好了，但还不能整链开干。**

prod 刚核过：
· `paimon.dwd.dwd_sdk_init_d_r` 已在
· 今日 `dt=2026-09-04` 约 **81.8 万行**
· 昨日分区暂 0（多半刚起写）

按狂人 #7880：11 个主干事件里原先只差 `sdk_init`；表齐之后要把 `daily`/`stage` 里读 `dw.dw_user_event_detail_new` 的几处换成对应 `_r`，**new 表从主干摘掉**。改造清单由他统一下发——**清单还没到，禁抢跑改 SQL**。

已 bus 催清单（出站 #7896）。清单一到立刻改。

现在能动的：explain 线可续跑；**别再深挖 legacy daily.sql**。

[user] <timestamp>Friday, Sep 4, 2026, 11:07 AM (UTC+8)</timestamp>
<user_query>
[私聊#462] 现在表有了阿
</user_query>

[assistant] 表齐了这点没异议。我先看狂人清单有没有回，并摸清现在还在扫 `new` 的事件，能开就开。

[assistant] 对，表有了，这点没拦着。

prod 已核过：`dwd_sdk_init_d_r` 在，今日分区约 81.8 万行。

现在卡的不是表，是狂人 #7880 说的「表好了由他统一发改造清单」——清单还没到。我刚已 bus 催过（#7896）。

你要是让我**先按已确认选表起草改 SQL**（不等清单），直接说一声，我马上开改。

[user] <timestamp>Friday, Sep 4, 2026, 11:27 AM (UTC+8)</timestamp>
<user_query>
[私聊#464] 继续下一步
</user_query>

[assistant] 理解为：按已确认选表推进大漏斗摘 `new`——先改 `stage_metrics`，核对各 `_r` 列后再动 SQL。

[assistant] `stage_metrics` 已按 `_r` 改过。正在核对还剩什么，以及 git 状态。

[assistant] 本地摘 `new` 已改完但未入库。先核对 diff，再 commit+push，并 bus 请狂人审。

[assistant] 下一步已做完。

· `stage_metrics` 已摘掉 `dw_user_event_detail_new`，11 主干 + page_view 全改读对应 `*_r`（含 `dwd_sdk_init_d_r`）
· `_h_r` 天计次用 `COUNT(DISTINCT event_id)`；`dt` 裸串
· legacy `daily.sql` 冻结不投
· 已 push：`563013e7`
· 已 bus 狂人请审（#7899）；PASS 后开 SF-81 sandbox explain

你这边不用再等清单了。

[user] <timestamp>Friday, Sep 4, 2026, 11:31 AM (UTC+8)</timestamp>
<user_query>
[私聊#465] 现在需要我催狂人什么内容，发给我，我去催
</user_query>

[assistant] 直接转给狂人即可：

---

狂人，大漏斗摘 `new` 这轮帮我审一下。

· commit：`563013e7`（已 push origin/dev）  
· 文件：`ops_system/04.dws/dws_app_event_funnel_d_d/spark/sql/dws_app_event_funnel_d_d_daily_stage_metrics.sql`  
· 按你 #7880 已透选表改的：register/login=`*_v2_r`，order_paid=`d_r`，`_h_r` 天计次 `COUNT(DISTINCT event_id)`，sdk_init=`dwd_sdk_init_d_r`，stage 零引用 `dw_user_event_detail_new`；legacy daily 冻结不投  

请回：**选表/计次 PASS 或打回点**。PASS 了我马上开 SF-81 sandbox explain。

（bus 已发过 #7899，群里催一句也行）

---

[user] <timestamp>Friday, Sep 4, 2026, 11:32 AM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#7900 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 7900 --text "[ACK] bus#7900 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 7900 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：收到 bus#7897。主人放行你自己干这条我认。#2287 结算 3 处修法部署决策未落地前不切进你这轮 —— 落地后立刻拉 stage_metrics diff 复审, 重点 3 条: ①order_created/coin 的 COUNT(DISTINCT event_id) 是否每 step 都落到位 ②主干 11 事件 + page_view 有没有漏切到 _r ③legacy daily.sql 冻结方式(仓库删 or task.yaml disable)。你别空转, 有想同步的其他项照发 bus。
</user_query>

[assistant] 先 ACK，再按狂人三条重点自检并回执。

[assistant] ACK 被静默丢了，改用 `--no-dedup` 重发；同时把 legacy `daily.sql` 在 `task.yaml` 里显式标 disable。

[assistant] bus#7900 已回（出站 #7903）。

要点：
· 认你「#2287 落地前不切复审」
· 三条自检已交底：① order_created/coin 全是 `COUNT(DISTINCT event_id)` ② 11+page_view 全切 `_r` ③ legacy **仓库保留 + task.yaml `disabled`**（不删）
· 冻结补提交已 push：`8b613fb6`（改造本体仍是 `563013e7`）

ACK 两次被服务端 silent 丢，实质回执用 reply 已结案。

[user] <timestamp>Friday, Sep 4, 2026, 11:52 AM (UTC+8)</timestamp>
<user_query>
[私聊#466] 现在到哪了，大漏斗这个今天要搞定数据的
</user_query>

[assistant] 先对一下现状和卡点，再判断今天怎么把数据跑出来。

[assistant] 代码已齐；主人要今天出数。狂人审可并行，我这边先开沙箱 explain / SF-81 试跑。

[assistant] 注意到 yarn 壳与 SQL 宏可能对不上。先同步集群代码并核对宏，再开跑。

[assistant] 集群 SQL 还是旧版。正在同步最新代码并开 SF-81 沙箱 explain。

[assistant] **进度一句话：代码齐了，正在出数路上（explain 已起）。**

已完成
· 摘 `new`、全切 `*_r`：`563013e7`；legacy 冻结：`8b613fb6`
· 集群 SQL 已同步（含 `dwd_sdk_init_d_r`）
· 狂人 #7900：他等 #2287 再复审——**不挡我们今天出数**

正在做
· 沙箱 **explain** 已起：SF-81 · `dt=2026-09-04` · 写 `test.dws`
· 日志
```
