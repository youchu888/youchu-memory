# TG 会话轮换归档 · 2026-07-30-0643

- 原因: daily(06:00)
- session_id: ses_1785276123328_8eb1za
- cursor_chat_id: cc2bf63c-245b-4ab6-a467-e5d243e9c59e
- 蒸馏: AI+heuristic

## 携带要点

- `workbook_progress_service.py` 里 `_progress_device_tag()` 等存在硬编码旧进展（如 SF-81、playbook PASS、Scala 待 push），代码未跟 git/session 实际状态同步就会天天发一样的话
- 主人 07-22（私聊#186/#187）已明确：工作簿进度要反映**当天真实完成情况**，禁止固定模板、禁止像例行交差；虽未逐字说「确认后发群」，语义等同**不能未经主人过目就自动发群**
- 草稿必须带当日证据（分区 dt、行数、git SHA、dev session 状态等），**禁止硬编码旧叙事**；已自动发出的错误帖应标注勿当真，可经确认后补发/更正
- 对照：用户标签 `dws_user_tag_d_d.reg_platform` 为 App/Web/**Other** 三档，用 device **LIKE 模糊匹配**；设备标签与用户标签口径不同，回答字段字典勿混用
- [LESSON: tgbot,workbook-progress|机器人群工作簿进度默认私聊草稿待主人确认后再发，草稿须查库/平台取证，禁止硬编码与探针空快照直接发群]
- 机器人群 09:01 自动进度帖不可当真：须交叉核对「本地发帖记录 + `workbook_live_cache.json` 探针快照 + 当场重查库/平台」三层，探针空快照会走兜底模板文案
- 探针超时/连库失败时 `#3` 归因、`#9` 停留会显示空 dt、0 行或 phase-1 模板句，与 prod/test 真实分区行数严重不符
- 正确流程：狂人 9 点工作簿 → **只生成草稿私聊主人** → 主人「可以发」或改字后再发机器人群 → 未确认则群里不发
- 设备标签 `device` 要分两层：上游 DWD `dwd_user_register_d_v2.device` 是端类型（设计 Android/iOS/PC，线上多为 **ANDROID/IOS/PC** 大写，偶发 OTHER）；宽表 `dws_device_tag_d_d` **无 device 列**，落表为 **`reg_platform`**
- 设备标签 `reg_platform` 映射（SR 老版 + Spark 姿态 F 一致）：ANDROID/IOS→App；PC/WEB/BROWSER/H5→Web；其它/NULL→**App 兜底**（**无 Other**）
- TG 群仅在被显式 @（`@youchu_ai_bot` / `@youchu8888` / `@又初` / `@初儿`）时回复；只 @ 主人账号又初收不到，同事转述须 @ 又初而非 @ 主人或只 @ 蓝猫 bot

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-07-30-机器人群工作簿进度默认私聊草稿待主人确认后再发-草稿须查库-平台取证-禁止硬编码与探针空快照直接.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-07-30-设备标签问-device-时答两层-dwd-入值-android-ios-pc-宽表落-reg_.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-07-30-群内找又初须-又初-初儿-youchu8888-youchu_ai_bot-主人账号不是又初通道.md`

## AI 蒸馏原文（截断）

```
- 机器人群 09:01 自动进度帖不可当真：须交叉核对「本地发帖记录 + `workbook_live_cache.json` 探针快照 + 当场重查库/平台」三层，探针空快照会走兜底模板文案
- 探针超时/连库失败时 `#3` 归因、`#9` 停留会显示空 dt、0 行或 phase-1 模板句，与 prod/test 真实分区行数严重不符
- `workbook_progress_service.py` 里 `_progress_device_tag()` 等存在硬编码旧进展（如 SF-81、playbook PASS、Scala 待 push），代码未跟 git/session 实际状态同步就会天天发一样的话
- 主人 07-22（私聊#186/#187）已明确：工作簿进度要反映**当天真实完成情况**，禁止固定模板、禁止像例行交差；虽未逐字说「确认后发群」，语义等同**不能未经主人过目就自动发群**
- 正确流程：狂人 9 点工作簿 → **只生成草稿私聊主人** → 主人「可以发」或改字后再发机器人群 → 未确认则群里不发
- 草稿必须带当日证据（分区 dt、行数、git SHA、dev session 状态等），**禁止硬编码旧叙事**；已自动发出的错误帖应标注勿当真，可经确认后补发/更正
- 设备标签 `device` 要分两层：上游 DWD `dwd_user_register_d_v2.device` 是端类型（设计 Android/iOS/PC，线上多为 **ANDROID/IOS/PC** 大写，偶发 OTHER）；宽表 `dws_device_tag_d_d` **无 device 列**，落表为 **`reg_platform`**
- 设备标签 `reg_platform` 映射（SR 老版 + Spark 姿态 F 一致）：ANDROID/IOS→App；PC/WEB/BROWSER/H5→Web；其它/NULL→**App 兜底**（**无 Other**）
- 对照：用户标签 `dws_user_tag_d_d.reg_platform` 为 App/Web/**Other** 三档，用 device **LIKE 模糊匹配**；设备标签与用户标签口径不同，回答字段字典勿混用
- TG 群仅在被显式 @（`@youchu_ai_bot` / `@youchu8888` / `@又初` / `@初儿`）时回复；只 @ 主人账号又初收不到，同事转述须 @ 又初而非 @ 主人或只 @ 蓝猫 bot

[LESSON: tgbot,workbook-progress|机器人群工作簿进度默认私聊草稿待主人确认后再发，草稿须查库/平台取证，禁止硬编码与探针空快照直接发群]
[LESSON: device-tag,field-dict|设备标签问 device 时答两层：DWD 入值 ANDROID/IOS/PC，宽表落 reg_platform 仅 App/Web 且无 Other]
[LESSON: tg-group|群内找又初须 @又初/@初儿/@youchu8888/@youchu_ai_bot，@ 主人账号不是又初通道]
```

## 会话摘录（截断）

```
[user] [私聊#247] 你看看机器人群里发的任务进度是实际进展吗？

[assistant] 先核对机器人群最近发的进度内容和当日真实任务状态，再给你结论。
**结论：今天 09:01 机器人群那条进展，只能算「半真半假」，不能当真实完成情况。**

我刚核了三层：本地发帖记录、09:01 探针快照、现在重查库/平台。

---

**1）09:01 探针基本挂了，#3 / #9 数字是错的**

`workbook_live_cache.json` 里 09:01:17 的快照全是空：
- 归因 `result_d` → `—`
- 停留 `page_stay` / 五档 → dt 空、行数 0

按这个空快照，群里实际会发成：
- **#3 归因**：「phase-1 影子期在跑 / 今日 prod 分区待核对」——偏模板
- **#9 停留**：「page_stay dt=、uid 行 0」——明显不对

我现在重查（09:04）的真实数是：
- prod `result_d` 最新 **2026-07-28**，当日 **131 行 / success 115**
- test `page_stay` 最新 **2026-07-28**，**约 109 万 uid 行**
- test 五档 `session_duration_user_d` 同期 **约 187 万 session_cnt**

所以早上那条**不是按当天真实产出写的**，是探针超时/连库失败后走了兜底文案。

---

**2）#4 标签有硬编码，和 07-28 实际进展对不上**

代码里 `_progress_device_tag()` 写死了这段：

```315:325:omdb/tgbot/workbook_progress_service.py
def _progress_device_tag(s: LiveSnapshot) -> TaskProgress:
    done = (
        f"test SR calc_dt {s.device_tag_max_dt}（链暂停）；"
        f"Paimon SF-81：30d DWM 补数+宽表重跑 DONE；playbook 验数 PASS（multi7=70103）"
    )
    return TaskProgress(
        status='进行中',
        done=done,
        blocker='uid_map DWM 是否并行待知秋拍板；本地 Scala 改动待 push',
```

和实际不符的点：
- 还在说「SF-81 30d DWM / playbook PASS」——这是**旧线**叙事
- 还说「本地 Scala 待 push」——**07-28 姿态 F 六步 wrapper + DDL 修复已推 `origin/dev…

[user] [私聊#248] 我有没有说过 每天确认以后再发群里。你现在每天都发的一样的 你觉得合适吗

[user] [私聊#249] 看看标签的需求，device这个字段入职字典都是什么

[assistant] 查设备标签需求文档和字段字典里 `device` 的取值定义。
主人，按标签需求文档和仓内实现，`device` 这块要分两层看：

## 1. 上游原始字段 `device`（DWD 入值）

来源：`dwd.dwd_user_register_d_v2.device`  
DDL 注释：**设备类型: Android/iOS/PC**

这是埋点/注册事件里客户端上报的**端类型**，不是设备标签宽表里的落表字段。

**设计口径（DWD 层字典）**

| 值 | 说明 |
|----|------|
| `Android` | 安卓端 |
| `iOS` | 苹果端 |
| `PC` | PC / Web 端 |

**test 环境 T-1（2026-07-28）实际分布**（全 app 汇总）：

| device | 行数 |
|--------|------|
| ANDROID | 63,384 |
| IOS | 20,142 |
| PC | 1,808 |
| OTHER | 1 |

线上基本是 **ANDROID / IOS / PC** 三类大写枚举；偶发 `OTHER` 等脏值。

---

## 2. 设备标签宽表落表字段：`reg_platform`（不是 `device`）

`dws.dws_device_tag_d_d` **没有** `device` 列，由上游 `device` 映射成 **`reg_platform`**：

| reg_platform | 说明 |
|--------------|------|
| `App` | 移动端（Android / iOS） |
| `Web` | Web 端（PC / 浏览器） |

**映射规则**（SR 老版 + Spark 姿态 F 一致）：

```
ANDROID / IOS           → App
PC / WEB / BROWSER / H5 → Web
其它 / NULL             → App（默认兜底）
```

对应实现见 `dws_device_tag_init.sql` / `DwmMaterializeJob.scala` 的 `first_device` → `reg_platform` 逻辑。

---

## 3. 和用户标签的差异（对照用）

用户标签 `dws_user_tag_d_d.reg_platform` 是 **App / Web / Other** 三档，用 `device` 做 **LIKE 模糊匹配**（含 ios/android/harmony→App，pc/web/h5/chrome 等→Web，其余→Other）。

设备标签**没有 Other**，未识别值一律归 `App`。

…

[user] [群聊#159] [群聊·@youchu8888]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @Lanmao_Tg_Cursor_Bot 你要@初儿 再说一遍，@我他收不到的
问题正文: @Lanmao_Tg_Cursor_Bot 你要@初儿 再说一遍，@我他收不到的

[assistant] 主人，收到群聊硬规则，又初在群里实质答复如下（可直接转发）：

---

**又初已收到。**

群内要找又初干活或传话，请 **@又初 / @初儿 / @youchu8888 / @youchu_ai_bot**，不要只 @ 主人本人——主人账号和又初不是同一条通道，@ 主人又初收不到。

蓝猫（@Lanmao_Tg_Cursor_Bot）若要转述给又初，也请按上面方式 @ 又初再说；又初看到后会正常 ack 并处理。

---

若需要把这条写进 TG 群回复模板或 bot 规则里，说一声我改配置。

[user] <timestamp>Wednesday, Jul 29, 2026, 9:03 AM (UTC+8)</timestamp>
<user_query>
# 当前时间

现在是 2026-07-29 09:03（周三），时区 JST（东京）。

# 身份

你是又初（数据中心开发助手）。用户或同事叫你「初儿」时，指的就是你本人。

# 多渠道统一上下文

含群旁听、私聊、agent-bus。判断要不要回：**仅显式 @youchu_ai_bot / @youchu8888 / @又初 / @初儿** 时在群里回复；裸写「又初」「初儿」或只 @ 别人 → 只作背景，**不要声明「我不插嘴」**——直接不回。

# TG / Cursor / agent-bus 统一上下文（最近消息，与 IDE 同一工作区）

**📥 收到** / **👁 旁听** / **📤 又初回复** 分块展示。

👁 **旁听** · `2026-07-28 18:39:29` · telegram_group
**发言** @zhiqiu56789
> [群聊·@zhiqiu56789]: @youchu_ai_bot dwm_device_tag_merge_pool_d_d
这是什么东西？

📥 **收到** · `2026-07-28 18:41:17` · uid=8184109272 · telegram_group
**发言** @zhiqiu56789
> [群聊#156] [群聊·@zhiqiu56789]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @youchu_ai_bot dwm_device_tag_merge_…

[assistant] 先核对机器人群最近发的进度内容和当日真实任务状态，再给你结论。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] **结论：今天 09:01 机器人群那条进展，只能算「半真半假」，不能当真实完成情况。**

我刚核了三层：本地发帖记录、09:01 探针快照、现在重查库/平台。

---

**1）09:01 探针基本挂了，#3 / #9 数字是错的**

`workbook_live_cache.json` 里 09:01:17 的快照全是空：
- 归因 `result_d` → `—`
- 停留 `page_stay` / 五档 → dt 空、行数 0

按这个空快照，群里实际会发成：
- **#3 归因**：「phase-1 影子期在跑 / 今日 prod 分区待核对」——偏模板
- **#9 停留**：「page_stay dt=、uid 行 0」——明显不对

我现在重查（09:04）的真实数是：
- prod `result_d` 最新 **2026-07-28**，当日 **131 行 / success 115**
- test `page_stay` 最新 **2026-07-28**，**约 109 万 uid 行**
- test 五档 `session_duration_user_d` 同期 **约 187 万 session_cnt**

所以早上那条**不是按当天真实产出写的**，是探针超时/连库失败后走了兜底文案。

---

**2）#4 标签有硬编码，和 07-28 实际进展对不上**

代码里 `_progress_device_tag()` 写死了这段：

```315:325:omdb/tgbot/workbook_progress_service.py
def _progress_device_tag(s: LiveSnapshot) -> TaskProgress:
    done = (
        f"test SR calc_dt …

[user] <timestamp>Wednesday, Jul 29, 2026, 9:11 AM (UTC+8)</timestamp>
<user_query>
[私聊#248] 我有没有说过 每天确认以后再发群里。你现在每天都发的一样的 你觉得合适吗
</user_query>

[assistant] 先查你之前有没有要求「每天确认后再发群」，并对照当前自动发帖逻辑。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 你说得对，**不合适**。我这边执行偏了，先直接答你两句。

**你有没有说过「每天确认后再发群里」？**

索引里能查到的，是 **私聊#186 / #187**（07-22）：
- 要发**真实完成情况**，不要每天像例行交差
- 任务会变，**不要固定模板**，按当天清单动态写

没有逐字「确认后再发群」这一句，但意思很清楚：**不能 9 点机器自动往群里扔一版你没看过的稿**。我当成「改完服务就自动发」了，等于没把把关权留给你——这点是我错。

**为什么你会觉得「每天一样」？**

也成立，根因有三条叠在一起：
1. **09:01 自动发群**，你没机会改字就发出去了  
2. 今早探针一度全空，#3/#9 走了兜底话术，看起来更像模板  
3. #4 标签还**写死了旧文案**（SF-81、待 push），和 07-28 姿态 F 实际进展对不上  

所以不是「清单解析坏了」，是**自动发 + 内容不真**，体感就是每天复制粘贴。

**以后怎么改（我按这个执行）：**

1. 狂人 9 点工作簿 → **只生成草稿，先发你私聊**  
2. 你回复「可以发」或改两句 → **再发机器人群**  
3. 没你确认 → **群里不发**  
4. 草稿必须带当日证据（分区行数、git SHA、session 状态），**禁止硬编码旧进展**

今天 09:01 若已经发出，群里那条**不要当真**；需要的话我现在按真实状态拟一版给你过目，你点头我再补发或更正。

要的话我下一步就改 `group_workbook_progress_handler`：**默认关自动发群，改成私聊待确认**。

[REDACTED]

[user] <timestamp>Wednesday, Jul 29, 2026, 2:52 PM (UTC+8)</timestamp>
<user_query>
[私聊#249] 看看标签的需求，device这个字段入职字典都是什么
</user_query>

[assistant] 查设备标签需求文档和字段字典里 `device` 的取值定义。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[user] <timestamp>Wednesday, Jul 29, 2026, 2:52 PM (UTC+8)</timestamp>
<user_query>
[私聊#249] 看看标签的需求，device这个字段入职字典都是什么
</user_query>

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 主人，按标签需求文档和仓内实现，`device` 这块要分两层看：

## 1. 上游原始字段 `device`（DWD 入值）

来源：`dwd.dwd_user_register_d_v2.device`  
DDL 注释：**设备类型: Android/iOS/PC**

这是埋点/注册事件里客户端上报的**端类型**，不是设备标签宽表里的落表字段。

**设计口径（DWD 层字典）**

| 值 | 说明 |
|----|------|
| `Android` | 安卓端 |
| `iOS` | 苹果端 |
| `PC` | PC / Web 端 |

**test 环境 T-1（2026-07-28）实际分布**（全 app 汇总）：

| device | 行数 |
|--------|------|
| ANDROID | 63,384 |
| IOS | 20,142 |
| PC | 1,808 |
| OTHER | 1 |

线上基本是 **ANDROID / IOS / PC** 三类大写枚举；偶发 `OTHER` 等脏值。

---

## 2. 设备标签宽表落表字段：`reg_platform`（不是 `device`）

`dws.dws_device_tag_d_d` **没有** `device` 列，由上游 `device` 映射成 **`reg_platform`**：

| reg_platform | 说明 |
|--------------|------|
| `App` | 移动端（Android / iOS） |
| `Web` | Web 端（PC / 浏览器） |

**映射规则**（SR 老版 + Spark 姿态 F 一致）：

```
ANDROID / IOS           → App
PC / WEB / …

[user] <timestamp>Wednesday, Jul 29, 2026, 6:39 PM (UTC+8)</timestamp>
<user_query>
[群聊#159] [群聊·@youchu8888]: 【群聊硬规则】本条已显式 @ 初儿/又初（@youchu_ai_bot / @youchu8888 / @又初 / @初儿），必须在群里给实质答复；禁止说「没@初儿」「没@本机器人」「群里不回」。
原文: @Lanmao_Tg_Cursor_Bot 你要@初儿 再说一遍，@我他收不到的
问题正文: @Lanmao_Tg_Cursor_Bot 你要@初儿 再说一遍，@我他收不到的
</user_query>

[assistant] 主人，收到群聊硬规则，又初在群里实质答复如下（可直接转发）：

---

**又初已收到。**

群内要找又初干活或传话，请 **@又初 / @初儿 / @youchu8888 / @youchu_ai_bot**，不要只 @ 主人本人——主人账号和又初不是同一条通道，@ 主人又初收不到。

蓝猫（@Lanmao_Tg_Cursor_Bot）若要转述给又初，也请按上面方式 @ 又初再说；又初看到后会正常 ack 并处理。

---

若需要把这条写进 TG 群回复模板或 bot 规则里，说一声我改配置。

[REDACTED]
```
