# TG 会话轮换归档 · 2026-08-17-1644

- 原因: max_transcript(441KB>=400KB)
- session_id: ses_1786934698281_8ku730
- cursor_chat_id: 6c11c68e-37ba-4781-9463-cd290a7ae040
- 蒸馏: AI+heuristic

## 携带要点

- AI 批量生成的列级血缘**未经人工复核**，上线前必须对照现网海豚 task SQL 逐列核对，错了会误导影响分析。
- 机器核对已覆盖表级拓扑（FROM/JOIN 双向比对）：106/123 一致、0 差异时，**不要再花时间验「上游是不是那几张表」**。
- 周报「说人话」**不等于**改成日报结构；保留原周报版式（`### 专项分块`、`【卡点】`、下周表格、日报索引、「一句话给周会」），只改措辞。
- 周报正文去掉 `stage3`、`v189`、`bucket0`、`agent-bus`、`poller` 等内部说法，换成业务部门能直接听懂的表述；状态写「已完成」，不写「进行中」。
- 周报汇总口径：正式日报 + 双机 work-log 合并稿 + `hosts`；`new-mac` / `old-mac` 缺流水要**如实点明**，不假装双机齐全。
- 「一句话给周会」也要说人话：上周交付、当前卡点、本周动作各一句，方便复制进周会。
- 血缘核对 fallback：海豚元库连不上时，用 MCP 拉 live task SQL 做人工抽查，详报落 `.claude/database/reports/lineage_<bus>/`。
- 停更旧表 + 新表并行时（如 `dws_user_tag_d` vs `dws_user_tag_d_d`）：以**现网在跑 task 的目标表和 FROM/JOIN** 为准，旧表应 deprecated、为新表重建血缘。
- 列级血缘错不只来自源表选错：`WHERE` 过滤（如 `is_valid=1` 滤掉 bounce）会导致聚合列（如 `bounce_cnt`）与血缘描述不一致，prod 未发版时仓内 SQL 已对也仍算问题。
- prod 表可能是 **VIEW** 而非物理 INSERT，核对要先确认架构，不能按普通写入 task 套。
- 人工核对重心转到机器做不出的列级四类：同名列归属（JOIN 多表时的 `app_id`/`dt`/`channel`/`uid`）、WHERE 过滤、聚合口径、视图/停更表等特殊架构。

## 新建 lesson

- `/Users/mac/.dc-platform/memory/lessons/2026-08-17-周报说人话只改措辞不改版式-保留原分块-卡点-周会一句话结构.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-17-表级拓扑机器验过后-人工只盯列级-同名列归属-where过滤-聚合口径-停更-视图架构.md`
- `/Users/mac/.dc-platform/memory/lessons/2026-08-17-血缘与现网不一致时以-prod-海豚-live-task-为准-旧表停更须-deprecated.md`

## AI 蒸馏原文（截断）

```
## 携带要点

- 周报「说人话」**不等于**改成日报结构；保留原周报版式（`### 专项分块`、`【卡点】`、下周表格、日报索引、「一句话给周会」），只改措辞。
- 周报正文去掉 `stage3`、`v189`、`bucket0`、`agent-bus`、`poller` 等内部说法，换成业务部门能直接听懂的表述；状态写「已完成」，不写「进行中」。
- 周报汇总口径：正式日报 + 双机 work-log 合并稿 + `hosts`；`new-mac` / `old-mac` 缺流水要**如实点明**，不假装双机齐全。
- 「一句话给周会」也要说人话：上周交付、当前卡点、本周动作各一句，方便复制进周会。
- AI 批量生成的列级血缘**未经人工复核**，上线前必须对照现网海豚 task SQL 逐列核对，错了会误导影响分析。
- 血缘核对 fallback：海豚元库连不上时，用 MCP 拉 live task SQL 做人工抽查，详报落 `.claude/database/reports/lineage_<bus>/`。
- 停更旧表 + 新表并行时（如 `dws_user_tag_d` vs `dws_user_tag_d_d`）：以**现网在跑 task 的目标表和 FROM/JOIN** 为准，旧表应 deprecated、为新表重建血缘。
- 列级血缘错不只来自源表选错：`WHERE` 过滤（如 `is_valid=1` 滤掉 bounce）会导致聚合列（如 `bounce_cnt`）与血缘描述不一致，prod 未发版时仓内 SQL 已对也仍算问题。
- prod 表可能是 **VIEW** 而非物理 INSERT，核对要先确认架构，不能按普通写入 task 套。
- 机器核对已覆盖表级拓扑（FROM/JOIN 双向比对）：106/123 一致、0 差异时，**不要再花时间验「上游是不是那几张表」**。
- 人工核对重心转到机器做不出的列级四类：同名列归属（JOIN 多表时的 `app_id`/`dt`/`channel`/`uid`）、WHERE 过滤、聚合口径、视图/停更表等特殊架构。

## 新坑/铁律

[LESSON: weekly-report|周报说人话只改措辞不改版式，保留原分块/卡点/周会一句话结构]

[LESSON: lineage|表级拓扑机器验过后，人工只盯列级：同名列归属、WHERE过滤、聚合口径、停更/视图架构]

[LESSON: lineage|血缘与现网不一致时以 prod 海豚 live task 为准；旧表停更须 deprecated 并为在跑新表重建血缘]
```

## 会话摘录（截断）

```
[user] [私聊#324] 周报整理也要和日报一样，说人话

[assistant] 按日报的「说人话」风格重写 W33 周报。先对照既有周报模板和上周工作记录。
按日报体例重写 W33 周报：扁平条目、业务表述、去掉内部黑话。
已按日报体例改好 W33 周报，定稿在 `.cursor/work-log/reports/2026-W33-周报.md`。

主要调整：
- 结构对齐日报：`【本周完成】` / `【死锁阻碍】` / `【下周动作】`
- 每条用 `[TQ-002 | DMP系统]`，约 40～70 字，句末「已完成」
- 去掉 stage3、v189、bucket0、agent-bus 等内部说法，改成业务表述
- 卡点并入 `【死锁阻碍】`，不写「进行中」

---

# 周报 · 又初·2026-W33（08-10 周一 ~ 08-16 周日）

[REPORT-ORG:天穹部门] [LEVEL:L1] [TYPE:周报] [DATE:2026-08-17]
> 提交人: 又初  
> 工号: DN6517  
> 岗位: 后端 BE  
> 层级: L1  
> 统计周期：自然周 W33 · 有效工作日 6 天（08-10～08-15）· 08-16 周日休息

## 【本周完成】
- [TQ-002 | DMP系统] 大漏斗事件统计完成 Spark 日批入库，并按样例产品核对展示量、页面浏览去重与明细源一致，点检通过，已完成；
- [TQ-002 | DMP系统] 大漏斗两阶段加工链路在样例产品上冒烟跑通，知秋侧确认可继续推进；日汇总表字段说明与建表语句已整理成交后端对接文档，已完成；
- [TQ-002 | DMP系统] 按知秋要求把用户停留时长怎么算、末页怎么处理、只看一页怎么计、五档区间怎么划四段口径写清并回执，已完成；
- [TQ-002 | DMP系统] 修复测试环境用户停留汇总表「跳出就离开」的会话长期未计入问题，发版验数通过，分档与源表对齐，相关字段说明已同步更新，已完成；
- [TQ-002 | DMP系统] 按知秋评审意见把页面访问分析指标文档按大漏斗模板重写并上传平台指标库，业务侧可按统一结构查阅口径，已完成；
- [TQ-002 | DMP系统] 按知秋要求整理指标库体系 v0.2 止血设计稿，明确三处上线硬门槛与优先换刀方案，本地定稿，已完成；
- [TQ-002 | DMP系统] 按知秋要求拉取 48 个项目搜索引擎占比，并核查测试环境数据概览与用户活跃两页活跃数不一致问题，复验为日批未完成时的中间态、两页口径一致，已完成；
- [TQ-002 | DMP系统] 完成站群来源搜索词一次性探查并回传 TOP 词条，启动历史明细补数已跑日期的行数核对；协助跟进漫画分析字段字典复审，口径层已通过并整理交底材料，已完成；

## 【死锁阻碍】
- [TQ-002 | DMP系统] 用户停留时长生产侧「只计…

[user] [私聊#325] 格式还是保持原来的，是让你说人话

[assistant] 保留原周报版式，只把正文改成通俗业务表述。
明白了：**版式按原来的**（分块、卡点、下周表格、日报索引、一句话给周会），只把措辞改成说人话。

已更新：`.cursor/work-log/reports/2026-W33-周报.md`

改动要点：
- 保留 `### 大漏斗 / 停留时长 / 页面访问…` 等原结构
- 去掉 stage3、v189、bucket0、agent-bus、poller 等内部说法
- 「卡点」「一句话给周会」也改成业务部门能直接听的表述

**一句话给周会**（供你复制）：

> **上周**大漏斗按天加工和样例试跑都通了，对接材料已交；页面访问指标上了指标库；停留时长在测试修通了跳出计入，但生产口径还在等产品拍板。**卡点**是跳出怎么算、大漏斗上游缺数、指标库止血稿还没上平台。**本周**先把停留口径定下来、指标库 v0.2 落地，再推大漏斗重跑验数。

还要改哪一段措辞，直接说。

[user] [私聊#326] 再发一遍周报给我

[assistant] # 周报 · 又初·2026-W33（08-10 周一 ~ 08-16 周日）

[REPORT-ORG:天穹部门] [LEVEL:L1] [TYPE:周报] [DATE:2026-08-17]
> 统计周期：自然周 W33 · **有效工作日 6 天**（08-10～08-15 周一至周六）· 08-16 周日休息 · 无法定假日  
> 汇总口径：正式日报 + 双机 work-log 合并稿 + hosts（`old-mac` / `new-mac`）  
> hosts 缺口（已点明、未假装齐全）：08-12～08-14 缺 `new-mac` 手写流水；08-16 双机无当日 hosts

## 【本周完成】

### 运营系统 · 大漏斗（自开 YC-FUNNEL-001）
- 大漏斗按天加工入库已跑通，并用样例产品核对：各类展示量、页面浏览去重与明细源对得上
- 两阶段加工在样例产品上试跑通过，知秋侧确认可以继续往下做
- 按后端对接需要，把日汇总表每个字段什么意思、建表语句整理成文档并已交付
- 全量压测因上游源表缺数先停跑，已转去查上游

### 用户停留时长（工作簿 #9）
- 按知秋要求，把停留怎么算、末页怎么处理、只看一页怎么计、五档区间怎么划，四段说明写清并回执
- 测试环境修了「用户点开就离开」这类会话长期没计入的问题，发版验数通过，分档与源表对齐
- 表上相关字段说明已补全（各档阈值、怎么判跳出、查数要带统计粒度等）
- **口径还没定死**：生产侧八月改法与七月定的「跳出单独成一档」对不上，近 45 天历史重跑先冻住，等产品/知秋拍板（见卡点）

### 页面访问与指标库
- 页面访问分析指标文档按大漏斗模板重写，已上传平台指标库
- 指标库体系 v0.2 止血设计稿本地定稿（三处必须先满足的上线条件 + 优先换刀方案），待上传平台逐项落地

### 数据核查与协作支持
- 按知秋要求拉 48 个项目的搜索引擎占比；数仓无现成指标时先用自然新增占比作参考，并交知秋侧核对网页端来源域名
- 核查测试环境 YC-001「数据概览」和「用户活跃」两页活跃数对不上：8/12 复验两页都是 15.98 万，上午差异是日批还没跑完时的中间态
- 站群来源搜索词一次性探查完成并回传 TOP 词条；历史明细补数已跑日期的行数核对已启动
- 协助跟进漫画分析字段字典复审：v2.5.2～v2.5.4 口径层已通过，互动覆盖范围、渠道兜底等交底材料已整理进团队知识库

### 工程与协作基建
- 工作簿进展改为群里同步知秋，同时走私信通道双发，避免漏看
- 工作簿项名与团队编号对齐（页面统计、渠道归因、设备标签边界）
- 修了消息轮询超时没续心跳导致服务反复重启；日报定时推送里主机名误粘注释的问题也已修

## 【卡点】

- **用户停留「跳出单独…

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 自动初筛 16 张表均通过；接下来抽查几张关键表，并核对现网任务是否定位正确。

[REDACTED]

[assistant] 海豚元库连接失败，初筛未对照现网 SQL。改用 MCP 拉 live task 并做人工抽查。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[user] <mcp_meta_tools>
You have access to MCP (Model Context Protocol) tools through `GetMcpTools` and `CallMcpTool`.

## MCP Tool Discovery and Invocation

Use `GetMcpTools` to discover tool schemas, then `CallMcpTool` to invoke them. Aim to minimize round-trips: ideally one `GetMcpTools` call followed by one `CallMcpTool` call.

If the user mentions, references, or links to a product or service that corresponds to an available MCP server, and the request likely depends on information from that service, proactively inspect that MCP server before answering. Do not wait for the user to explicitly ask you to use MCP. If you are unsure which server matches, use `GetMcpTools` with a pattern based on the service name.

`GetMcpTools` supports four modes:

1. `{"server":"<id>"}`: returns full input sch…

[user] <timestamp>Monday, Aug 17, 2026, 4:26 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#6597 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 6597 --text "[ACK] bus#6597 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 6597 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：【血缘核对派单 · 请在 2026-08-18 18:00(北京)前回执】发起人: 狂人(worker_ant)  ## 背景  今天我把 dc-parent 全部 123 张 online 表的**列级血缘**重建并推到了开发服务平台(server run_id=35, 3423 条边)。血缘是我用 9 组 AI 并行读 ETL 生成的, **没有经过人工复核**。血缘错了会误导所有看血缘图做影响分析的人, 所以现在需要你帮忙核一遍。  **你要做的事**: 拿到下面分给你的表, 对每一张, 把平台上的血缘和真实的现网代码逐列对一遍, 找出对不上的地方。  ## 怎么看血缘  ```bash curl -s "…

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] 继续完成 bus#6597：深入核对 4 张问题表，并整理正式回执。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]

[assistant] bus#6597 已处理完毕：已回执知秋（bus reply id=6613），详报落在 `.claude/database/reports/lineage_bus6597/reply_20260817.md`。

## 核对结论（16 表）

**11 张无问题**：源表与 prod 海豚 SQL 一致。  
**5 张有问题**（按严重程度）：

### 1. `dws.dws_user_tag_d`（严重）
- 血缘仍指向 6 张 DWD；现网 task `dws_user_tag_d_d` 写入 **`dws_user_tag_d_d`**，源为 `dim_user_all` + `dwm.*` + `dws_user_finance_d`
- `dws_user_tag_d` 停更于 **2026-06-06**；`dws_user_tag_d_d` 在跑到 **2026-08-06**
- `lifecycle_flag` 等列源表全错，建议 deprecated 旧表、为 `d_d` 重建血缘

### 2. `dws.dws_session_duration_user_d`（列级）
- 表级源 `dwm_app_session_sid_d` 正确
- **`bounce_cnt`**：血缘写 `SUM(is_bounce)`；prod task `180283360953472` 仅 `WHERE is_valid=1`，bounce 被滤掉
- prod T-1：`bounce_sum=0`，dwm `is_bounce≈9M`；仓内 SQL 已改，**prod 未发版**

### 3. `dws.dws_app_retention_d` / `dws.dws_app_user_m`（架构）
- 两张表 prod 已是 **VIEW**（非物理 INSE…

[user] <timestamp>Monday, Aug 17, 2026, 4:41 PM (UTC+8)</timestamp>
<user_query>
[agent-bus 入站 bus#6610 from worker_ant mode=work] 在本【同一 Cursor 主会话】处理（勿 spawn 新 Agent）。铁律：①60秒内先执行 ack → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind ack --reply-to-bus-id 6610 --text "[ACK] bus#6610 收到，开干"；②干完再 reply → python3 .claude/database/scripts/notify/agent_bus_send.py --to worker_ant --kind reply --reply-to-bus-id 6610 --text "..."；③reply 成功前不要 mark 结案。读完 bootstrap；正文：【狂人 · 补充: 请调整核对重点, 能省你一半时间】  我刚跑完一轮**机器核对** —— 把血缘里的源表和"现网真在跑的"海豚任务 SQL 的 FROM/JOIN 做了双向比对(查漏边也查多边)。结果:  **123 张表里 106 张表级拓扑完全一致, 0 张有差异**; 另 17 张是视图或没有在跑写入任务的表(已单独查清)。  ⇒ **"这张表的上游是不是那几张表"这个层面基本已经验过了, 你不用再花时间在这上面。**  请把力气全压在**机器核不出来的列级细节**上, 按这 4 条重点看:  1. **同名列归属**: JOIN 多张表时, app_id / dt / channel / uid 这类…

[assistant] 收到 bus#6610：先 ACK，再按知秋要求把核对重心转到列级细节。

[REDACTED]

[assistant] [REDACTED]

[assistant] 表级拓扑已由机器验过；接下来对 16 张表做列级深挖，重点查过滤条件、聚合和同名列归属。

[REDACTED]

[assistant] [REDACTED]

[assistant] [REDACTED]
```
