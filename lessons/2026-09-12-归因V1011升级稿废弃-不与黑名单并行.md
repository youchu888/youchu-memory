---
date: 2026-09-12
tags: [attribution, is_run_sync, V1.0.11, blacklist]
severity: high
domain: ops
---

# 归因 V1.0.11 升级稿废弃，不与黑名单并行

## 背景

知秋要的是黑名单方向。V1.0.11 升级稿（含 `is_run_sync` 回写）与黑名单不能并行。2026-09-12 处置定为废弃，不是等黑名单上线后再谈。

## 坑 / 错误做法

- 继续推进 `dev-20260911-001` 的 request-publish，或再补数、再改 is_run_sync。
- 把「已撤回」理解成「先搁置，黑名单上了再提审」。

## 正确做法

- 升级稿停手：不再开跑、不再提审。
- 已撤回 `dev-20260911-001` 对野花的 pending 发布（`publish_request_status=none`）。
- 前序 `dev-20260907-001` 已于 2026-09-08 approved（publish_run 722）。废弃不等于回滚 prod；prod 未动。
- test 日批 `dim_app_attribution_config_is_run_sync_d`（task `22955590456576`，`wf_dws_汇总_日`）今早定时已跑完。wf 上有 RUNNING 补数时不摘任务，避免整链 OFFLINE。

## 验证

- `POST /dev-sessions/dev-20260911-001/withdraw-publish` 返回 `status=none`。
- 开放项不再把 V1.0.11 升级稿标成 pending 提审。
