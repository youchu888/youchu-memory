---
date: 2026-08-03
tags: [away, launchd, tgbot, vpn, prevent-sleep, ops]
severity: high
domain: ops
---

# 半个月离机长跑：进程体检与加固

## 已确认健康

| 组件 | 状态 |
|------|------|
| tgbot + Telethon 查岗/在线 ping | OK（同 auth 双连已修） |
| agent-bus poller + wake-bridge | OK |
| cursor-executor（away.flag） | OK KeepAlive |
| vpn-sync 30min + away-health 强制续期 | OK |
| memory-git-sync 10min | OK |
| away-health 3min 自愈+TG 告警 | OK |
| jike 上下班计划 | OK（随 bot） |

## 本轮加固

1. `com.youchu.prevent-sleep` → `caffeinate -dims` KeepAlive（不依赖 ToDesk/Cursor）
2. away-health 增加大日志轮转（wake_feed/poller/daemon 等）
3. 已轮转 337MB wake_feed + 75MB poller；写回 `away.flag`

## 离机前主人注意

1. **电源**：保持 MagSafe；优化充电可能停在 ~80%，断电后靠电池
2. **请假日历**：若整段不上班，用 `set_leave_day.py` 写请假日（跳过打卡/抽查/在线保活时段逻辑）；**勿停 VPN**
3. **CHcode git 日同步**：工作区 dirty + behind，每日 09:05 **SKIP pull**——离机期间仓内代码不会自动跟上；勿依赖本机 dirty 树自动更新
4. IDE 可不挂；Away 靠 **cursor-executor** 接 bus

## 自检命令

```bash
bash .cursor/scripts/agent-bus-poller-check.sh
bash "$HOME/Library/Application Support/youchu-agent-bus/away-health-run.sh"
pgrep -lf 'bot.py|caffeinate|agent_bus_poll|cursor_executor'
```
