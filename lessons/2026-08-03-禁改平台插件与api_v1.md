---
date: 2026-08-03
tags: [git, ownership, vscode-extension, dc-platform-server, 禁改]
severity: high
domain: ops
---

# 提交只动 ops_system：禁改平台插件与 api_v1

## 背景

`vscode-extension/`、`dc-platform-server/`（含 `api_v1.py`）不是我维护的范围。误把补数 API + 插件文案跟 ops_system 一起 push，已撤回。

## 坑 / 错误做法

- 为「顺手修好补数权限/文案」改平台 API / 插件并 commit push
- 把非主责改动和 `ops_system` 业务改动混在同一 commit

## 正确做法

1. 提交前对照 `~/.dc-platform/memory/project_chcode_directory_index.md`：标 **禁改** 的路径一律不 stage
2. 可提交范围默认：`ops_system/**`、本人 session/playbook/ETL、明确交办的脚本
3. 平台/插件问题：报知秋/平台侧或只本地试验，**不入库**
4. 若已误 push：新 commit 恢复禁改文件到改前版本再 push（勿 force）

## 验证

- `git diff --cached --name-only` 无 `vscode-extension/`、`dc-platform-server/`
- 远程仅剩 `ops_system` 相关 diff

## 关联

- 目录地图：`project_chcode_directory_index.md`（vscode-extension / dc-platform-server ❌ 禁改）
- 撤回提交：`381e5cd0`
