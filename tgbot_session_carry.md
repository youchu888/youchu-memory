# TG 会话热携带（轮换沉淀 · 自动维护）

> 更新：2026-09-24 · 最新归档：`sessions/tg-rotate-2026-09-24-1902.md`
> 用途：Cursor resume 清空后，新会话仍能继承关键铁律/结论。

## 携带要点

- 同一私聊题重复问（如 #514 → #515）：**不必再跑一遍长搜**；直接给 ** condensed 结论 + 链接 + 可抄交互表**，并主动问是否要做 **列表+详情线框** 对照 B′ 落地。
- 「重启 agent」：冷启动后 **一句话交代上一轮主题**（如指标库 UI 参考），**让用户选续做项**，不要默认接着干。
- [LESSON: metric-library,ui,product|指标库前端对齐 B′：发现页大搜索+认证/lifecycle 徽章；详情左口径右实现+血缘；统一「指标超市」入口，勿拆多菜单。]
- [LESSON: agent-bus,tg,dedupe|用户短时间重复同一调研类私聊，复用已有结论短答，避免重复全网检索与长文复述。]
- 指标库 UI 调研可固定对标：**Atlan**（治理型目录：搜索、认证徽章、Owner、术语关联）、**GoodData Analytics Catalog**（Certified/Draft/Inherited 分层 + 按认证筛选）、**PostHog Data Catalog**（列表 proposed/approved → 详情可编辑定义、漂移告警、试跑、血缘；可看 GitHub PR 交互）、**观远指标中心**（国内完整：超市卡片/列表 + 详情五 Tab：信息/血缘/应用/版本/操作史 + 收藏订阅）、**Hex Explore**（选指标后只展示合法维度，自助取数）。
- 发现页共性：**大搜索框**；结果卡片带 **认证徽章、一句业务口径、Owner、新鲜度**；默认筛 **「只看已认证」**；lifecycle 用 **绿/黄/灰** 区分状态。
- 详情页共性：**左侧业务口径、右侧技术实现（SQL/表字段）**；下方 **血缘 + 下游看板/应用**；与 B′（概念层 + 实现层 + lifecycle）可直接对齐。
- 信息架构：**单一入口「指标超市」**，避免指标分散在多个菜单。
- 延伸阅读：**beefed.ai** 的 metrics catalog discovery experience 长文，补「发现体验」原则。
- 「重启 bot」：不能假定一次 `restart.sh` 成功；**TG API 超时时要查网络/VPN**，再走 **daemon 自愈 + 手动拉起 + 验收 API 是否通**。
- 修复要点：失败不占 20h 冷却；旧证复用不能当续期成功；续不上应报错/红灯，禁止假绿灯。
- [LESSON: vpn-sync,ovpn,launchd|仅当 ovpn 成功导入且 `imported_at` 更新后再写冷却；失败清冷却或勿写；过期旧证禁止「复用跳过导入」并打 ✅]
- [LESSON: cursor-agent|连接失败丢弃 resume 时勿假设上下文还在，须冷启动记忆后重跑或让用户重发/新开 agent]
- VPN「没自动续」先查 launchd 是否在跑；常见是脚本假成功，不是定时任务停了。
- 续期链路：`/request` → 生成 ovpn → 导入 → 拉起 OpenVPN；任何一步失败都不应记成「已续期成功」。
- **假成功模式**：失败仍写入 20h 冷却 → 后续轮询「距上次 /request 不足 20h」→ 复用本地旧 ovpn → 跳过导入 → 日志打 ✅，证书其实已过期。
- 判因要看「上次**真正导入**时间」（如 `imported_at`），不能只看「上次尝试过 /request」的时间戳。
- 卡死时：查完整 `/request` 日志 + 本地 ovpn 日期，先强制续一次，再改脚本逻辑。

