# TG 会话热携带（轮换沉淀 · 自动维护）

> 更新：2026-09-24 · 最新归档：`sessions/tg-rotate-2026-09-24-0608.md`
> 用途：Cursor resume 清空后，新会话仍能继承关键铁律/结论。

## 携带要点

- 修复要点：失败不占 20h 冷却；旧证复用不能当续期成功；续不上应报错/红灯，禁止假绿灯。
- [LESSON: vpn-sync,ovpn,launchd|仅当 ovpn 成功导入且 `imported_at` 更新后再写冷却；失败清冷却或勿写；过期旧证禁止「复用跳过导入」并打 ✅]
- [LESSON: cursor-agent|连接失败丢弃 resume 时勿假设上下文还在，须冷启动记忆后重跑或让用户重发/新开 agent]
- VPN「没自动续」先查 launchd 是否在跑；常见是脚本假成功，不是定时任务停了。
- 续期链路：`/request` → 生成 ovpn → 导入 → 拉起 OpenVPN；任何一步失败都不应记成「已续期成功」。
- **假成功模式**：失败仍写入 20h 冷却 → 后续轮询「距上次 /request 不足 20h」→ 复用本地旧 ovpn → 跳过导入 → 日志打 ✅，证书其实已过期。
- 判因要看「上次**真正导入**时间」（如 `imported_at`），不能只看「上次尝试过 /request」的时间戳。
- 卡死时：查完整 `/request` 日志 + 本地 ovpn 日期，先强制续一次，再改脚本逻辑。
- GitLab 命令行用 Token：网页建 PAT → 域名级代理（如需）→ `remote` 改 HTTPS → 钥匙串存凭证 → `git ls-remote` 验收。
- 对外说明**只写步骤**，不带任何真实 token；对方用自己账号生成 PAT。
- 私聊/bus 发失败或 Cursor「会话连接失败、丢弃旧 resume」时，改发机器人群（如 `@mudan99_bot`），并让用户重发或「重启 agent」。
- 同一 howto 可多渠道：先前 bus 已发过，仍可按主人要求再发群，内容保持一致、不含密钥。
- 后台扫盘/巡检任务超时或停止，若计划时间已从可靠配置得出，应单独声明「不影响打卡结论」，勿让运维噪音盖过主答案
- 网络/API 异常（Cursor API 失败、ECONNRESET）时，**不要**因此反复触发打卡或上传；先确认主人是否已手工完成对应动作
- [LESSON: punch|主人说已手动打卡时，禁止再重试或自动补打，当次打卡视为已完成]
- 主人明确「今晚/今天已手动打完卡」→ 当日不再自动重试、补打或二次触发打卡流程
- 手动完成的是**前一日下班卡**时，须与**当日**自动上下班计划分开说明，避免误判「今日已打满」
- 查打卡计划的标准答法：上班/下班**计划时刻** + **打卡窗口**（例：09:50:40，窗口 09:30–10:00）

