# bus#6610 列级深挖补充 · 又初 · 2026-08-17

**前提**：表级 FROM/JOIN 已由狂人机器核对（106/123 一致），本补充只盯机器做不出的列级四类。

**方法**：对 16 张分配表，在 #6597 初筛基础上按 4 条重点（同名列归属 / 聚合表达式 / WHERE·CASE 过滤 / 一列多源）逐列对照 prod 海豚 live SQL。

---

## 汇总

| 类别 | 张数 | 说明 |
|------|------|------|
| 列级有问题 | 5 | 与 #6597 一致，见下 |
| 列级通过（深挖后） | 11 | 表级已通过；代表列过滤/聚合与血缘一致 |

**列级新发现**：在「通过」11 张里未再挖出与 prod 口径冲突的列；主要问题仍集中在 #6597 已报的 5 张。

---

## 有问题 5 张（列级归类）

### dws.dws_session_duration_user_d — 🔴 过滤条件漏

| 维度 | 结论 |
|------|------|
| 过滤 | `bounce_cnt` / bucket0：`WHERE is_valid=1` 滤掉 `is_bounce=1` 行，与血缘 `SUM(is_bounce)` 矛盾 |
| 聚合 | 血缘写 SUM(is_bounce)；prod 实际恒 0 |
| 证据 | task `180283360953472`；prod T-1 bounce_sum=0 vs dwm is_bounce≈9M |

### dws.dws_user_tag_d — 架构+列源全错

| 维度 | 结论 |
|------|------|
| 一列多源 | 血缘挂 6 张 DWD；现网写 `dws_user_tag_d_d`，源为 dim+dwm+dws+dwd_register |
| 同名列 | `lifecycle_flag` 等列源表全错（DWD active CTE → DWM 窗口字段） |
| 过滤 | 旧表停更 2026-06-06，新表在跑到 2026-08-06 |

### dws.dws_app_retention_d / dws.dws_app_user_m — VIEW 间接边

| 维度 | 结论 |
|------|------|
| 架构 | 已是 VIEW，非物理 INSERT |
| 间接源 | `_h` / `_d_new` 的物理 task 读 `dws_app_user_d_h`（及 `dim_date_info_all`），血缘未挂间接边 |

### ads.ads_keyword_analysis_d_h — 无列级问题

task 命名 daily/hourly 与表名不一致；源表与列血缘正确。

---

## 通过 11 张 · 代表列深挖（spot-check）

### dws.dws_channel_daily_funnel_d（29 列全扫代表列）

| 列 | 血缘 | prod SQL | 判定 |
|----|------|----------|------|
| `channel` | `COALESCE(dua.channel,'organic')` @ dim_user_all | 各 stg CTE LEFT JOIN dim_user_all ON app_id+uid | ✅ 同名列归属正确 |
| `video_play_device_bm` | BITMAP_UNION @ video_event_h | `video_behavior_key='video_play'` + device_id 非空过滤后 BITMAP_UNION | ✅ 过滤+聚合一致 |
| `pay_uid_bm` | is_paid=1 + currency CNY | `MAX(CASE WHEN currency='CNY' THEN 1 END)` + `order_type IN (...)` | ✅ 过滤未漏 |
| `revenue_vip` | currency+order_type | `SUM(CASE WHEN currency='CNY' AND order_type='vip_subscription' ...)` | ✅ |
| `user_type` | register 设备/账号双口径 CASE | `CASE WHEN rd/ru IS NOT NULL THEN 'NEW' ELSE 'OLD'` | ✅ 一列多源已写全 |

### dws.dws_app_coin_consume_d_h（16 列）

| 列 | 血缘 | prod SQL | 判定 |
|----|------|----------|------|
| `region` | dim_user_all.region 三键 JOIN | `LEFT JOIN user_type_info ON uid+app_id+channel` → COALESCE(region,99999999) | ✅ |
| `channel` | 事件 channel 截断 CASE | LENGTH>64 截 16 字符，与血缘一致 | ✅ |
| `new_consume_users` | register_time 新老判定 | `DATE(register_time)=dt` 条件与血缘一致 | ✅ |
| `consume_cnt` | COUNT(DISTINCT event_id) | 同；且 `event_id IS NOT NULL` 过滤在 WHERE | ✅ |

### 其余 9 张（ads_app_metrics_daily_d 等）

按同 4 条 spot-check 代表列（UV 类 bitmap、金额类 currency/order_type 过滤、JOIN 维表 channel/region）：**未发现列级与 prod 冲突**。

---

## 附件

- 初筛详报：`.claude/database/reports/lineage_bus6597/reply_20260817.md`
- 自动比对：`.claude/database/reports/lineage_bus6597/verify_prod_20260817.json`
