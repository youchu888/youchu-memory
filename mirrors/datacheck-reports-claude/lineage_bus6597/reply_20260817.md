# bus#6597 血缘核对回执 · 又初 · 2026-08-17

**范围**：16 张表 / 652 条边（DWS×10 + ADS×6）  
**真值**：海豚 prod 现网 SQL（project `171982119739200`）  
**方法**：平台 lineage API + `dolphin.get_task_sql` 逐表比对源表与关键列

---

## 汇总

| 结论 | 张数 | 表 |
|------|------|-----|
| 无问题 | 11 | 见下「通过」 |
| 有问题 | 5 | `dws_session_duration_user_d`、`dws_app_retention_d`、`dws_app_user_m`、`dws_user_tag_d`、`ads_keyword_analysis_d_h`（末张仅 task 命名，源表无误） |

---

## 有问题（逐表）

### 1. dws.dws_session_duration_user_d

**表级源**：`dwm.dwm_app_session_sid_d` ✅（task `180283360953472` / wf `wf_dws_汇总_日`）

| 列 | 血缘说什么 | 现网 SQL 实际 | 证据 |
|----|-----------|--------------|------|
| `bounce_cnt` | `SUM(CASE WHEN is_bounce=1 THEN 1 ELSE 0 END)` 来自 `dwm.is_bounce` | session 段 `WHERE is_valid=1` 过滤，**bounce 行（is_bounce=1, bucket=0, is_valid=0）被排除**，`bounce_cnt` 恒为 0 | task `180283360953472` L31 `AND is_valid = 1`；prod T-1：`bounce_sum=0` vs dwm `is_bounce=8550961` |
| `duration_bucket`（session） | 直接来自 dwm | 同上过滤，**bucket=0 不进表** | 同上 |

> 仓内 ETL（`ops_system/04.dws/dws_session_duration_d/dws_session_duration_user_d.sql`）已改为 `is_valid=1 OR (is_bounce=1 AND duration_bucket=0)`，**prod 未发版**。血缘按仓内/设计口径生成，与 prod 真值不一致。

---

### 2. dws.dws_app_retention_d

| 项 | 血缘 | 现网实际 |
|----|------|---------|
| 写入方式 | 隐含物理表 ETL | **VIEW**，`SELECT … FROM dws.dws_app_retention_d_h`（`SHOW CREATE VIEW` / `dws_app_retention_d_view_ddl.sql`） |
| 直接上游 | `dws.dws_app_retention_d_h` | ✅ VIEW 定义一致 |
| 间接上游缺边 | 无 | `_h` 日 task `174729603403587` 物理读 `dws.dws_app_user_d_h`，**血缘未挂** |

无同名 INSERT task（旧物理表已下线）——属**架构变更未反映**，非单纯缺边。

---

### 3. dws.dws_app_user_m

| 项 | 血缘 | 现网实际 |
|----|------|---------|
| 写入方式 | 隐含物理表 | **VIEW**，`bitmap_union_count(*) FROM dws.dws_app_user_m_d_new GROUP BY …` |
| 直接上游 | `dws.dws_app_user_m_d_new` | ✅ VIEW 定义一致 |
| 间接上游缺边 | 无 | task `174729603403585`（`dws_app_user_m_d_new`）读 `dim.dim_date_info_all` + `dws.dws_app_user_d_h`，**血缘未挂** |

---

### 4. dws.dws_user_tag_d ⚠️ 严重

| 项 | 血缘 | 现网实际 |
|----|------|---------|
| 现网写入表 | `dws_user_tag_d` | task `21949259453184`（`dws_user_tag_d_d`）**INSERT→`dws.dws_user_tag_d_d`** |
| 数据新鲜度 | — | `dws_user_tag_d` max calc_dt=**2026-06-06**（冻结）；`dws_user_tag_d_d` max dt=**2026-08-06**（在跑） |
| 表级源 | 6 张 DWD（page_view/login/register/video/ad/order） | `dim.dim_user_all` + `dwm.dwm_app_active_user_d` + `dwm.dwm_user_video_d` + `dwm.dwm_user_ad_d` + `dws.dws_user_finance_d` + `dwd.dwd_user_register_d_v2`（仅 reg_device） |

| 列示例 | 血缘 | 现网实际 |
|--------|------|---------|
| `lifecycle_flag` | CASE 基于 `dwd_app_page_view_d`/`dwd_user_login_d_v2`/`dwd_user_register_d_v2` 的 active CTE | CASE 基于 `dwm_app_active_user_d` 窗口字段（`last_N_login_day`/`has_prior_active`/`d_active`） |
| `last_7_video_day` 等 | `dwd_video_event_h` | `dwm_user_video_d` 聚合 |
| `last_7_ad_click` 等 | `dwd_ad_click_h` | `dwm_user_ad_d` 聚合 |
| `total_pay_amount` 等 | `dwd_order_paid_d` | `dws_user_finance_d` 聚合 |

**结论**：血缘对应**已停更的旧表+旧 DWD 链路**；现网真表是 `dws_user_tag_d_d` + DWM 中间层。建议平台侧：**下线 `dws_user_tag_d` 血缘或标 deprecated**，为 `dws_user_tag_d_d` 重建。

---

### 5. ads.ads_keyword_analysis_d_h

| 项 | 血缘 | 现网实际 |
|----|------|---------|
| task 名 | `ads_keyword_analysis_d_h` | `ads_keyword_analysis_d_h_daily`（`21897365954048`）+ `_hourly`（`21925427200256`） |
| INSERT 目标 | `ads.ads_keyword_analysis_d_h` | ✅ 一致 |
| 源表 | `dwd_keyword_search_h`, `dwd_keyword_click_d`, `dim_user_all` | ✅ 一致 |

**结论**：源表血缘正确；仅 task 命名与表名不一致（可接受，建议在平台标注别名）。

---

## 通过（11 张）

- `dws.dws_ad_request_metric_d` — 核对 5 列，无问题（task `180032172673152`）
- `dws.dws_app_channel_retention_d` — 核对 7 列，无问题（task `174729603403602`）
- `dws.dws_app_coin_consume_d_h` — 核对 16 列，无问题（日 `180989598224512` + 小时 `180989601539200`）
- `dws.dws_channel_daily_funnel_d` — 核对 29 列，无问题
- `dws.dws_user_first_recharge_retention_d_h` — 核对 37 列，无问题
- `dws.dws_user_promotion_behavior_h` — 核对 23 列，无问题
- `ads.ads_app_metrics_daily_d` — 核对 50 列，无问题（task `174729507871558`）
- `ads.ads_channel_metrics_daily_d` — 核对，无问题
- `ads.ads_channel_summary_d` — 核对，无问题
- `ads.ads_product_day_stat_d` — 核对 53 列，无问题（task `174729507871563`）
- `ads.ads_user_page_path_d` — 核对 7 列，无问题

---

## 附件

- 自动比对 JSON：`.claude/database/reports/lineage_bus6597/verify_prod_20260817.json`
- 脚本：`.claude/database/scripts/lineage_verify_assigned_prod.py`
