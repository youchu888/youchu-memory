# youchu agent memory（双 Mac 共享）

又初 / Cursor 长期记忆：lesson、feedback、工作簿、协作习惯。

- **不要**提交 token / `.env` / 私钥（见 `.gitignore`）
- **不要**把本仓库合进 `dc-parent`（独立私有库）
- 同步：`bash ~/.dc-platform/scripts/sync-memory-git.sh`

机器角色不变：旧 Mac 跑 bot；两边只共享本目录内容。
