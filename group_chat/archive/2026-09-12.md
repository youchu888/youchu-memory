# 监控群聊上下文归档 · 2026-09-12

> chat_id: `-5376962870` · 记录数: 3 · 归档时间: 2026-09-13 14:13

## 摘要

- 旁听 0 条 · 对话 3 条 · agent-bus 0 条
- 关键词: app, flag, ##, **0**, **1**, **一次注册, **不能并行**, **同一个

## 旁听

| 时间 | 发言 | 内容 |
|------|------|------|
| — | — | 无 |

## 对话（又初参与）

| 时间 | 用户 | 问题 | 又初回复 |
|------|------|------|----------|
| 2026-09-12 10:27:31 | 用户 | [私聊#480] is_run的码值1是什么0是什么 | 先查元数据里 `is_run` 的字段注释，再给你准确定义。`dim.dim_app_attribution_config.is_run`：  - **1**：已启用（前端主列表只看 `IFNULL(is_run,0)=1`；日批 sync 对新入围 app 自动写成 1） - **0**：未启用 / 拉黑（不进主列表；sync 不会把已有行改回 1，黑名单会保留）  补充：现网归因计算已不靠 ` |
| 2026-09-12 12:26:16 | 用户 | [私聊#481] ## 五、需要你确认的一件事  黑名单版和你们那份 V1.0.11 升级稿是两个方向，**不能并行**。这次知秋要的是黑名单。你们那份升级稿（含 is_run_sync 回写任务）现在的处置 —— 是废弃、还是等黑名单上线后再谈 —— 请找知秋确认，别自己开跑。  这个要废弃了 | 理解为：归因 V1.0.11 升级稿（含 is_run_sync 回写）废弃，黑名单方向单独走。先核对 session 和待审状态，再停掉并回知秋。待审单还挂在野花名下。先撤回发布申请，再通知知秋：升级稿作废，不和黑名单并行。升级稿还在野花的待审队列里。先撤回申请，并核对 is_run_sync 有没有还在跑。待审已撤回。接着看 is_run_sync 是否还挂在日批上，并核对那条还在跑的补数。升 |
| 2026-09-12 14:05:34 | 用户 | [私聊#482] - 系统为 iOS。 - 注册渠道为自然渠道。 - UID 非空。 - 同一归因链路中的 app_install.attribution_flag=1 - 同一归因链路中的 user_register.attribution_flag=1 这个，什么叫统一归因链路？怎么跟app_install这个关联 | 先对一下入围 SQL 里注册和安装是怎么接上的，再用这个条件解释「同一归因链路」。「同一归因链路」不是一张表，也没有单独的链路 ID。它指的是：**一次注册，能和一次安装配成一对**。配上了，才算这条注册进了归因计算。  配对只看这三件事，全中才算同一条：  1. **同一个 app**：注册的 `app_id` = 安装的 `app_id`。 2. **同一台设备**：注册的 `device_i |
